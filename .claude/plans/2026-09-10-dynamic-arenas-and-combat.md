# Plan: Dynamic arenas, abilities, and tactical personalities

**Goal:** Add continuously roaming hills, shared cliff/road terrain, charge and timing-sensitive shield/stun, room movement controls, and configurable tactical opponents through shared authoritative mechanics.
**Out of scope:** Deployment/push, finishing prior human/live race playtests, 3D/jumping, held-shield stamina, new account/host roles, LLM/RL bots, arbitrary terrain editors, rollback networking, and camera redesign.
**Status:** Steps 1–23 are implemented; Step 23 passed both advisory C++ lanes 69/69, full web 986/986, and the complete browser gate 24/24 without retries or skips. The owner accepted Step 5's bounded physics model and has now chosen to retain current bot controls with an explicit acceptable-overshoot contract. Step 24 is in review: correct the braking guarantee, add production-loop evidence, resolve the carried formatting drift and two overstated configuration comments, then hand off the still-required native Linux/amd64 release and benchmark gates. All Mac/arm64-hosted Linux/amd64 results are advisory. Keeper's stable interior, Bully's recovery path and ordinary-thrust shove remain deferred. No push or deployment is authorized.

## Context

- Baseline `b282a90` includes the local camera implementation; the worktree was clean when planning began. The recorded live hill deployment is older and is not changed by this plan.
- `docs/architecture/0008-dynamic-arenas-and-combat.md` is the proposed design contract. On 2026-09-10 the owner confirmed adjustable numeric acceleration/top-speed controls with live room-wide updates. Tap shield, timings, normal propulsion ceiling semantics, cooperative admission phases, Apply/Reset interaction, and session v3 remain proposals; these two confirmed choices do not approve the entire design.
- Current contacts are discrete (`src/simulation/game_simulation.cpp:453`); high-speed non-contact is an accepted old test (`tests/unit/simulation/physics_tests.cpp:353`). Continuous motion requires an explicit ADR 0003 amendment and reviewed fixture changes, not a claimed behavior-preserving refactor.
- Shared respawn retains non-body components (`src/gameplay/shared/respawn_system.cpp:67`). Race currently records checkpoint/finish before endpoint off-road elimination (`src/gameplay/race/race_mode.cpp:50`). Both constrain ability/terrain lifecycle work.
- Controllers cannot link gameplay (`src/controllers/CMakeLists.txt:19`); geometry belongs below both consumers. Room tuning cannot mutate process-global configuration (`src/server/game_api_router.cpp:625`).
- `./scripts/verify-focused '<regex>'` is the existing incremental CTest wrapper, with no-tests rejection. `./scripts/verify-linux release` is the full host-orchestrated matrix including native build, four C++ lanes, web/browser, bounded fuzzing, and dependency evidence. It does not authorize publishing/deploying. Mac-hosted emulation remains advisory.
- Proposed filenames below identify intended canonical modules; they do not claim those files already exist. Register all new production/test files in the existing explicit CMake targets. Keep each implementation checkpoint buildable; do not ship partially implemented v3 development commits.

### Facts verified in the tree by the 2026-09-10 design review

- **A contact response cannot see gameplay state.** `ContactRule::Response` receives two `Subject`s, the contact, and a `TickContext` that exposes only the tick, the delta, the simulation config, the map, and the spatial index (`src/simulation/contact_rule.hpp`, `src/simulation/tick_context.hpp:79-92`). Predicates read the committed world; responses do not. The only way gameplay state reaches a response today is which row's predicates matched, which is the flag-combination table ADR 0008 rejects. `ContactResponse` can replace two bodies and emit events, nothing else; it has no motion termination.
- **A mode has seven declarations, read once at construction** (`src/simulation/game_mode.hpp`, `src/simulation/game_simulation_setup.hpp:30-39`). ADR 0004 § "The tick" and ADR 0003 § "Canonical tick" both state the kernel has exactly two policy sockets and no third. A motion-trigger socket is therefore a versioned amendment to both, plus an eighth declaration.
- **Thrust maximum is authored once per mode section**, `[royale]`, `[king_of_the_hill]`, and `[race]` (`src/application/application_config_loader.cpp:118,124,135`), validated by three configurations, and passed to three `ThrustSteeringSystem::create(...)` calls. The key also appears in `config/blob-royale.cfg`, `deploy/ubuntu-pc/blob-royale.cfg`, every `frontend-react/e2e/fixtures/*.cfg`, every `tests/fixtures/replays/*/match.ini`, the fuzz corpus under `tests/fuzz/corpus/application/`, `tests/integration/server_process_fixture.cpp`, and `tests/unit/application/application_input_test_fixture.hpp`. `[simulation] drag_per_second` is the precedent for a physical parameter authored once below every mode.
- **One random generator.** `GameWorld::random()` is a single SplitMix64 seeded from the match seed; hazards draw from it (`src/gameplay/shared/hazard_spawn_system.cpp:60`) and the snapshot publishes one `random_draw_count` (`src/simulation/world_snapshot.hpp:76`). Every bot receives the same match seed (`src/application/bot_reconciliation.cpp:110`).
- **The race road is copied every tick.** `CoursePublisherSystem` stamps `track` and `track_half_width` into `RaceModeState` at `kLifecycle`; the client draws from that copy (`frontend-react/src/features/simulation/rendering/raceCourseRenderer.ts`), and the racer bot re-implements the nearest-segment projection (`src/controllers/racer_controller.cpp:24`) because nothing below gameplay owns it (`src/gameplay/race/race_course.cpp:79`).
- **Body-bound cleanup is per owner.** Respawn erases only the `PhysicsBody` and knows no other kind; each owner clears its own counter (`src/gameplay/shared/respawn_system.hpp`). `c84e2fb` fixed the hill's missed clear on the knockout tick (`src/gameplay/king_of_the_hill/hill_scoring_system.cpp`, pinned by `tests/unit/gameplay/king_of_the_hill/hill_return_timing_tests.cpp`). Royale needs no clear because `placement_recorder` destroys the entity. `ComponentPublication<T>` (`src/simulation/component_publication.hpp`) is the model for a per-kind trait the registry consumes generically.
- **Timers count down.** `RespawnTimer` and `Lifetime` decrement with a write per tick; nothing yet expresses an absolute-tick half-open window.
- **Hill motion holds no committed state.** `hill_center` is a pure function of the markers and the elapsed running ticks (`src/gameplay/king_of_the_hill/hill_geometry.hpp`); random roam needs committed velocity, retarget tick, and stream state.
- **Seating occupancy uses one common radius and `std::hypot`** (`src/simulation/spawn_seating.cpp:15`); every other geometry in the tree writes out `sqrt(dx*dx + dy*dy)` for cross-toolchain agreement.
- **Controllers hold exactly two capabilities** and see only what the snapshot carries (`src/controllers/README.md`); terrain reaches a bot only through the published snapshot value.
- **Welcome carries static session facts** (`lobby_id`, `seat_count_maximum`, `npc_controller_kinds`; `src/protocol/session_welcome.hpp:54-63`), which is the precedent for room data that never changes during a session.
- **Test names** are `unit.<domain>.<test case>`, `fixtures.<test case>`, and `integration.<name>`; `ctest -R` matches test-case text, so every filter below uses a domain prefix rather than a word from a test name.
- **Review artifacts** live at `docs/reviews/<date>-<slug>-review.md`.

## Execution constraints

- **The accepted baseline does not move before Step 16.** Every wall, pair, replay, and deployment fixture and the accepted-baseline oracle pass untouched after every step of Phases A and B. A step in those phases that changes one committed value stops and surfaces. Fixture configurations author an explicit normal-speed ceiling no fixture reaches, so Step 10 changes no committed value either.
- **Both lanes, every C++ step.** Every `verify-focused` line is run on `linux-gcc-debug` and on `linux-clang-asan-ubsan`, one build at a time, and both are written out below. The client runs through `./scripts/verify-web` and the browser gate through `./scripts/verify-browser-e2e`, each inside `./scripts/run-linux-toolchain`.
- **One step per commit**, with the step name as the subject and an execution note in this file for every departure. Stage explicit paths.
- **Promotions are pure moves.** Arithmetic that moves to a canonical module keeps its written operation order byte for byte, and a test proves the old and new answers identical before any reader switches. A behavior change is a separate commit with the test that fails without it.
- **Gate rule.** Step 5 blocks Phase C only. Phase B does not depend on the solver and may proceed while Step 5 awaits the owner. Phase C is provisional: if the gate changes the motion model, amend Steps 16 through 24 and ADR 0008 before executing them; do not guess through them.
- **Wire rule.** Session v3 is one major. The version const moves once, in Step 7, and every later kind, block, member, and command lands under 3.0 in the step whose authoritative behavior it carries, with its schema, generated client types, client validation, examples, and the row in `docs/protocol/v3.md` in that same commit. No vocabulary is reserved ahead of behavior, and no command is advertised in `welcome` before its server handler exists. Development commits are not deployable releases of v3; the release ships server and assets together after Step 24.
- **Kernel seams change only in the steps that name them**: Step 2 (root arithmetic and event order), Step 9 (random streams), Step 10 (the existing closed controller-addressed phase-0 handler and bounded commit-only tuning decisions returned from `step`), Step 13 (component lifetime trait), Step 14 (owner-approved persistent input-generation command/value/registry additions and the explicit StunRequest foundation exception; no new policy socket), Step 15 (NPC profile/declaration values, catalogue admission, and an optional exact-declaration guard on the existing indexed join handler), and Step 16 (solver, response signature, motion disposition, trigger declaration). Step 10 adds no policy socket, batch access on `TickContext`, generic event bus, or external mutable-world access. A feature step that needs a kernel change it does not find stops and amends this file rather than adding it inline.
- **ADR lockstep.** A step that changes a stated contract amends the named ADR section in the same commit with a dated entry. ADR 0008 records the Step 1 decisions before any dependent step runs.
- **Body-bound rule.** A component that exists only while a body exists declares the Step 13 trait and nothing else; no system writes its own clear after Step 13.
- **Timer rule.** Every ability window is the Step 14 tick-window value stored as absolute ticks; no new countdown field.
- **Root rule.** Every event time, contact or trigger, comes from the Step 2 module under the one event-order policy; no second root solver or comparator anywhere.

## Steps

### Phase A: contracts and pure foundations

- [x] **Step 1: Approve gameplay and ownership contracts**
  - Verify: human review
  - Notes: Preserve the confirmed numeric controls and live room-wide tuning. Review the remaining proposals: timed tap shield, normal propulsion cap versus burst/knockback, centre-based falls, initial timings, cooperative authority and phase admission, Apply/Reset interaction, and v3 migration. Mark ADR 0008 accepted only after the owner's decision. Amend contradictory older ADR sections explicitly; preserve historical review/deployment evidence. No source implementation before this checkpoint.
  - Decisions added by the 2026-09-10 design review, each with the recommended answer and the step that depends on it. Record every decision in ADR 0008 as a dated entry; this plan does not approve them.
    - (a) **Contact responses read the committed world**, exactly as predicates already may, and `ContactResponse` gains a per-body motion disposition. Alternative: defense state stored on `PhysicsBody`. Recommended: the first; it keeps gameplay status out of the kernel value and is one signature change. Amends ADR 0004 § "Contact rules". Consumed by Steps 4 and 16.
    - (b) **Terrain travels once in `welcome`**, and the snapshot value carries a shared immutable reference for in-process readers that the frame encoder does not serialize. Alternative: terrain in every frame under a proven frame budget. Recommended: `welcome`; terrain never changes during a session, which is the precedent `lobby_id` and `npc_controller_kinds` already set. Refines ADR 0008 § "Wire ownership and migration". Consumed by Step 7.
    - (c) **One shared `[movement]` section** authors acceleration and normal top speed for every mode, replacing the three per-mode thrust keys. Alternative: per-mode defaults. Recommended: shared; a match-owned value seeded from three authoring homes is the second source of truth ADR 0008 forbids. Refines ADR 0008 § "Normal movement and web tuning". Consumed by Step 10.
    - (d) **The terrain cutover is expand and contract** across Steps 6, 7, and 8, with the old race road fields kept as a derived, equality-tested mirror for exactly one wire step. Alternative: the single atomic checkpoint the ADR's planning review asked for. Recommended: expand and contract; the single checkpoint is one commit across every library and the client, which cannot be reviewed or bisected and defeats Step 24. No state is authored or simulated in both places at any time. Refines ADR 0008 § "Wire ownership and migration". Consumed by Steps 6 through 8.
  - Execution note (2026-09-10): The owner instructed execution of this step with the review's recommended answers, which is the human review this step's verify names. ADR 0008 is marked accepted with a dated § "Design review and acceptance, 2026-09-10 (plan Step 1)" recording the seven accepted first-version proposals and decisions (a) through (d) as recommended; § "Owner decisions" marks each row accepted, and five passages of the ADR carry in-place refinements pointing at that section. Dated pointers were appended to ADR 0003, ADR 0004, ADR 0005, and ADR 0007 naming the sections each later step amends and the step that lands it; those sections are otherwise unchanged and every accepted fixture stands. No source, map, configuration, schema, or protocol file changed. The planning commit `890bb9e` captured this plan and the proposed ADR as they stood; this step's changes are the commit after it.

- [x] **Step 2: Establish the swept-geometry primitive and the event-time order**
  - Verify: `./scripts/verify-focused 'unit.simulation' && ./scripts/verify-focused 'unit.simulation' linux-clang-asan-ubsan`
  - Notes: One pure module in `blob_simulation`, proposed `src/simulation/swept_geometry.{hpp,cpp}`: the first parameter in `[0, 1]` at which a linearly moving point or disc reaches a circle boundary from either side, and at which it reaches a segment-capsule boundary, with the written operation order, no `std::hypot`, and no standard trigonometry. Beside it, proposed `src/simulation/motion_event_order.hpp`: one total order over event times, time then explicit event priority then canonical ids, with one documented root rounding rule and no epsilon comparator; the rule is a written contract, not a helper. No consumer changes. Tests: exact tangency, initial penetration, separating motion, both roots, degenerate segment, a capsule end cap, and comparator transitivity over adversarial near-tied inputs. Step 4 may amend the rounding rule with a written reason; nothing may fork it.
  - Execution note (2026-09-10): The primitive exposes both sorted boundary roots and `first()`, plus the shared linear wall equation and remaining-interval time mapping, so later contacts and triggers need no second root arithmetic. Times retain computed binary64 values with only signed-zero normalization. Review found that ordinary scaled quadratic arithmetic lost a long-sweep/small-circle crossing and misclassified rotated rational tangencies; bounded exact-sign expansions and power-of-two normalization replace that calculation inside the same module. The precision envelope rejects nonzero normalized lengths below `2^-200` and unrepresentable distinct root separation rather than silently converting a crossing to a tangent or miss. Explicit FMA residuals are documented; implicit contraction remains disabled. Further review regressions cover exact capsule sides and endpoint root separation. ADR 0008 records the pure foundation's rounding and priority contract. No live consumer, accepted oracle, fixture, or replay expectation changes.
  - Verification (2026-09-10, **advisory Mac/Docker**, not native evidence): `./scripts/verify-focused 'unit.simulation'` passed **423/423**; `./scripts/verify-focused 'unit.simulation' linux-clang-asan-ubsan` passed **423/423**. The explicit baseline supplements `./scripts/verify-focused 'fixtures'` and `./scripts/verify-focused 'fixtures' linux-clang-asan-ubsan` each passed **42/42**, with no expectation changes. There are 31 new geometry/order cases. The first compile exposed a Catch2 compound-assertion syntax error, corrected before these successful final runs; preliminary runs before the final edge-case regressions are not the recorded completion evidence. The pinned formatter and `git diff --check` pass. Independent read-only review closed the identified numerical findings; the Step 5 architecture/native-performance gate remains pending.

- [x] **Step 3: Author the terrain definition and canonical queries**
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.application|unit.gameplay' && ./scripts/verify-focused 'unit.simulation|unit.application|unit.gameplay' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions`
  - Notes: Proposed `src/simulation/terrain_definition.{hpp,cpp}`: the immutable validated value ADR 0008 § "Terrain" names, owned by `MapDefinition`, with the rectangular envelope, positive ground as the whole rectangle or a bounded set of named polyline corridors, and bounded circular holes; shape and point limits in `simulation_limits.hpp`. Proposed `src/simulation/terrain_queries.{hpp,cpp}`: point support, swept support interval and first exit over Step 2's roots, nearest usable ground (the nearest supported point; radius fitting is the separate disc-clearance query), and disc clearance, with real union and subtraction semantics and the boundary convention (closed road edge, `kPositionTolerance`, exact hole rims supported). Strict map authoring through the existing loader and ~~section-family machinery in `src/application/map_loader.cpp`~~ — the family machinery is private to `ApplicationConfigLoader`, not a shared API; extend the existing strict map parser with the same family convention, never marker metadata. ~~Every existing map under `maps/` gains an explicit solid-ground declaration~~ — that conflicts with the circuit's corridor-only ground. Every existing non-circuit map declares solid ground; `maps/circuit-960x640` declares its road as a named corridor. Migrate the valid e2e/test map declarations too. Startup validation rejects an unsupported spawn marker for the configured radius beside `require_spawn_points_are_seatable`, and a map's static-body centers must be supported as ADR 0008 requires. Move the corridor distance arithmetic of `RaceCourse::distance_to_centreline` into the corridor query verbatim and add a test asserting bit-identical answers between the two over the circuit map's markers and a sampled grid, so Step 6's delegation is a proven pure move; `RaceCourse`, the racer, and the client are otherwise untouched. Conformance fixtures: an overlapping road seam is not a cliff, exact edge, tangent, a narrow hole crossed and re-landed, interval union, disc clearance, and the limits. ~~Fuzz corpus entries for accepted and rejected terrain sections~~ — the existing configuration fuzzer never loads maps; add a bounded production `MapLoader` harness and register it with the fuzz regression runner, then add accepted/rejected terrain sections. No second authoring or publication owner.
  - Execution note (2026-09-10): Promote `ArenaBounds` into its own module without changing its arithmetic or diagnostics, avoiding a terrain/map include cycle. `MapDefinition` owns one `TerrainDefinition`; its existing bounds factory explicitly constructs solid terrain for programmatic callers and accepted replay fixtures, while authored map files require `[terrain]`. One immutable storage allocation owns both the authored fields and the derived exposed-boundary cache. This prevents stale cache state on copying/assignment without a second authoring owner. Shape bounds are 8 corridors, 32 total segments, 40 total points, and 32 holes; cache/temporary arrangement bounds are explicit and fail visibly. These are engineering limits pending Step 4 measurements and Step 5 acceptance, not certified capacity. Native/release performance remains unclaimed.
  - Geometry review note (2026-09-10): Analytic line/arc Boolean-boundary compilation replaces internal union seams with exposed features and preserves supported rims/isolated points. Review fixes preserve exact endpoint provenance, reject conflicting or missing incidence, and use symbolic supported sectors at multi-feature vertices. Translated projections can round into void: bounded directed witness correction retains the selected feature and rechecks canonical support; clearance cannot increase its computed distance. Recovery searches one selected sector ray, not every nearby binary64 point, and fails explicitly on exhaustion. This is not an exact-real interval certificate. Temporary admission uses deterministic retained logical-slot counts, never library-defined vector capacity. Axis, diagonal, near-zero, overlapping-hole, mixed-boundary, and narrow rotated-sector regressions characterize these policies. The previously failing overlap regression passed after the fix; the subsequent complete verification chain below is the completion evidence.
  - Verification (2026-09-10, **advisory Mac/Docker**, not native evidence): `./scripts/verify-focused 'unit.simulation|unit.application|unit.gameplay'` and `./scripts/verify-focused 'unit.simulation|unit.application|unit.gameplay' linux-clang-asan-ubsan` each passed **796/796** (44 new cases). `./scripts/verify-fuzz-regressions` passed all **55 corpus executions across 7 harnesses**: configuration 7, map 10, CLI 3, HTTP 3, protocol command 15, request id 15, scenario 2; this is fixed-corpus replay, not a fresh fuzz campaign. The baseline supplements `./scripts/verify-focused 'fixtures'` and `./scripts/verify-focused 'fixtures' linux-clang-asan-ubsan` each passed **42/42**, with accepted fixtures/oracles/replays untouched. Both pure-move proofs passed, including the circuit marker/grid bit comparison. Earlier attempts found two new off-road parser-test setups and witness-rounding defects; those were corrected, and an interrupted sanitizer attempt is not completion evidence. The final complete serial chain, pinned formatter, `git diff --check`, and focused independent geometry review passed. Step 5 still requires owner review and native performance evidence.

- [x] **Step 4: Prove continuous motion and contact composition**
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.gameplay' && ./scripts/verify-focused 'unit.simulation|unit.gameplay' linux-clang-asan-ubsan && ./scripts/run-benchmarks-linux`
  - Specialist: rigorous-architect
  - Notes: Build/test the canonical pure `continuous_motion` functions, initially without wiring the live kernel, consuming Step 2's roots and event order and Step 3's queries. ~~Reuse existing mass/restitution equations directly~~ — current pair helpers re-run legacy detection using `hypot`. First add contact-taking helpers with verbatim impulse arithmetic, prove old/new bits on both lanes before switching the old wrappers, and retain independent proof goldens. The wrappers preserve their old detectors; live pair rows remain on those wrappers. Promote static reflection with the same proof sequence. Prove swept candidates, wall/body ties, initial overlap, ordinary dual-shield separation, moving perfect-shield defender, mutual perfects, zero-time repeats, actual path records, and bounded-work errors. ~~Prove the guarded pair composition as one pure function with the Step 1(a) signature~~ — the live response signature, Shield/Stun components, and status events do not exist until Steps 14/16/18. Prove one permanent committed-world composition core over explicit frozen `PairGuardFacts`, returning the canonical per-body disposition and typed gameplay consequences; Step 18 projects committed Shield windows into those facts and translates consequences, calling the identical core without a second equation. Prove the pure MotionTrigger seam for support loss over Step 3's queries and for race ordered gates, including finish termination before a later would-be collision; cursors are bounded tick-local values, not arbitrary world mutation. Add deterministic benchmark workloads to `benchmarks/blob_simulation_benchmarks.cpp` for charge speed, dense holes, and dense shield contacts; record baseline costs. No alternate throwaway engine or permanent fast-body path. Write the prototype review at `docs/reviews/2026-09-10-continuous-motion-prototype-review.md` with the proposed event/tolerance order, bounds, and fixture impact.
  - Execution note (2026-09-10): Keep all live kernel/contact/registry seams untouched. One typed, pure continuous-motion driver owns fixed-epoch swept candidates, exact Step 2 event selection, bounded trigger cursors and consequences, and actual anchored paths. Unrelated events must not change future root bits; same-time geometric certificates survive velocity-only responses unless termination removes the body. Closing-only contact callback admission retains the current kernel's velocity predicate; stationary/separating/tangent contacts produce no gameplay consequence in this prototype. Pair repeat suppression records post-response motion revisions. Provisional ceilings and the shield's external-impulse energy behavior are measured/tested hypotheses for Step 5, not accepted native capacity or global energy conservation. Final verification and the required rigorous-architect prototype review are recorded below.
  - Promotion proof (2026-09-10, **advisory Mac/Docker**): Before any legacy reader delegated, `./scripts/verify-focused 'unit.simulation.(contact-taking|contact impulse promotion|general impulse promotion|radius wrappers|static velocity extraction)'` and that identical command with `linux-clang-asan-ubsan` each passed **7/7**. The original radius solvers and static response remained unchanged during these runs; the new helpers retained their written impulse arithmetic, and the tests compared actual old/new results plus an independent frozen reference. An earlier narrower filter selected only two cases and is not the promotion gate evidence. Full post-delegation Step 4 and accepted-fixture verification are recorded below.
  - Numerical amendment (2026-09-10): ~~Infer tangent versus impact solely from the rounded hit normal's relative velocity~~ — independent review produced an exact rotated tangent with a closing rounding residual larger than the velocity tolerance. Extend the same Step 2 circle owner with exact initial membership and supporting-line topology from its existing expansion facts; preserve every root expression and bit. Prove detailed-query/root-wrapper equivalence on both lanes before delegating the old wrapper or admitting the new solver consumer. The solver rejects tangent/stationary geometry before velocity admission and never treats a later exit root as a new entry. This adds metadata, not a second discriminant, time epsilon, or root-rounding policy.
  - Circle promotion proof (2026-09-10, **advisory Mac/Docker**): `./scripts/verify-focused 'unit.simulation.circle topology promotion'` and `./scripts/verify-focused 'unit.simulation.circle topology promotion' linux-clang-asan-ubsan` each passed **4/4** before the original circle wrapper delegated or the solver consumed the detailed result. These cases cover 25 frozen input/root-bit rows, exact topology/initial relation, adjacent tangent/hit/miss inputs, and unchanged rejection diagnostics. The original function was byte-for-byte intact during the proof. The first integration compile found a deleted rvalue accessor call and missing optional aggregate initializers in the new solver; both were corrected before these successful runs.
  - Release-build execution note (2026-09-10): The first canonical benchmark build exposed pinned GCC's optimized `maybe-uninitialized` diagnostic in `GameSimulationSetup::of_mode`'s chain of moved empty optionals after terrain acquired shared storage. Replace that factory's temporary chain with direct construction of the identical map/mode value; preserve its public API, defaults, and declaration ownership. This is a value-construction compatibility fix, not a new kernel seam or gameplay behavior. Do not suppress the warning; verify setup/mode behavior and all accepted fixtures on both lanes.
  - Benchmark-input amendment (2026-09-10): ~~Read the currently deployed configuration as the royale benchmark workload~~ — the required runner now fails `BENCHMARK.DEPLOYMENT_MODE_UNEXPECTED` because commit `a55dca4` changed the deployment to king of the hill. Preserve the prior royale workload in `benchmarks/fixtures/royale-roster.cfg`, sourced exactly from `a9e0104ca25724ac4660a4fc9031620b8a852d40:deploy/ubuntu-pc/blob-royale.cfg`, with provenance. Update the runner, benchmark input/output labels, benchmark README, and ADR 0006's budget interpretation to identify a historical reference rather than the current deployment. Retain the old case identity with explicit historical metadata; do not silently override a mode, drop the case, change live configuration, or alter accepted fixtures. Run the same required benchmark command after correction.
  - Retained-contact amendment/proof (2026-09-10, **advisory Mac/Docker**): ~~A retained geometric certificate needs only a new rounded closing-speed check~~ — a velocity-changing tied response can make that certified pair exactly tangent or receding. Expose initial radial direction from the existing exact dot-product coefficient and exact center coincidence from canonical offsets. Both `./scripts/verify-focused 'unit.simulation.circle topology promotion'` and its `linux-clang-asan-ubsan` counterpart passed **5/5**, including the unchanged 25 root-bit rows and 22 new radial/coincidence rows, before the solver read the additional metadata. Only velocity-revised retained contacts receive the directional veto; their geometry/time cannot be re-gated by rounded membership or future roots. Distinct centers keep a geometric normal; exact coincidence retains its deterministic fallback. At tick end, a full-quantum reference direction classifies current contact only and never authorizes future travel. Final full verification is recorded below.
  - Verification (2026-09-10, **advisory Mac/arm64 hosting Docker Linux/amd64**, not native evidence): The final serial chain ran the exact required `./scripts/verify-focused 'unit.simulation|unit.gameplay'` and `./scripts/verify-focused 'unit.simulation|unit.gameplay' linux-clang-asan-ubsan`, each passing **741/741**, then `./scripts/run-benchmarks-linux`, passing all **8 cases** plus its delivery check after a clean release build. The accepted-baseline supplements `./scripts/verify-focused 'fixtures'` and `./scripts/verify-focused 'fixtures' linux-clang-asan-ubsan` each passed **42/42**. There are 62 new named cases: 7 impulse promotion, 5 circle metadata, 25 motion, 24 guard, and 1 setup equivalence. No accepted fixture/oracle/replay expectation changed. Pinned formatter dry-run, runner shell syntax, and `git diff --check` passed. Earlier compile warnings, an invalid snake-case name in a new hole test, and deliberate old-binary tangent regressions were corrected or retained as red-test evidence, not counted as completion.
  - Prototype gate record (2026-09-10): The required `rigorous-architect` source review and historical-benchmark-input addendum closed with no remaining blocking source finding; the specialist did not run tests or certify native performance. See `docs/reviews/2026-09-10-continuous-motion-prototype-review.md` and the sibling `2026-09-10-continuous-motion-prototype-baseline.json` for the full numerical contract, limits, exact hashes/work counts, raw samples, and platform. **Advisory** median solve times are **26.406750 µs** (charge speed), **577.843438 µs** (32 holes), and **238.037438 µs** (dense shields), from 9 samples of 16 solves per case. Terrain compilation, live intake/status/publication, and worst-case body capacity are not measured; stress speed is not approved charge tuning. The solver remains unwired. Step 5 stays unticked: native evidence, practical ceilings, closing-only/exact-radius/exact-center contact policies, shield external-energy behavior, and explicit numerical-admissibility/failure boundaries require owner review. No Phase B/C work, push, or deployment follows this checkpoint.

- [x] **Step 4a: Prove per-object contact effect admission**
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.gameplay' && ./scripts/verify-focused 'unit.simulation|unit.gameplay' linux-clang-asan-ubsan && ./scripts/run-benchmarks-linux && ./scripts/verify-focused 'fixtures' && ./scripts/verify-focused 'fixtures' linux-clang-asan-ubsan`
  - Specialist: rigorous-architect
  - Notes: The owner rejected a single global contact-effect admission rule. Amend the permanent pure pair path to distinguish a certified geometric touch observation from an optional closing-impact certificate. Objects must be able to select incoming-impact or any-touch effect eligibility, with any-touch including exact grazing and initial stationary contact/overlap; physical impulses still require closing impact. Per-object means an effective value on an individual object, not merely a mode-wide switch or a class/type name. Prototype frozen per-object facts first; document the authoritative authoring/storage projection before Step 16, allowing an archetype default without preventing instance-specific policy. Keep one pair event/composition and the existing root/time owners, anchored epochs, same-time certificates, canonical ordering, filters, and termination rules. Do not retrofit moving-body pair effects through unary MotionTrigger callbacks or create another contact engine. Center-based cliff/darkness effects use the existing terrain support-loss query, not the player's outer rim or a second cliff-collider representation. The center criterion is confirmed; exact supported-rim equality remains the current Step 3 convention unless explicitly amended. Generalizing center-entry to body-shaped hazards, independently filtered sensor contacts, and once-per-encounter delivery is not silently included: name those choices if implementation needs them.
  - Proposed repeat contract for review: consume even a no-motion touch observation against both post-response motion revisions; its own response and time advancement cannot immediately emit it again. External trajectory changes may re-enable the pair, with the existing event/effect budgets bounding work. This is revision-based delivery, not a promised once-per-separation/re-entry encounter API. A lethal effect terminates its recipient immediately. Exact graze eligibility must not manufacture a perfect parry without incoming opponent motion. Tests cover mixed per-object policies and swapped IDs, tangent/stationary effects without impulses, initial overlap, consumed no-op observations, externally re-enabled pairs, support/contact/finish ties, and no later effects after death. Preserve the completed Step 4 review/baseline as historical evidence and record a supplemental review and newly labelled advisory measurements. No live kernel, registry, authoring, wire, or accepted-fixture change in this supplemental proof.

  - Execution/review (2026-09-10): One source-oriented `PairContactObservation` carries certified touch geometry, optional physical impact, and each object's effect eligibility. The final frozen policy span defaults omitted instances to closing impact and rejects oversized, duplicate, unknown-entity, or invalid-policy input. Existing post-response revision consumption now covers no-op touches; exact tangent/stationary certificates cannot acquire an impulse from rounded normals. A source-review finding restored early rejection before normal construction for default ineligible tiny geometry, with a regression. The required `rigorous-architect` review closed without a remaining source blocker at `docs/reviews/2026-09-10-per-object-contact-prototype-review.md`; it also specifies the future Step 16 sparse body-bound nondefault policy component and explicit instance-over-archetype authoring. No live kernel, registry, wire, authoring, accepted fixture, clock, root arithmetic, or physical equation changed.
  - Verification (2026-09-10, **advisory Mac/arm64 hosting Docker Linux/amd64**, not native evidence): The exact serial Step 4a chain above passed **761/761** simulation/gameplay tests on each lane, all **8 benchmark cases** plus delivery, and **42/42** accepted fixtures on each lane. Twenty new named cases cover policy validation, mixed source policies/swapped identities, grazing/stationary/separating contact, retained and terminal certificates, no-op/self-response consumption, external re-enablement/budgets, and termination/ties. All three unchanged default-policy benchmark workloads retained identical complete correctness records, hashes, and deterministic work counts. The newly labelled sibling `2026-09-10-per-object-contact-prototype-baseline.json` preserves raw samples and comparison results; medians were **34.124438 µs**, **702.192563 µs**, and **280.408563 µs** for charge, 32 holes, and dense shields respectively. These are advisory observations, not certified regression budgets, supported capacity, or approved shield tuning. The historical Step 4 artifacts remain unchanged. Formatter and `git diff --check` passed. Step 5 is not accepted by these execution results.

- [x] **Step 5: Accept the physics prototype boundary**
  - Human verification completed (2026-09-11): The owner answered "yes" to "Do you approve Step 5 so I can begin Phase C?" after the Step 15 handoff linked the revised prototype review and advisory benchmark baseline and identified the remaining numerical, repeat-contact, and shield-energy limitations. This explicitly accepts the reviewed Step 4/4a design with the verified Step 7a repair disposition and releases Phase C without changing the motion model. Bounded binary64/whole-sweep admission, initial-overlap/revision-based repeat semantics, limited recovery search, and fail-visible precision/work exhaustion remain explicit. Native capacity/performance selection stays deferred, and Step 24 still requires native evidence. No live kernel adoption, shield/charge implementation, push, or deployment is implied by recording the gate.
  - Verify: human review
  - Historical numerical limitation (2026-09-10, Step 7; disposition corrected 2026-09-11): Include `docs/reviews/2026-09-10-terrain-clipped-corner-admissibility-review.md` in this review. ~~The original width-10/radius-15 clipped-corner input trips the unchanged missing-incidence precision guard; legitimate representability rejection versus lost incidence is not diagnosed.~~ Step 7a traced and repaired lost construction identity; the unchanged original input and probes passed both advisory lanes. The calibrated publication fixture remains historical evidence, not the repair. This closes that diagnosed defect, not bounded representability limitations or the separate human approval gate.
  - Notes: ~~Require native performance evidence and final supported solver ceilings at this gate~~ — the owner deferred capacity/performance work during the Step 5 clarification and expects materially fewer than 256 physical bodies. Review Step 4 plus the new Step 4a behavioral proof and explicit numerical limitations before approving live adoption. Existing fail-visible precision/work bounds remain; the mouse-input decision is not acceptance of arbitrary numeric inputs. Native evidence and a practical operating envelope remain future work, and native evidence is still required for performance/release claims at Step 24. Geometry simplification or changes to clock rate/resolution may be considered later, but none is authorized or applied here; changes to the 400 Hz clock or deterministic representation would require their own ADR/fixture migration, never an adaptive silent fallback. The owner subsequently confirmed ordinary protection with reduced knockback after the perfect opening (Step 18); that gameplay choice does not accept this physics gate. This step still requires an explicit human decision after the revised proof; these clarifications do not tick it. Phase B may proceed while it is pending.

### Phase B: gate-independent work

- [x] **Step 6: Bind race to the named corridor behind a derived mirror**
  - Verify: `./scripts/verify-focused 'unit.gameplay|unit.application|fixtures' && ./scripts/verify-focused 'unit.gameplay|unit.application|fixtures' linux-clang-asan-ubsan`
  - Notes: `[race]` names its corridor (proposed key `road`), `track_half_width` retires from `[race]` because the corridor authors it, and `RaceCourse::create` binds to the map's named corridor, delegating distance and half-width to the Step 3 query with the pure-move test already in place. The `track` marker kind retires from `maps/circuit-960x640/markers.csv` and from the race test maps; checkpoint-inside-corridor and spawn-inside-corridor validation use the canonical query. `TrackBoundsSystem` and `CheckpointProgressSystem` keep their endpoint sampling until Step 17; they read the corridor through `RaceCourse`. `CoursePublisherSystem` keeps stamping `track` and `track_half_width` into `RaceModeState`, now derived from the corridor, and a test asserts per-tick equality between the mirror and the terrain so the two cannot drift. Racer bot, wire, and client unchanged. Update every configuration that authors `[race] track_half_width`: `config/blob-royale.cfg`, the e2e fixture configs, the race replay `match.ini` files, `tests/integration/server_process_fixture.cpp`, and the application test fixture. Race replay fixtures keep their expectations; the geometry is identical.

  - Authoring amendment before execution (2026-09-10): ~~Migrate only the listed race width keys and race replay marker rows~~ — the strict loader also consumes deployment, application fuzz, and historical benchmark configurations, while the replay harness constructs solid terrain through a private map reader. Migrate every replay's map authoring to a self-contained canonical `MapLoader` directory beneath that replay, preserving the old map-name leaf and replay directory identity. Replace inline `[map]` data with one named `[match] map` reference beneath the fixed replay-local `maps/` directory; reject non-leaf references. Remove the fixture-specific marker reader. Race corridors receive the unchanged ordered track coordinates and width; remaining markers use the canonical four-column schema, and non-race maps declare solid terrain. Preserve commands, seeds, timing, entity allocation, published race mirrors, and accepted expectations. Required display metadata and race terrain are intentionally new map-authoring values, not a claim of whole-`MapDefinition` equality; existing committed snapshots do not carry them. Update fixture-format/error documentation. No production parser API is added.
  - Migration safeguards (2026-09-10): `RaceCourse` retains owning immutable terrain plus the corridor identity, never a borrowed pointer into a temporary map. Named-road validation and missing-road errors replace retired marker-count ownership; the terrain factory owns segment validity/limits, and checkpoint radius versus road width is checked after binding. Preserve the old exact distance/width admission convention, not full Boolean support or a new tolerance. Keep an independent frozen legacy distance reference in the promotion proof after delegation. The benchmark fixture becomes an explicitly documented schema-only derivation of its historical source when `track_half_width` becomes `road`; all measured royale tuning stays unchanged and historical baseline/review artifacts remain intact. Include deployment and complete fuzz input consumers in the strict-key migration. Native capacity remains deferred.
  - Initial verification correction (2026-09-10): The first GCC lane compiled, then caught `APPLICATION.MAP.HEADER_INVALID` in replay fixtures: unlike the removed fixture parser, the canonical map CSV grammar requires the exact header on physical line one and permits no comment rows. Move the preserved marker-layout comments into the corresponding `map.cfg` comments, leaving canonical CSV data unchanged. Do not widen production parsing or alter replay outcomes to accommodate the old fixture grammar. Rerun the full required verification after this data-format correction; the failed run is not completion evidence.
  - Execution/review (2026-09-10): Race configuration owns the required road identity; the course retains immutable terrain plus a stable corridor index and derives width/points/distance from it. Six configuration tests, four course tests, one committed-tick mirror test, and two replay-map tests were added. The promotion proof retains the independent `ba75500` arithmetic. All 16 replay bundles passed literal before/after audits of names, bounds, match values, marker coordinates/order, race widths/points, empty static content, and preserved comments; commands remain byte-identical. The shipped map's marker-count assertion deliberately changes from 10 to 7 when its three retired track rows disappear; existing geometry, body, spawn, committed motion, and lifecycle assertions remain. Independent source review closed, followed by a focused audit of the CSV-comment correction. No live kernel, endpoint chronology, wire, controller algorithm, or physics equation changed.
  - Verification (2026-09-10, **advisory Mac/arm64 hosting Docker Linux/amd64**, not native evidence): After the data correction, `./scripts/verify-focused 'unit.gameplay|unit.application|fixtures'` and the identical command with `linux-clang-asan-ubsan` each passed **425/425** (**381 unit + 44 fixture cases**). The first run's 19 header failures are retained as correction history, not accepted outcomes. Supplemental `./scripts/verify-fuzz-regressions` passed **55 fixed-corpus executions across 7 harnesses**; this was corpus replay, not exploratory fuzzing. Supplemental `./scripts/run-linux-toolchain -- out/build/linux-gcc-debug/benchmarks/blob_simulation_benchmarks --royale-reference-config benchmarks/fixtures/royale-roster.cfg --maps-directory maps` passed all **8 cases** plus delivery, with complete correctness records identical to Step 4a and identical deterministic work for the three prototypes. This diagnostic Debug run is not a replacement release/native timing baseline; historical measurements remain unchanged. Pinned formatter dry-run and `git diff --check` passed. Step 7/8 preflight also identified and documented their route-envelope, truthful fixture terrain, point-projection proof, and selected-road-identity requirements before implementation.

- [x] **Step 7: Publish terrain on session v3**
  - Verify: `./scripts/verify-focused 'unit.protocol|unit.server|unit.runtime|unit.controllers|unit.simulation|integration' && ./scripts/verify-focused 'unit.protocol|unit.server|unit.runtime|unit.controllers|unit.simulation|integration' linux-clang-asan-ubsan && ./scripts/run-linux-toolchain -- ./scripts/verify-web && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e`
  - Route-boundary amendment before execution (2026-09-10): ~~Rename only the room-session URL and reuse the existing upgrade-required error~~ — discovery and the old room-one alias are also active v2 routes, while `PROTOCOL.UPGRADE_REQUIRED` means a missing WebSocket attempt, not an obsolete application major. Move discovery to `/api/v3/lobbies` and sessions to canonical `/api/v3/lobbies/<lobby_id>/session`; migrate room-one callers instead of adding another `/api/v3/session` alias. Recognize `/api/v2/session`, `/api/v2/lobbies`, and grammar-valid old room-session paths as retired before room lookup, method/handshake handling, upgrade-token consumption, or WebSocket/controller allocation, but after the existing global HTTP/identity/Host/supplied-Origin checks. Return a bounded v3-envelope `426 PROTOCOL.SESSION_VERSION_UPGRADE_REQUIRED`, non-retryable, with fixed `required_protocol_version: "3.0"` detail and guidance naming the canonical route/subprotocol. Document the explicit retirement-envelope exception; do not retain a second general v2 encoder or extend v1's closed `HttpError` registry. Preserve ordinary missing-WebSocket and invalid-subprotocol errors on active routes and reject malformed legacy aliases normally. Tests pin unchanged leases, controller IDs, directory/session counts, and global security-error precedence.
  - Error-envelope clarification from pre-implementation security review (2026-09-10): The single current v3 session envelope also covers generic/global errors for parsed targets beginning `/api/v2/` or `/api/v3/`, including Host/Origin/framing/rate/identity failures and malformed old aliases. Only grammar-valid retired routes receive the new retirement code; an early security error keeps its established code/precedence. This intentionally supersedes historical v2 target-prefix envelope selection without retaining a general v2 encoder. Immutable v1 routes and their closed error registry remain unchanged; transport failures without a parsed target retain the existing generic fallback. New upgrade guidance/details are fixed values and do not echo an untrusted raw target. Document and test this broader envelope migration explicitly.
  - Ownership/publication clarification (2026-09-10): Allocate shared immutable map ownership before entering any existing `noexcept` constructor; snapshots retain an aliasing handle to that map's actual terrain member, survive simulation destruction, and compare authored terrain values rather than pointer identity. `Observation` exposes that same retained terrain. Welcome requires explicit terrain, with no fallback rectangle at publication; frames omit it. Make the racer's isolated observation fixtures truthful during this step: their old straight/bent road literals and width **80** become actual fixture maps passed through `engine_defaults().with_map(...)`, without mode systems or an extra tick. The unrelated gameplay race-test map has width 70 and is not a replacement. Keep controller algorithms and road mirrors until Step 8.
  - Wire/rendering clarification (2026-09-10): Welcome terrain carries required `bounds` (`width_world_units`, `height_world_units`), `ground`, ordered `corridors` (`name`, `half_width`, `{x,y}` points), and ordered `holes` (`name`, `{x,y}` center, `radius`). Enforce the existing shape/aggregate-point/segment/name/containment/nonzero-segment rules in client validation and exact agreement with v1 configuration bounds, without serializing compiled arrangements or reserving future fields. Preserve historical v2 schema/example validation deliberately when labelling them historical; active generation remains v3-only. Shared validated geometry-golden data covers solid/corridor unions and overlapping holes. Rendering must subtract the union of holes, not XOR overlapping holes or refill a hole while painting a later corridor; reuse the one camera projection. Cliff rims/combat visuals remain Step 20.
  - Notes: Route `GET /api/v3/lobbies/<lobby_id>/session`, the v3 subprotocol, `protocol_version` 3.0, generated schemas under `docs/protocol/schema/v3/` with one active encoder/decoder in `src/protocol/`, and `docs/protocol/v3.md` opened with its versioning table. v2 session requests fail with an explicit upgrade-required response; v2 docs and schemas are labelled historical; v1 public configuration stays immutable. Under Step 1(b), `SessionWelcome` carries the complete terrain under explicit shape/point limits, `GameSimulation` holds the map through shared immutable ownership so `WorldSnapshot::from_world` hands the snapshot the same terrain object, `WorldSnapshot` and `Observation` expose it, and the frame encoder does not serialize it. Client: v3 session validation and generated types, a terrain renderer as the bottom world layer under `frontend-react/src/features/simulation/rendering/` through the existing projection, and the race renderer reduced to gates and objectives in this same step so no road is drawn twice. The race mode-state block still carries the mirror fields on the wire for this one step and says so in `v3.md`. Golden geometry fixtures are shared as validated data, never as code, between C++ and the client. Every existing browser scenario is migrated to v3 and stays green with no retry or skip. If the owner chooses per-frame terrain instead, this step carries the maximum-frame proof against the existing 2 MiB limit.

  - Execution in progress (2026-09-10): Shared map/snapshot/observation retention and truthful width-80 racer fixtures are implemented; a bounded independent source review found no ownership blocker. Active session implementation, generated types, and browser readers move to v3; v2 schemas/examples remain historical and validated. Shared JSON geometry cases feed canonical C++ factory/support checks and browser validation/render traces. Route tests cover retirement grammar, global error precedence, resource counters, and bounded real HTTP pipelines through the existing loopback client. Production v1 behavior and accepted motion expectations are unchanged. This is not completion evidence; full checks remain pending.
  - Review corrections before completion (2026-09-10): Correct stale inherited documentation that contradicted both-prefix v3 error selection. Add the fixed new-retirement-row `Upgrade: websocket` and `Connection: Upgrade` option required by RFC 9110, preserving persistence/close behavior and leaving existing v1 handling unchanged. Extend the existing C++ frame oracle because its welcome branch otherwise ignored terrain semantics; configuration-bounds agreement remains explicitly external to that oracle. Browser terrain admission reuses the existing negative-zero policy. These are fixes within the Step 7 contract, not a second geometry implementation or a Phase C adoption.
  - Initial web corrections (2026-09-10): The first full web run stopped at strict AJV compilation because new conditional schema branches omitted explicit object/array types; correct the schema declarations and regenerate, without weakening strict validation. The next run passed all 71 schemas/19 examples (including historical v2), then caught a new golden test fixture that replaced the full v1 world with terrain bounds and omitted player radius; retain the existing radius while overriding bounds. These failed runs are correction history, not acceptance evidence. The full web gate is rerunning; C++ verification waits for the conformance-oracle source freeze.

  - Source-review closure (2026-09-10): The independent security review closed both new findings after rereading fixed retirement headers, the bounded terrain oracle, negative-zero lexical handling, and mutation tests. Root separately reviewed the oracle, HTTP pipeline extension, and client boundary/rendering path. Eight terrain-conformance cases supplement the two encoder/error cases, seven route cases, six snapshot/observation/fixture cases, and one shared geometry-fixture case. No review substitutes for the remaining execution gates.
  - Web verification (2026-09-10, advisory Mac/Docker): The full `./scripts/run-linux-toolchain -- ./scripts/verify-web` passed **410/410** tests with zero skips/todos, strict typecheck, lint, formatting, generated-type drift checks, **71 schemas and 19 examples** across v1/v2/v3, and the production Vite build. A diagnostic verbose test run identified the prior gate's sole failure: an accidental migration edit changed the existing 1,200-tick elapsed-exposure expectation from 3.0 s to 2.5 s. Restore the exact original expectation; production elapsed-time arithmetic never changed. The diagnostic run was **409 passed / 1 failed**, not acceptance evidence; the recorded full success follows the fix. Web ran while the C++-only oracle was finalized, without concurrent builds. Both C++ lanes, accepted-fixture supplements, and the real-browser gate remain pending.

  - C++ verification (2026-09-10, advisory Mac/Docker): The exact GCC and Clang ASan/UBSan Step 7 filters each passed **899/899** with zero failures. The first supplemental fixture run passed **45/46**, rejecting the new sixth shared terrain golden during canonical construction. No accepted fixture failed. Following the existing exact-effective-radius test convention, the new fixture changes authored width/radius to `9.999999999`/`15.000000001`, preserving topology/probes but not claiming identical authored geometry. Both subsequent `./scripts/verify-focused 'fixtures'` lanes passed **46/46** (**45 fixture cases plus one observation test matched by its name**). An independent review accepted this publication-test calibration only with the original exact input/failure preserved as unresolved in `docs/reviews/2026-09-10-terrain-clipped-corner-admissibility-review.md` and carried into Step 5. No geometry source, precision guard, or accepted baseline expectation changed. Full web is rerunning against the final shared data, followed by the real-browser gate; Step 7 remains unticked.

  - Final verification (2026-09-10, **advisory Mac/arm64 hosting Docker Linux/amd64**, not native certification): The exact required C++ filter `unit.protocol|unit.server|unit.runtime|unit.controllers|unit.simulation|integration` passed **899/899 on GCC** and **899/899 on Clang ASan/UBSan**. Both final accepted-fixture supplements passed **46/46** as detailed above. The full `./scripts/run-linux-toolchain -- ./scripts/verify-web` rerun against the calibrated shared data passed **410/410**, zero skips/todos, strict typecheck/lint/format/generation drift, **71 schemas/19 examples**, and the production build. `./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e` passed **8/8 Chromium scenarios**, **zero retries/skips**. The supplemental GCC `[terrain][budget] --success` check passed **160 assertions/1 case** and measured **10,332 bytes** for the complete maximum-cardinality welcome, including all 8 corridors/40 points/32 holes and 64 NPC names; this is not a mathematical maximum over all numeric spellings. Exact-byte and one-byte-short bounds passed. Pinned formatter dry-run over all 74 changed/new C++ paths and `git diff --check` passed. No push/deployment, native performance claim, production geometry change, or Step 5 approval. The unresolved original clipped-corner input remains recorded, not silently fixed by fixture calibration.

- [x] **Step 7a: Preserve terrain boundary construction provenance**
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.application|unit.gameplay|unit.controllers|fixtures' && ./scripts/verify-focused 'unit.simulation|unit.application|unit.gameplay|unit.controllers|fixtures' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions && git diff --check`
  - Specialist: rigorous-architect
  - Notes: The owner approved the researched provenance-preserving repair strategy. Capture the exact failing vertex, incident feature identities, authored directions, and reconstructed rays before changing the canonical compiler. Preserve known side/cap joins and classify local boundaries from their construction relationships; retain distinct unrelated tangent branches and lower-dimensional supported rims/points. Add certified calculations only where the diagnosis requires them, using the existing canonical arithmetic owners rather than a second root solver. Do not epsilon-merge, snap coordinates, calibrate the failing fixtures, weaken rejection guards, or change the clock/accepted motion baseline. Keep recovery's bounded representable-witness contract explicit. Add unchanged sloped/fractional construction regressions and diagnose the separately preserved original clipped-corner input; do not infer that its cause is identical. Record source review and exact red/green evidence in the blocker review, and amend ADR 0008's terrain-foundation contract for any refined provenance rule. This is a separate geometry repair, not the Step 8 pure promotion; do not switch Step 8 names/readers until its own full pre-delegation gate passes on both lanes. Preserve all uncommitted Step 8 work and stage only verified prerequisite changes.
  - Execution started (2026-09-10): Read the handoff and both preserved admission reports. Production repair and independent degeneracy/regression review are assigned separately; root owns serial toolchain execution. No repair or verification success is claimed yet. Step 5 remains a human gate and Phase C remains blocked.
  - Execution/review (2026-09-11): Captured both original failures before repair. Sloped/fractional joins lost authored tangent directions; the clipped envelope independently reconstructed a known endpoint two binary64 steps away, then line/circle recomputation duplicated it at one line parameter. Retain authored side normals and arc construction directions, classify sectors before materializing the selected supported witness, and preserve endpoint/diameter identities only under literal axis certificates that also agree with retained authored orientation. General root arithmetic and all ambiguity/precision/work guards remain; no arbitrary cut/ray merging. Four new cases and three strengthened controls cover original inputs, support/recovery/clearance, unrelated crossing, different-curvature cusp, singleton touch, rim-only arcs, and isolated corners. Independent rigorous-architect review closed with no blocking source finding in `docs/reviews/2026-09-10-terrain-provenance-repair-review.md`. Both original admission reports now retain red/partial/green evidence. No accepted fixture, replay value, solver seam, or clock changed.
  - Verification (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**, not native certification): The exact required filter `unit.simulation|unit.application|unit.gameplay|unit.controllers|fixtures` passed **1,031/1,031 on GCC** and **1,031/1,031 on Clang ASan/UBSan**. The fixed fuzz corpus passed **55 executions across 7 harnesses**, not a new fuzz campaign. Pinned formatter dry-run and `git diff --check` passed. Verification ran in the preserved worktree including unswitched Step 8 extraction/proof candidates; the separate geometry commit excludes those candidates and all Step 8 wire/reader changes. Earlier diagnostic runs were **0/3**, **4/5**, clipped-only **0/1**, then focused **5/5**; only this final complete chain accepts Step 7a. Logs remain under `/tmp/blob-royale-terrain-provenance.veIQIP`. Step 8 still needs its own exact pre-delegation commands; Step 5 remains unapproved and no native/performance/release claim is made.

- [x] **Step 8: Retire the mirrored road fields**
  - Completion (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): The exact final C++ filter passed **1,155/1,155 on GCC** and **1,155/1,155 on Clang ASan/UBSan**. The complete web gate passed installation, formatting, generated-protocol drift, schema examples, strict type checking, lint, **422/422 tests with zero failed/skipped/todo**, and production build. `git diff --check` passed. Final logs are `step8-final-gcc.log`, `step8-final-clang-after-relink.log`, and `step8-final-web.log` in `/tmp/blob-royale-terrain-provenance.veIQIP`; the first Clang discovery failure and exact-byte relink evidence remain separate below. No failed acceptance case was skipped or retried. Required selected-road identity replaces both geometry mirrors across state/wire/client; initialization retains recorder-before-publisher and tick-zero `NoModeState`, including an initially Running first-tick finisher. Independent frozen name/projection references and all 19 original racer scenarios remain after the pre-proved delegation. Direct boundary fixtures now supply actual matching terrain; the synthetic race-only dimension ceiling deliberately changes `1e12` to canonical terrain width `1e9`, without narrowing unrelated physical/hill bounds. Replay expectations now read geometry from its canonical owner with identical values; authored maps/configuration/replay outcomes and the accepted motion baseline are unchanged. ADRs 0004/0007/0008, domain docs, schema/generated assets/examples, and the v3 development row move together. Step 5 and native/performance/release certification remain separate, unapproved gates.
  - Cutover verification in progress (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): All three implementation slices are source-frozen. Required road identity replaces both mirrors; the racer and distance reader share the pre-proved projection; seat/contact names delegate to their pre-proved storage policies. Independent frontend boundary review found one new reconnect fixture that allowed only lobby 1 while opening lobby 2; its explicit allowed-room list was corrected before verification. Independent name-contract review found no defect. Generation and pinned formatting passed. The final GCC filter passed **1,155/1,155**. Clang built all targets but test discovery ran **zero tests** because the controller test executable had mode `0644` instead of `0755`. Preserving and relinking only that artifact produced the same SHA-256 (`0ba36e172fb2f2551220938f280eee9b2c2389c23ab10a863125e1bbd445c423`) with executable mode; no source, test expectation, or sanitizer setting changed. The exact Clang gate and subsequent web gate are now running; neither is claimed passed. Original failure, mode diagnostic, preserved binary, relink, and subsequent run logs remain under `/tmp/blob-royale-terrain-provenance.veIQIP`.
  - Prerequisite release (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): Step 7a is committed separately as `96a3fc0`. The exact pre-delegation commands below now each pass **605/605**, on GCC and Clang ASan/UBSan, while both old projection readers and both old bounded-name classes are still unchanged. Logs: `/tmp/blob-royale-terrain-provenance.veIQIP/step8-preproof-gcc.log` and `step8-preproof-clang.log`. The original racer sloped/fractional cases pass without calibration; independent frozen references remain. This releases the Step 8 name/projection readers and required-road/mirror-removal implementation, not its checkbox or the Step 5 gate.
  - Pre-delegation verify: `./scripts/verify-focused 'unit.simulation|unit.controllers' && ./scripts/verify-focused 'unit.simulation|unit.controllers' linux-clang-asan-ubsan` before switching the old bounded-name types or either projection reader. These commands prove the prerequisite extractions while the old readers still exist; they do not complete this step.
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.controllers|unit.protocol|unit.application|fixtures' && ./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.controllers|unit.protocol|unit.application|fixtures' linux-clang-asan-ubsan && ./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Missing projection proof amendment before execution (2026-09-10): ~~Treat Step 3's distance proof as proof of the racer's nearest target~~ — the canonical query returns only distance, while the racer privately computes both a projected point and distance with first-authored-segment tie retention. Promote one point-plus-distance query under `terrain_queries` with the identical written projection/clamp/sqrt order and strict `<` update. Before either the old distance reader or racer delegates, prove x/y/distance bits and actual old-racer command bits on both lanes over straight/bent/sloped/fractional/clamped/exact-tie/adjacent-tie cases, retaining an independent frozen reference/goldens. `nearest_supported_point` is not a substitute: it has different Boolean-support and boundary tie semantics. Then delegate and retain all existing heading/caution expectations.
  - Projection-domain clarification (2026-09-10): A standalone validated corridor can have signed coordinates even though map terrain requires closed-envelope containment. Its current distance query is `noexcept` and never constructs a projected `Vector2`; extreme signed endpoint arithmetic can produce a raw projected coordinate beyond that value's bound. Keep one private raw projection core, with the existing distance wrapper reading only its distance and the new typed point/distance wrapper explicitly allowed to validate the point. Do not route the old distance API through new point validation, silently narrow its domain, or change clamp/arithmetic order. Prove the extreme signed case alongside valid-map racer command cases before delegation.
  - Missing binding identity amendment before execution (2026-09-10): Observation terrain can contain multiple arbitrarily named corridors; the current race state publishes no selected name. Add `RaceModeState::road` as the validated bounded name of the bound corridor while removing the two geometry mirrors, with the complete Step 8 v3 schema/encoder/generated types/client validation/selectors/examples row. `RaceCourse::road_name()` views the existing bound corridor name, not another stored geometry copy; `CoursePublisherSystem` retains gates/clocks and stamps this identity. The racer resolves exactly that name and fails visibly on a missing binding, never selecting the first corridor, assuming `road`, or inferring it from checkpoints. Test alternate names and reordered corridor declarations. Identity is not a retained geometry mirror.
  - Construction/ownership clarification before execution (2026-09-10): Do not add generic simulation create/commit mode-state validation. Require a non-default-constructible road-name value in the race arm, initialized through the existing race-owned helper from the bound course. Both publisher and standings recorder already own that course. Preserve the recorder-before-publisher order, including an initially Running first-tick finisher; production tick-zero snapshots remain `NoModeState`. Direct controller/protocol fixtures supply explicit identities and actual matching terrain. Validate exact binding at race initialization and existing controller/encoder/client boundaries; typing proves grammar, not membership in an arbitrary terrain. The browser retains welcome terrain for subsequent snapshot binding validation. No new tick, hardcoded name, first-corridor fallback, or kernel socket.
  - Bounded-name prerequisite before execution (2026-09-10): The third bounded name reaches `SeatKindName`'s documented consolidation point. Extract one policy-specialized bounded-name implementation, preserving distinct type policies, existing seat/contact validation and diagnostics verbatim, empty-default sentinels, owned fixed storage, zero tails, copy/move/equality, deleted temporary views, and nothrow moves. Road policy disallows default construction. Prove the existing contract against frozen reference behavior before aliasing the old public names to the shared implementation; cover exact errors, capacities, embedded NUL/non-ASCII, lifetime, assignment, and compile-time invariants. This is source-API compatibility, not an ABI/type-identity promise; no dependent nominal/layout uses were found in preflight. Do not duplicate a third array/accessor implementation or reuse seat-domain diagnostics for roads. Include simulation tests in final verification because the prerequisite lives there.
  - Notes: Remove `track` and `track_half_width` from `RaceModeState` (`src/simulation/mode_states/race_mode_state.hpp`), its v3 schema and examples, `CoursePublisherSystem`, and the Step 6 mirror test. The racer controller reads the corridor through `Observation`'s terrain and the canonical query, and `nearest_course_target` is deleted (`src/controllers/racer_controller.cpp:24`). `RaceCourse` keeps only its checkpoints, gate radius, and corridor binding. Client selectors and generated types regenerate; `checkpoint_radius`, checkpoints, and standings stay in the race block. Behavior identical: race replay fixtures and racer tests keep their expectations. After this step no geometry exists in two places.
  - Execution started (2026-09-10): Build the bounded-name and projection candidates with independent proof fixtures first, leaving existing C++ readers/types unchanged until both pre-delegation lanes pass. Wire/client work proceeds in parallel within this same uncommitted step; it must freeze for root-owned serial verification. The name and projection extractions do not imply Step 8 completion or alter any physics seam. Correct the stale inherited v3 documentation statement that the already-shipped client uses only whole-map fit; the actual manual/follow camera remains unchanged.
  - **Verification blocker (2026-09-10, advisory Mac/Docker):** The exact pre-delegation GCC filter passed **599/601**; Clang did not run. Six bounded-name proof cases and the remaining successful projection/controller cases are source-reviewed, but the two-lane prerequisite is not complete. Correct the new signed-extreme test's mistaken `1e9` point bound to the actual physical bound `1e12` with expected ULP `0x1p-13`, without changing production limits. The two-case focused diagnostic then passed that case and failed the actual-racer proof: named `sloped`, `fractional`, and `fractional_gate` sections throw during terrain construction, before command comparison (**1,120 assertions passed; three construction exceptions**). Preserve exact inputs and the tangent-provenance hypothesis in `docs/reviews/2026-09-10-racer-projection-admission-blocker-review.md`. No width/coordinate calibration, precision-guard weakening, or canonical compiler fix is authorized inside this step. Pause for direction on a dedicated geometry prerequisite. Old C++ names/readers, accepted motion values, and the Step 8 checkbox remain unchanged. Frontend/schema and target docs are incomplete uncommitted work, not a functioning cutover; generated v1 outputs remain unchanged. The frontend's synthetic mirror-only race ceiling specimen deliberately changes from `1e12` to the canonical terrain-width ceiling `1e9`; unrelated physical/hill bounds stay unchanged. No final Step 8 web/fixture/native evidence is claimed.

- [x] **Step 9: Add named deterministic random streams**
  - Completion (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): The exact final filter `unit.simulation|unit.gameplay|unit.protocol|fixtures` passed **974/974 on GCC** and **974/974 on Clang ASan/UBSan**, with zero failures. The complete web gate passed installation, formatting, generated-protocol drift, schema examples, strict type checking, lint, **438/438 tests with zero failed/skipped/todo**, and production build. Pinned C++ formatter dry-run and `git diff --check` passed. Logs are `final-gcc.log`, `final-clang.log`, `final-web.log`, `final-generation-format.log`, and `final-format-check.log` under `/tmp/blob-royale-random-streams.60XDcn`. Both final C++ lanes retain the original 803/803 pre-delegation proofs, plus real failed-tick full-bit continuation, snapshot ownership/equality, actual hazard independence under extra hill draws, and exact server count-encoding boundaries. All accepted motion/replay fixtures, bot seeds, and v1 assets remain unchanged. Nineteen new C++ cases and sixteen web cases cover the Step 9 contract; no failed acceptance case was skipped or retried. ADR 0004, domain docs, v3 schema/examples/generated files, encoder, client validation, and development row move together. Independent review and the inherited browser numeric-decoding limitation are recorded in `docs/reviews/2026-09-11-random-streams-cutover-review.md`; parsed-number validation is not a claim of lossless lexical decimal parsing. Step 5, native performance, and release certification remain separate unapproved gates.
  - Cutover review/verification in progress (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): Implementation is source-frozen; independent cross-reviews closed stream ownership, actual hazard preservation, full-bit failed-tick continuation, and server encoding. The exact final GCC filter passed **974/974**; Clang ASan/UBSan and the complete web gate remain pending. Root corrected only documentation/comments after the first build began; no behavior or test expectation changed. The inherited browser `JSON.parse` boundary can round malformed decimal tokens to safe integer values before schema validation. Record this limitation in `docs/reviews/2026-09-11-random-streams-cutover-review.md`, without claiming lexical arbitrary-precision validation or inserting a general parser rewrite into Step 9. The production writer emits exact unsigned counts and rejects unsafe uint64 values. Final protocol review must resolve or explicitly characterize this inherited decoding boundary before a release-conformance claim.
  - Prerequisite release (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): Both exact pre-delegation filters passed **803/803**, GCC and Clang ASan/UBSan, with old `next_bits`, world/snapshot APIs, and the production hazard reader still unchanged. Nine new simulation cases prove the unused mixing candidate/stream value against frozen references and integer goldens; two real-production hazard cases pin seed `2026`, independent scheduling, newborn bits/lifetimes/markers, and counts **22 births/66 draws** versus skipped-tick-140 **21/63**. The shared frozen RNG imports no production constants or arithmetic; the hazard oracle independently freezes crossing/lifetime calculations. Logs: `/tmp/blob-royale-random-streams.60XDcn/preproof-gcc.log` and `preproof-clang.log`. Source review and pinned formatting passed. This releases world/snapshot/finalizer/hazard/encoder cutover, not the Step 9 checkbox or final gate. Uncommitted frontend/schema preparation was present during these focused proofs and is not a functioning end-to-end wire change yet.
  - Pre-delegation verify (2026-09-11 amendment): `./scripts/verify-focused 'unit.simulation|unit.gameplay' && ./scripts/verify-focused 'unit.simulation|unit.gameplay' linux-clang-asan-ubsan` before changing the old generator's written finalizer, `GameWorld::random()`, or the production hazard draw reader. Add the unused canonical mixing candidate and owned-stream value first; compare against an independent frozen old generator across mixed distribution operations. Also pin actual production hazard births against independent frozen generator/crossing arithmetic with seed `2026`, an independently scheduled 280-tick declaration-order/reservation specimen, and a no-reservation-at-tick-140 companion. Existing accepted hazard specimens remain unchanged. Retain these proofs after delegation; only their observed count accessor changes. The existing two-live-run test alone cannot detect a consistent seed or draw-order shift.
  - Ownership/seed/publication clarification (2026-09-11): A fixed-array `RandomStreams` value and one closed registry own stable explicit ordinals `hazards=0`, `hill=1`, names, and order. Hazards initialize exactly as the old match-seeded generator. Hill initializes from `mix64(match_seed + golden_gamma * 1)` using unsigned modulo-2^64 arithmetic and the canonical SplitMix64 finalizer; initialization consumes neither stream. Unknown runtime enum values fail with a structured error, never unchecked indexing or fallback. Preserve the old generator's count/state increments and distribution arithmetic, and its existing counter-wrap semantics. World copying and the existing non-throwing commit own all stream rollback; add no transaction socket. Snapshot publishes only a fixed per-stream count value, not seeds or generator state.
  - Wire/evidence correction (2026-09-11): `random_draw_count` exists only in the current C++ snapshot, not in v3 JSON. Replace that C++ scalar with named counts and **add** required closed v3 `random_draw_counts: {hazards, hill}`; do not claim removal of a nonexistent wire member. Use canonical data order `tick_sequence`, `random_draw_counts`, `entities`, `match`, and registry order within counts. Counts remain full uint64 in C++; encoding rejects values above `2^53 - 1` with stream-specific context rather than rounding/clamping. Test the actual production count-encoding helper directly at the boundary without adding RNG-state setters. Rollback proof must draw both streams, fail later, preserve the prior published snapshot, then produce the same successful observable continuation as a clean control. Extra draws on either stream must leave the other's actual sequence unchanged. The browser's load-bearing hazard seed remains untouched; accepted replay fixtures do not exercise hazard randomness, so the new frozen production proof supplies that missing evidence.
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.protocol|fixtures' && ./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.protocol|fixtures' linux-clang-asan-ubsan && ./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Notes: `GameWorld` owns the closed, ordered stream collection; every `world.random()` caller migrates to its explicit named accessor. The snapshot replaces its C++ scalar count with one count per stream and adds the matching v3 member in this step's wire row. Existing hazard draw sequences/counts and all accepted replay outcomes remain bit-identical. Amend ADR 0004 § "Determinism obligations for framework code" with a dated entry. Bots keep their existing external seeds and behavior; their identity-based derivation is Step 15. Hill motion consumes no new draws until Step 12.
  - Execution started (2026-09-11): Step 8 is committed as `42be52c`. Build and review the unused simulation candidates and frozen production-hazard proofs before either exact pre-delegation command. Frontend/schema preparation may remain uncommitted in parallel, but C++ world/reader/encoder cutover waits for both gates. Root owns all serial toolchain execution. No Step 9 verification or completion is claimed yet; Step 5 remains a separate human gate.

- [x] **Step 10: Add authoritative movement tuning**
  - Completion (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**, not native/release/performance evidence): Both exact full C++ gates passed **1377/1377**. The complete `./scripts/run-linux-toolchain -- ./scripts/verify-web` passed **505/505**, zero skipped/todo tests, formatting, strict typecheck/lint, generated drift, **75 schemas/21 examples**, and the production build (`final-web-lint-fixed.log`). The prior web attempts stopped on test-only readonly mutations and assertion lint; corrected input construction and typed last-call assertions preserve all expectations and production readonly contracts. `./scripts/verify-fuzz-regressions` passed **65 fixed-corpus executions across 7 harnesses** (`final-fuzz.log`), not a fuzz campaign. Pinned C++ formatter dry-run and `git diff --check` passed. All logs are under `/tmp/blob-royale-movement-tuning.PDrGmr`. Public v1 schemas/generated files and accepted motion expectations are unchanged. Shared authoring, intent recomputation, commit-only decisions, bounded session delivery, correlated client state, and independent semantic coverage are complete; UI controls remain Step 11. The documented ordinary-scale locomotion precision limitation remains, with no exhaustive representability claim or Step 5 approval. No push/deployment or new timing baseline was performed.
  - Final C++ gates (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): The exact required GCC filter passed **1377/1377** in `final-gcc-range-fixed.log`; the exact Clang ASan/UBSan filter passed **1377/1377** in `final-clang.log`, under the movement log directory. The first full GCC build caught two copy warnings in new conformance tests, corrected to const references before both successful runs. All accepted fixture motion expectations remain unchanged. Initial full web passed formatting, generated drift, and **75 schemas/21 examples**, then caught read-only result mutations in test inputs at strict typecheck. Correct those by constructing replacement input specimens, not weakening production types; full web rerun and fixed-corpus replay remain pending. No Step 10 completion is claimed yet.
  - Lower-layer sanitizer diagnostic (2026-09-11, advisory emulated Clang ASan/UBSan): The same explicit simulation/gameplay/runtime target build and filter passed **926/926**, with log `cutover-lower-clang.log`. Both lower-layer diagnostics now pass; they do not replace either exact final full filter. The fixed command-fuzzer's exhaustive visitor also gains the new tuning arm and four accepted/rejected request seeds before the required corpus replay; its old server-issued-command rejection remains unchanged.
  - Lower-layer diagnostic (2026-09-11, advisory emulated GCC): Explicit simulation/gameplay/runtime targets and `unit.simulation|unit.gameplay|unit.runtime` passed **926/926** after fixing a missing TickContext include, an obsolete scalar Sandbox test constructor, and two closed command-inventory expectations that omitted the newly appended kind. The initial two build failures and 924/926 test run remain correction history; no accepted motion oracle changed. Log: `cutover-lower-gcc-registry-fixed.log` under the existing movement log directory. This is supplemental feedback while backend/client tests finish, not the required full final filter. Independent result-path review closed room isolation and teardown/nested-callback findings after fixes. Add the single-frame oracle's three schema-inexpressible movement/result coverage checks before final verification.
  - Cutover inventory correction (2026-09-11): The active historical benchmark configuration also requires strict shared-movement migration. Preserve acceleration 400 and all historical workload values, author fixture ceiling 10000, and seed fresh benchmark worlds from that pair. Amend derived-schema provenance in its INI, README, and JSON; add a production-loader fixture regression. This is schema/construction maintenance, not a new timing baseline. Independent frontend review additionally requires room changes to clear prior-room exchange state while interrupted same-room requests become unknown; cover both at the hook boundary before final gates.
  - Prerequisite release (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): The unchanged required filter `unit.simulation|unit.gameplay|fixtures` passed **869/869 on GCC** and **869/869 on Clang ASan/UBSan** while old steering, match/command, configuration, and wire readers remained unchanged. The supplemental `unit.protocol.*movement tuning wire` passed **14/14 on each lane**. Logs under `/tmp/blob-royale-movement-tuning.PDrGmr`: `preproof-gcc-trajectories.log`, `preproof-clang-trajectories.log`, `preproof-wire-gcc.log`, and `preproof-wire-clang.log`; the earlier 866-test GCC run remains separate. Frozen legacy arithmetic/real-system bits, 28 sustained default trajectories, and the exact ordinary-scale later-identity guard witness remain in tests. The review records that numerical limitation; no totality/native/performance claim follows. This releases gameplay, simulation/runtime, application authoring, and protocol/server/client cutover, not the Step 10 checkbox or Step 5. Sandbox retains its existing command mask because it has no seats; shared authored tuning still drives its movement. Root owns all serial verification; final full C++/web/fixed-fuzz gates remain mandatory.
  - Supplemental unused-result proof (2026-09-11): Run `./scripts/verify-focused 'unit.protocol.*movement tuning wire'` on GCC and with `linux-clang-asan-ubsan` before wiring the new protocol-owned result value. The required simulation/gameplay/fixtures prerequisite builds this value but does not execute its direct protocol boundary tests. This supplements, never replaces, the exact prerequisite and final filters.
  - Owner acceptance/release (2026-09-11): The owner selected **Proceed with commit-confirmed feedback**: request IDs, server-confirmed applied/rejected results, and interrupted requests shown as unknown until current room state is checked. This resolves the review checkpoint below, not Step 5. The bounded design and implementation map are in `docs/reviews/2026-09-11-movement-tuning-boundary-review.md`; its adopted contract supersedes the old queue-acceptance assumption. Root owns all serial builds, generation, and verification. Release unused movement-value/arithmetic candidates and frozen proofs first; no live steering, command, configuration, or wire cutover before both pre-delegation gates pass.
  - Pre-delegation verify (2026-09-11 amendment): `./scripts/verify-focused 'unit.simulation|unit.gameplay|fixtures' && ./scripts/verify-focused 'unit.simulation|unit.gameplay|fixtures' linux-clang-asan-ubsan`. Retain unchanged `steered_acceleration` and its production reader while proving a frozen old implementation against both old and unused candidate arithmetic, including signed zero, subnormals, analog magnitude, unit-circle neighbors, and standalone legacy extremes. Pin real old-system sequences for held/replaced/zero input, authored acceleration before any command, and existing phase admission. New finite-step cap tests are separate from the pure-move proof. Retain the frozen proofs after delegation; never rewrite their expected bits to fit a new reader.
  - Ownership/arithmetic amendment (2026-09-11): ~~Validate the value in gameplay~~ — `src/simulation/movement_tuning.{hpp,cpp}` owns the validated pair, because match/command/runtime/protocol cannot depend on gameplay. Intrinsic tuning limits are acceleration **0..10,000 wu/s²**, normal top speed **1..10,000 wu/s**, defaults **400/600**; these are bounded first-version parameter ranges, not native solver-capacity certification or a clamp on external momentum. Fixture configurations explicitly use ceiling **10,000** and retain each prior acceleration; unchanged complete replay bits must prove the ceiling stays inactive. `GameModeConfiguration` carries the shared authored pair. `MatchState::movement` owns current/default pairs, revision, and effective tick; active values survive round reset. Normalize intent once using the old written arithmetic; preserve absent intent versus explicit zero. ~~Every tick that begins below the ceiling is unchanged~~ — the exact uncapped branch is unchanged; a tick crossing the ceiling must constrain propulsion. One gameplay locomotion module projects the requested Euler endpoint into the disc of radius `max(normal ceiling, current speed)` and returns acceleration, never velocity. Recheck canonical integration, speed, and non-amplification after materialization with a bounded fail-visible rounding policy. Integration and drag remain in phase 1. No new Running-only thrust gate. Reset old-body intent at the existing shared seating owner without discarding newly recorded commands; strip private intent at publication.
  - Bounded numerical policy (2026-09-11): Compute requested velocity only through canonical integration; preserve its physical-domain failures. Compare computed binary64 squared norms, not exact-real norms. Return requested acceleration verbatim when its endpoint satisfies `w·w <= max(V², v·v)`. Otherwise form the first radial projection from the original endpoint. If that first computed projection equals current velocity componentwise, accept zero as a quantized-projection identity; this can erase a tiny turn and is not a pure-outward certificate. Otherwise try the initial radial factor plus at most eight `nextafter` corrections toward zero, each recomputed from original `w`; each attempt permits its initial non-amplifying acceleration scale plus at most eight corrections. Recheck both canonical integrated squared speed and acceleration non-amplification. A later zero acceleration or endpoint equal to `v` fails immediately instead of shrinking into artificial braking. Exhaustion is `GAMEPLAY.LOCOMOTION_PRECISION_LOST`, never silent coast. This bounded witness search is not a correctly rounded projection or an exhaustive representability guarantee. Retain the tiny-noncollinear-turn regression from the numerical review.
  - Command/result amendment (2026-09-11): Extend the existing closed phase-0 controller visitor. Rank tuning after thrust, before Start, preserving all existing relative ranks. At tick N freeze entry revision R; seated membership is checked at the command's existing canonical position in all four accepted phases. Last eligible contender against R wins; matching-R losers are `superseded`, mismatches `stale_revision`, unseated senders `not_seated`, and maximum revision `revision_exhausted`. Commit the complete pair once to R+1/effective N even if values are equal; no winner changes nothing. Construct bounded tuning decisions before commit and return them only after successful commit. IDs correlate but never order contenders. Runtime exchange slots live under the mailbox's existing mutex so admission, priority eviction, completion, claim, and close have one atomic owner. Tuning remains non-lifecycle traffic. Terminal boundary refusals are `rate_limited`, `mailbox_full`, or `mailbox_evicted`, with no invented decision tick/revision. One unresolved exchange per open controller; strictly increasing safe request IDs (gaps allowed). Second unresolved requests close 1008 `tuning_request_in_flight`; reused IDs close 1008 `tuning_request_id_reused`. Initial tuning rate is one new exchange per **500 ms**, preserving the existing global pre-parse limiter and malformed-frame policy. Claim terminal results into the session active-write record before encoding/write and reopen the runtime slot then; a write callback releases only its own record, never a newer request. Disconnect/runtime/write failure before observation is unknown; no reconnect replay or acknowledgment recovery.
  - Wire/client amendment (2026-09-11): Required `match.movement` carries current/default pairs, limits, revision, and effective tick. Welcome publishes the tuning minimum interval. Add required nullable snapshot `data.tuning_result` outside match, carrying request ID, status, decision tick, revision, and nullable retry-after milliseconds; only a rate refusal carries a retry interval. A protocol-owned validated result plus server adapter keeps runtime types out of protocol. The same `sendCommand` path tracks pending/resolved/unknown tuning state and validates exact correlation before accepting a frame; no success inference from a shared revision or send return. Step 11 adds controls, not a second sender. Amend ADRs 0002/0003/0004/0005/0007/0008 in lockstep; retain the existing migration inventory below, including Sandbox, direct factories, replay parsing/initial seeding, and startup cross-value validation. The final verify below remains mandatory, plus the fixed fuzz corpus because authored configuration and command decoding change.
  - Review checkpoint (2026-09-11): Read-only source and rigorous-architect preflight found missing ownership/intake/result details, recorded in `docs/reviews/2026-09-11-movement-tuning-boundary-review.md`. The value must live in simulation; the current closed phase-0 handler must own controller-addressed changes; and mailbox acceptance is not proof that a tuning change committed. Proposed request correlation, commit-confirmed outcomes, bounded result ownership, and interrupted-request unknown status need the pending owner review. The `executing-plans` skill requires a review pause after successive substantial amendments. Step 9 is committed as `e4fad4b`; no Step 10 source, schema vocabulary, configuration, or ADR contract has been changed. After the checkpoint, amend this step and its kernel-seam allowlist/ADRs explicitly before implementation. Existing cooperative seated authority remains unchanged; this is not a request for new host/admin roles or approval of Step 5.
  - Owner clarification (2026-09-10): Browser keyboard/mouse movement supplies zero or unit direction; pointer distance or pointer speed never determines thrust strength. Requested acceleration comes from the authoritative shared movement tuning, subject to the established speed-ceiling and movement-lock rules. Preserve existing subunit analog command semantics for other producers and all accepted fixtures; do not globally renormalize the protocol to implement fixed-strength mouse control. Future fast/slow zones or periods may vary effective movement parameters at this same authoritative locomotion owner; no zone, timer, configuration key, or schema is implemented or reserved now.
  - Verify: `./scripts/verify-focused 'unit.gameplay|unit.simulation|unit.runtime|unit.protocol|unit.server|unit.application|fixtures|integration' && ./scripts/verify-focused 'unit.gameplay|unit.simulation|unit.runtime|unit.protocol|unit.server|unit.application|fixtures|integration' linux-clang-asan-ubsan && ./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Additional verify: `./scripts/verify-fuzz-regressions` (fixed corpus, not a new fuzz campaign).
  - Notes: `MovementTuning` (acceleration in wu/s², normal top speed in wu/s, both finite and inside a validated safety envelope) is a `MatchState` field beside `seats`, not a mode-state arm, so no mode duplicates it (`src/simulation/match_state.hpp`). Authored defaults come from the shared `[movement]` section of Step 1(c), validated in proposed `src/gameplay/shared/movement_tuning.{hpp,cpp}` and carried as a `GameModeConfiguration` member beside `hazards`. Retire the three per-mode `thrust_max_world_units_per_second_squared` keys and move every authoring of them, which the loader's strictness forces: `config/blob-royale.cfg`, `deploy/ubuntu-pc/blob-royale.cfg`, every `frontend-react/e2e/fixtures/*.cfg`, every `tests/fixtures/replays/*/match.ini` and the fixture reader, the fuzz corpus configs, `tests/integration/server_process_fixture.cpp`, `tests/unit/application/application_input_test_fixture.hpp`, and the four configuration test files. `ThrustSteeringSystem` no longer takes a scalar; it reads the match value, so amend the shared-systems rule at `src/gameplay/README.md:72-76`. Persist normalized steering intent on `Controllable` beside `commands_this_tick` and strip it at publication the same way, so no new kind and no private input on the wire. One shared locomotion function in `src/gameplay/shared/` recomputes stored acceleration every tick from intent, tuning, current velocity, and an input-lock predicate that has no lock source until Step 14: below the ceiling the arithmetic is `steered_acceleration` verbatim, so committed values do not move; above it, input may brake or turn but not add speed; charge and knockback are never clamped and decay under drag. Amend ADR 0003 § "State, units, and fixed time": acceleration persistence becomes intent persistence. Command `set_movement_tuning`: controller-addressed, both scalars, new arms in `command_registry.hpp` with the next rank after thrust, admitted in the approved phases with running-match admission required, deterministic winner among same-tick senders through the existing batch order, Apply plus Start in one batch starting with the accepted values, reset to authored defaults through the same command, rate limit and cross-room rejection at the existing boundary, and explicit outcomes. Publish the values, a revision, and the effective tick in `MatchState` and on the wire (this step's v3 row). Tests: perpendicular, reverse, and overspeed steering; a lowered limit keeps knockback momentum; stale, invalid, and rate-limited commands; competing controllers; two rooms isolated; held intent recomputed on the effective tick without bypassing locks; no process-global mutation or private per-player advantage.

- [x] **Step 11: Wire live tuning controls in the room UI**
  - Completion (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): Exact `./scripts/run-linux-toolchain -- ./scripts/verify-web` passed **568/568**, zero skipped/todo tests, formatting, strict typecheck/lint, generated drift, **75 schemas/21 examples**, and the production build. Log: `/tmp/blob-royale-tuning-ui.NXC1Wc/web-type-fixed.log`; the first run stopped at a new test fixture's TypeScript narrowing error, corrected without changing production types or assertions. Independent source review closed immediate prior-room outcome leakage, a wall-clock cooldown inconsistency, and local send refusal hidden behind a historical result. The panel preserves dirty drafts with explicit review, separates current state from exact outcomes, and uses one sender with welcome-scoped IDs and monotonic local rate guards. Canonical editing isolation and connection interval projection are covered. No C++/wire/accepted fixture changes, browser verification, native release evidence, or deployment are claimed by this frontend-only step. The unchanged dependency install reports the same four audit findings as Step 10; no dependency update was performed. Step 11a remains pending.
  - Commit-confirmed alignment (2026-09-11): The owner's Step 10 decision supersedes deriving an individual request outcome from a changed room revision. Read exact API-correlated resolved/unknown state, with current room pair/revision/effective tick displayed separately. Preserve one existing sender and allocate request IDs above conditional panel/body rendering, scoped to room plus immutable welcome-object identity (numeric controller IDs may repeat after a server restart), never ownEntityId. Project the welcome's existing minimum interval through the connection identity. Editing isolation belongs in the canonical thrust-input hook: clear held input on focus entering the tuning region, preserve native editor arrows, and require a fresh gameplay press on exit without changing camera-button bindings.
  - Owner acceptance (2026-09-11): **Keep draft; require review.** This supersedes the pending preflight question: when a peer changes tuning while the user has a dirty draft, retain the draft and its base revision, show the authoritative new values and conflict, and require explicit review/rebase before Apply. Reviewing changes the draft's comparison revision, not its values, and never implies an interrupted request applied. Reset remains an explicit submission of authored defaults through the same path. Implementation is released; the checkbox waits for the exact full web gate.
  - Verify: `./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Notes: Labelled numeric/range controls for acceleration and normal top speed in the room UI with the approved Apply/Reset interaction; coalesce local edits behind Apply and bound the update rate; show authoritative values, revision, and effective tick with pending/applied/rejected derived from published state, never from the send. Reset submits authored defaults through the same command. Keep camera controls independent. Tests: no thrust or camera command while editing, no optimistic false success, unchanged steering and camera bindings, and two hook instances for two rooms holding different values.

- [x] **Step 11a: Add direction-only mouse steering**
  - Completion (2026-09-11, **advisory Mac/arm64 hosting Docker Linux/amd64**): Exact `./scripts/run-linux-toolchain -- ./scripts/verify-web` passed **603/603**, zero skipped/todo tests, formatting, strict typecheck/lint, generated drift, **75 schemas/21 examples**, and production build. Exact `./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e` passed **11/11**, zero skipped/flaky/retried cases, with the pinned Chromium revision and e2e strict typecheck. Final logs: `/tmp/blob-royale-cursor-steering.JQebBC/web-layout-fixed.log` and `browser-layout-fixed.log`. All eight prior browser outcomes remain, with deliberate cursor/Space migration; three new cases prove unit-strength direction, release/coast, stationary-pointer/manual-camera/DPR/resize behavior, room isolation, editing/departure/drag cancellation, fresh activation, and overlay containment. Existing race/recovery cases now also hold go across observed body loss/reconnect. The generated `out/test-results/cursor-steering-review/room-tuning.png` was visually inspected after the layout correction. Independent review closed synchronous sender retirement, refused-send feedback retry, same-body sender replacement/throttle, and ordinary versus unexpected capture-release behavior. An initial new fixture typing error was corrected through the existing snapshot validator. The earlier browser focus failures and pre-layout passing runs remain separate evidence, as explained below. No C++/wire/accepted physics fixture changes, native-focus/performance/release certification, Step 5 approval, push, or deployment are claimed. The unchanged dependency install still reports the same four audit findings; no dependency update was performed.
  - Visual QA correction (2026-09-11): The first fully passing 603-test web/11-scenario browser snapshot exposed a layout defect in its screenshot: the taller movement sidebar stretched `SimulationStage`, placing the match overlay below the canvas. Bound the stage to its content height and existing 960-CSS-pixel width cap, and add real-browser overlay-card containment with a taller-sidebar precondition. This small layout correction changes no projection, camera policy, command, or physics; rerun both complete gates and re-inspect the image before completion.
  - Browser evidence boundary (2026-09-11): The pinned headless Chromium does not provide a real tab-blur transition through `bringToFront`, even after disabling Playwright's forced-focus emulation; `document.hasFocus()` remains true. Do not substitute a synthetic event or claim a browser blur result. Keep blur cancellation/fresh-activation in the full input/integration gate, and prove actual editing, pointer departure, manual drag, reconnect/body loss, and fresh activation in the real browser. Remove the unsupported newly proposed tab-blur subscenario, not any of the eight accepted browser flows or the production cancellation behavior. The exact full web/browser gates below remain required; this is a proof-boundary correction, not a completion claim.
  - Observation boundary (2026-09-11 preflight): Keep Feature's canonical input/sender and Canvas's existing projection/pointer owner, connected by a stable observational callback; no second scheduler or geometry implementation. Cancel on a new immutable welcome, a changed owned entity ID, or observed body loss/replacement, never every fresh snapshot/body object. The current wire has no body incarnation identifier, so same-entity removal/recreation wholly between delivered snapshots is not observable; do not infer it from position jumps or claim that stronger cancellation guarantee. No new wire vocabulary is introduced by this step.
  - Owner replacement (2026-09-11): ~~Right-button hold and keyboard-direction priority~~ are superseded by **cursor-only movement direction with a held keyboard “go” action**. Directional WASD/arrows no longer compete with the cursor; former movement keys are reserved for later abilities, not assigned charge/shield implicitly. The owner selected **Space** as the go key, replacing right-mouse activation; Step 21 must revisit its earlier proposed Space-for-charge binding. Preserve left-drag camera panning and the canonical zero/unit direction, fixed-strength sender. Release of go clears intent and coasts; clear held activation on blur, editing, disconnect, loss of body, and later stun, requiring fresh activation. With no nonzero direction at the exact center, request zero thrust; do not invent an aim or use pointer distance as strength. Tests and existing browser scenarios must migrate deliberately from keyboard direction to cursor aim plus go; do not retain an unrequested competing steering mode. This is a design amendment, not implemented controls or Step 5 approval.
  - Verify: `./scripts/run-linux-toolchain -- ./scripts/verify-web && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e`
  - Notes: Extend the canonical `useThrustInput` intent/sender path, not a second command scheduler. Use canvas-local CSS pointer position relative to the projected owned blob center and normalize to zero/unit direction; reuse `worldProjection` and recompute when the blob, camera, or viewport moves under a stationary cursor. Reuse `set_thrust` with its existing identity/admission/throttle; send no device-specific coordinates or new wire vocabulary. Cursor aim supplies direction exclusively and held Space activates propulsion; WASD/arrows no longer steer. Primary/left drag remains manual camera panning. Do not suppress right-click context menus for the superseded steering gesture. Space release clears thrust intent and coasts through existing drag rather than zeroing velocity. Exact-center aim is zero, never a retained accidental propulsion direction. Clear held activation on pointer departure/cancel, camera gestures, blur, editing/UI interaction, disable, disconnect, and body replacement; fresh activation is required afterward. Keep aim observation and propulsion activation separate so later abilities reuse the same aim. Tests cover equal near/far strength, cardinal/diagonal cursor aim, stationary-cursor updates, camera/DPR/resize invariance, ownership and every cancellation path, native keyboard control activation, and removed directional bindings. Deliberately migrate existing browser steering scenarios and add a real-browser cursor/Space movement-release-camera-isolation scenario.

- [x] **Step 12: Implement deterministic random hill roaming**
  - Completed (2026-09-11): All required gates passed on the Mac-hosted Linux/amd64 advisory toolchain, with unchanged accepted fixtures. `./scripts/verify-focused 'unit.gameplay|unit.simulation|unit.application|fixtures'` and the same command with `linux-clang-asan-ubsan` each passed 1,042/1,042. `./scripts/verify-focused 'unit.protocol'` and its `linux-clang-asan-ubsan` invocation each passed 155/155. `./scripts/verify-fuzz-regressions` passed all seven fixed-corpus harnesses (10 application configurations, 10 maps, 3 CLI, 3 HTTP, 19 commands, 19 request IDs, 2 scenarios); this is corpus regression, not a fuzz campaign. `./scripts/run-linux-toolchain -- ./scripts/verify-web` passed 626/626, zero skipped/todo tests, 76 schemas/22 examples, typecheck, lint, and production build. Logs: `/tmp/blob-royale-hill-roaming.jaAaig/{gcc-clock-fixture-fixed,clang,protocol-gcc,protocol-clang,fuzz,web-final}.log`. This supplies no native capacity, release, or Step 5 acceptance evidence.
  - Lifecycle-fixture correction (2026-09-11): The first complete GCC selection passed 1,041/1,042; only the new clock/reset scenario failed because it supplied one participant, invoking the existing immediate solo-victory rule before its intended clock deadline. Give that new fixture two explicitly seated participants/spawn markers while retaining the exact phase/reset assertions. The existing solo rule and every accepted fixture remain unchanged; rerun the full required gates.
  - Compile corrections (2026-09-11): GCC rejected a new snapshot test's `{}` argument because `SimulationConfig` is validated and not default-constructible, then a new boundary test's borrowed bounds from a temporary map. Supply the named fixture's explicit validated configuration and retain the map as a local value; no production API or accepted expectation changes. Restart both-lane verification after correction.
  - Verification correction (2026-09-11): The first web run passed format/schema/typecheck/lint but the new negative-zero velocity cases failed. A focused diagnostic isolated exactly those two failures; JSON Schema cannot distinguish signed zero. Extend the existing entity invariant walker for `hill_motion.velocity` using its canonical rejection helper, leaving the tests and all older kinds unchanged. Re-run the full gate; no completion is claimed from the initial failure or source review.
  - Superseded preflight checkpoint (2026-09-11): ~~Add a swept-disc-clearance/contact and ground-contained roam-admission prerequisite before Step 12.~~ The owner's clarification removes the premise: the hill does not bounce and may travel over restricted/dangerous cliff areas. The owner also selected any locally safe region, rather than requiring player access from a spawn; this does not reinstate safe-disc containment along the route. `docs/reviews/2026-09-11-hill-roaming-boundary-review.md` is retained as conditional historical geometry analysis, not an active hill implementation instruction. No shared geometry extension, reflection solver, ground-bound body, or safe-ground creation is needed for this clarification. Existing player terrain rules, marker-tour arithmetic, random consumption, fixtures, and the exact verify below remain unchanged.
  - Resolved outer-boundary contract (2026-09-11): ~~Steer back before the outer map edge or permit off-map travel.~~ The owner requires cancellation of outward velocity and position change instead. Retain ADR 0007's center-in-closed-map convention (the circle may overhang). Form the proposed endpoint through canonical unbounded motion then position integration. Independently per axis, strict overshoot retains the starting coordinate and zeros velocity; exact arrival commits the coordinate and zeros outward velocity. Preserve tangential motion, without renormalization, bounce, epsilon, early retarget, or extra draws. Whole-axis cancellation can stop short of an edge. Scheduled retargeting continues while stationary; repeated outward selections may remain canceled, so no maximum stationary duration is promised. Interior terrain never affects hill motion. No owner routing question remains.
  - Verify: `./scripts/verify-focused 'unit.gameplay|unit.simulation|unit.application|fixtures' && ./scripts/verify-focused 'unit.gameplay|unit.simulation|unit.application|fixtures' linux-clang-asan-ubsan && ./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Verify supplement (before implementation, 2026-09-11): `./scripts/verify-focused 'unit.protocol' && ./scripts/verify-focused 'unit.protocol' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions`. The new v3 kind and strict application keys require protocol and fixed-corpus coverage in addition to, not instead of, the original gates. Root runs all builds serially.
  - Concrete implementation contract (2026-09-11): Append strict fields `hill_motion`, `hill_speed_minimum`, `hill_speed_maximum`, `hill_retarget_minimum_seconds`, and `hill_retarget_maximum_seconds` to the existing configuration. Defaults are `marker_tour`, 20, 70, 0.35, and 1.2; migrate every existing authored configuration explicitly without changing its policy or accepted outcomes. Sampled scalar speed is finite and within `[2^-10, 1,000,000]` wu/s, with ordered minimum/maximum; the normalized vector realization is binary64-rounded. Retarget bounds convert to ordered positive ticks and are at most 3,600 seconds. These are engineering intake bounds, not balance/performance certification. New roam scenarios are separate fixtures.
  - Sampling/state contract (2026-09-11): One hill-owned pure policy, `hill_roaming`, reuses existing motion arithmetic; no player-kernel or geometry seam changes. Draw from the world's `hill` stream in written call order: unit interval for rational quarter-circle parameter, `next_below(4)` for quadrant, unit interval for scalar speed, then `next_below(max_ticks-min_ticks+1)` for inclusive retarget ticks. Equal ranges still draw; integer rejection remains the canonical generator's visible draw consumption. Rational direction plus written `sqrt(x*x + y*y)` normalization avoids trig, zero-vector retries, and platform distributions; angular density is deliberately not claimed uniform. `HillMotion` exists only for roam, with current velocity and optional private schedule (next-retarget tick and stream identity); world-owned `RandomStreams` remains the sole RNG state. Publication strips the schedule. No speculative mode-state fields are added. Lobby/countdown restore the initial marker and empty schedule without draws; running draws immediately when unscheduled and at scheduled expiry, then advances before scoring; ended freezes; new lobby resets the non-participant hill. Deadline overflow and invalid arithmetic fail visibly and transaction rollback restores motion and RNG.
  - Proof inventory (2026-09-11): Legacy tour fixtures and arithmetic unchanged with zero hill draws; deterministic seeds/draw order and retarget cadence; public/private projection and v3 positive/negative schema cases; boundary exact arrival versus overshoot, edge/corner sliding or rest, continued retarget while stopped, canonical no-boundary arithmetic and failure behavior; movement across dangerous/disconnected terrain without creating floor; running/ended/lobby lifecycle, score/geometry order, and failed-tick motion/RNG rollback. ~~Prove no stall and strictly positive actual speed every tick.~~ Boundary cancellation intentionally permits lower/zero actual speed; no dwell/teleport is introduced in interior movement.
  - Notes: Extend the existing hill motion seam with `marker_tour` and `random_roam` under a strict `[king_of_the_hill] hill_motion` key with positive speed and retarget ranges. Committed velocity, next-retarget tick, and stream identity live in a new component, proposed `HillMotion`, whose publication strips future targets through `ComponentPublication`; scoring and rendering keep reading the same published `Hill`. Roam draws only from the Step 9 `hill` stream with a written draw order; the tour policy draws nothing. ~~Reflection and clearance use Step 3's disc clearance; reject an impossible ground-contained roam region at map validation.~~ Superseded by the 2026-09-11 owner correction: the hill is terrain-independent and does not bounce; do not add terrain-fit or spawn-connectivity admission for its route. Lobby and countdown hold, running moves, ended freezes, and the non-participant hill's motion resets explicitly on the lobby tick; advance before scoring. Client: one registry entry for the new kind. Prove no authored dwell or teleport, with boundary rests explicitly allowed; travel across dangerous terrain and regions disconnected from spawns without altering player support; speed bounds; unchanged tour random consumption; failure rollback; and score/geometry agreement.

- [x] **Step 13: Add the body-bound component lifetime**
  - Completed (2026-09-11): `./scripts/verify-focused 'unit.simulation|unit.gameplay|fixtures'` and `./scripts/verify-focused 'unit.simulation|unit.gameplay|fixtures' linux-clang-asan-ubsan` each passed 920/920 on the Mac-hosted Linux/amd64 advisory toolchain. Logs: `/tmp/blob-royale-component-lifetime.7Mtbvi/{gcc,clang}.log`. Six new tests cover the registry sweep and shared respawn; the direct scorer scenario now invokes its actual lifetime owner with unchanged outcome assertions. All accepted hill/race return-timing and replay fixtures remain unchanged. Independent scoped source review found no actionable issues; it did not substitute for the gates. ADRs 0004/0007/0008 and domain READMEs now name the single cleanup owner. No new wire value, native/release evidence, or Step 5 acceptance is claimed.
  - Preflight clarification (2026-09-11): The registry sweep must clean every already-bodyless as well as newly bodyless entity, including non-participants, once at the end of `RespawnSystem::apply`. Put the default-false trait in a dependency-light simulation header and consume it through one `GameWorld::erase_body_bound_components_without_body()` method, iterating each bound store in ascending entity order without allocation. Migrate both the elimination-event and already-bodyless `HillPresence` cleanup from the scorer; its direct knockout unit scenario must call the shared lifetime owner while preserving its expectations. ~~`ZoneExposure` needs no migration~~ means no royale cleanup-path migration: its documented partial-grace lifetime is body-bound too, so declare the trait for it while leaving royale's whole-entity destruction unchanged. Keep all body-independent components, delayed/zero-delay return timing, accepted replays, and wire values unchanged. Verify registry-wide post-sweep invariants, adjacent removals, mixed alive/bodyless entities, persistence, idempotence, and duplicate elimination.
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.gameplay|fixtures' && ./scripts/verify-focused 'unit.simulation|unit.gameplay|fixtures' linux-clang-asan-ubsan`
  - Notes: A per-kind trait beside publication, proposed `src/simulation/component_lifetime.hpp` with `ComponentLifetime<T>::bound_to_body` defaulting to false, consumed generically over `ComponentRegistry`. `RespawnSystem` performs one registry sweep erasing every bound kind of every bodyless entity after its body removals, so it still names no kind (`src/gameplay/shared/respawn_system.cpp:67`). Migrate `HillPresence` onto the trait and delete the hand-written knockout clear in `hill_scoring_system.cpp`; `hill_return_timing_tests.cpp` and the hill replay fixtures must not change, which pins the tick. ~~`ZoneExposure` needs no migration because royale destroys the entity.~~ Its trait is declared too; royale's existing whole-entity destruction needs no cleanup-path migration. A test asserts that no bound kind survives on a bodyless entity after the sweep. Every later ability kind declares the trait and nothing else.

- [x] **Step 14: Add shared status windows, stun, and the steering input lock**
  - Completed (2026-09-11): Both original `unit.simulation|unit.gameplay|unit.protocol|unit.runtime|fixtures` gates passed 1,197/1,197; both `unit.controllers` supplements passed 87/87. Fixed-corpus regression passed all seven harnesses (10 application, 10 map, 3 CLI, 3 HTTP, 24 command, 24 request-id, 2 scenario inputs). Full web verification passed 692/692 tests plus 77 schemas and 25 examples, generated-drift checks, typechecking, and production build. Pinned C++ format dry-run and `git diff --check` passed; 93 non-Markdown source/schema/test paths remained hash-identical throughout final verification. Logs: `/tmp/blob-royale-stun-input.qGJOqv/{gcc,clang,controllers-gcc,controllers-clang,fuzz,web,cpp-format-check}.log`. Core, browser, and integration source reviews found no blockers; the documented inherited sender-replacement and invisible entity-replacement limitations remain explicit. Existing accepted physical/replay outcomes and diagnostic bot random behavior are unchanged. This is Mac-hosted Linux/amd64 advisory evidence, not native/performance/release certification or Step 5 approval. The status foundation is implemented; production parry impact remains Step 18.
  - Owner-approved implementation amendment (2026-09-11): Guarantee canceled Space activation across entirely missed stun snapshots, not merely observed stun. Follow `docs/reviews/2026-09-11-stun-input-generation-review.md`: optional persistent `Controllable.input_generation` and exact optional `ThrustCommand` echo; absence means never invalidated, and every applicable positive stun request assigns its positive committing tick. Preserve the token at publication and through expiry/same-entity return. Both nonzero thrust and zero releases require exact equality; stale commands do not replace valid intent. The browser captures a token only on fresh activation and cancels pending/held work on generation change without retagging it. This does not extend the existing guarantee across invisible entity destruction/replacement.
  - Dependency/ownership corrections (2026-09-11): ~~TickWindow lives in gameplay/shared~~ — it is a simulation value because simulation components contain it. ~~Extend the Step 10 lock predicate~~ — add the first canonical gameplay `input_is_locked` predicate; no such predicate exists yet. Step 14 explicitly owns the command/value/registry additions and intake/publication changes required by the selected guarantee; it adds no kernel policy socket, physics algorithm, or external mutable-world access. `StatusSystem` is appended last in every PostKernel list, preserving existing relative/lifecycle order; registration-count assertions change, not accepted world outcomes. Publish both activation and expiry ticks identically for browser and in-process readers.
  - Request/window contract (2026-09-11): Windows are `[t,t+d)` with checked safe-integer expiry. Zero-duration requests and missing/bodyless/static targets are no-ops. Validate/aggregate applicable requests before any mutation; reject positive requests at committing tick zero. Merge active intervals by maximum expiry, replace expired intervals without bridging gaps, and invalidate input even when a longer existing window wins. Clear intent to explicit zero (absence preserves authored acceleration) and clear propulsion, never velocity. Body-loss removal uses Step 13; later bumps and hazard lifetime continue. Duration one has no following fully locked steering tick because application is PostKernel.
  - Foundation exception (2026-09-11): Explicitly permit `StunRequest` registration with an injected in-tick test producer here before Step 18's production contact producer. Amend ADR 0004 and registry/domain documentation's no-unproduced-event rule narrowly; do not add a production stun command, parry implementation, or reserved ability vocabulary. Four active bots use shared public-observation steering authoring that echoes generation and suppresses observed stun. Literal scripted replay stays literal, and old CSV fixtures and diagnostic random behavior stay unchanged.
  - Verify supplement: `./scripts/verify-focused 'unit.controllers' && ./scripts/verify-focused 'unit.controllers' linux-clang-asan-ubsan`. These authoring sites now change; retain the original gates below in full, serially.
  - Mouse lifecycle amendment (2026-09-10): The fresh-input-after-stun rule applies equally to Step 11a's cursor aim plus held Space activation. A held go key must not resume thrust or queue an ability automatically on expiry. Stun stops current momentum at the parry and blocks self-propulsion, but the body remains movable by later external collisions.
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.protocol|unit.runtime|fixtures' && ./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.protocol|unit.runtime|fixtures' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions && ./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Notes: One tick-window value in ~~`src/gameplay/shared/`~~ `src/simulation/`, proposed `tick_window.hpp`: activation tick plus duration ticks, half-open, `contains(tick)`, `expired(tick)`, and status-owned active merge by maximum expiry; every ability timer from here on is this value and nothing decrements per tick. A `Stun` component (body-bound through Step 13, publishing activation and expiry) and a typed `StunRequest` world event, a new `WorldEvent` arm applied at `kPostKernel` by a shared status system that merges repeated requests by maximum expiry and never restores old velocity. ~~The Step 10 lock predicate gains stun as its first source~~ The new canonical gameplay input-lock predicate reads stun: stun clears steering intent, ignores commands received while active rather than queuing them, requires a fresh press after expiry, and blocks new ability activations. A stunned hazard stays stopped until bumped and its lifetime keeps counting. Zero-delay respawn and round restart clear it through the trait and the reset system. Wire: the `stun` component kind under v3 with a non-visual client registry entry until Step 20. Tests: exact expiry tick, the no-input interval, merge, bodyless and zero-delay cleanup, held key not resuming, and human/bot/replay symmetry; a test inserts the stun directly because the momentum kill at impact is Step 18's.

- [x] **Step 15: Add tactical profiles and decision infrastructure**
  - Completed (2026-09-11, Mac/arm64-hosted Docker Linux/amd64 advisory): After the session-harness correction, both original `unit.controllers|unit.application|unit.simulation|unit.protocol` lanes passed 1,014/1,014, including 124 controller tests each; both `unit.runtime|unit.server|fixtures` supplements passed 262/262. Full web passed 742/742, generated drift, 78 schemas/28 examples, strict typechecking, lint, and production build. Fixed corpus passed 91 executions over seven harnesses (15 application, 10 map, 3 CLI, 3 HTTP, 29 command, 29 request-id, 2 scenario). Complete Chromium passed 13/13 with no retries, flakes, or skips, including profile selection/reconnection and isolated seeker/coaster/human movement. All 95 changed/new C++ paths stayed hash-identical through this final serial chain; pinned formatting and `git diff --check` passed. Logs: `/tmp/blob-royale-step15.whkhDu/complete-{gcc,clang,web,supplement-gcc,supplement-clang,fuzz,browser,format-check}.log`. Exact commands, prerequisite proofs, source reviews, limits, and correction history are retained in `docs/reviews/2026-09-11-tactical-profiles-review.md`. No accepted replay/physical baseline, legacy diagnostic arithmetic/draw behavior, or shipped map changed. All evidence is advisory, not native/performance/release certification. Phase B is complete; Step 5 remains unapproved, Phase C remains forbidden, and nothing is pushed or deployed.
  - Supplementary harness correction (2026-09-11): GCC runtime/server/fixtures passed 261/261, but Clang passed 258/261 because three existing session tests missed the transient open state. A failure-only trace recorded `session.opened` before the directory assertion observed retirement. The boundary harness supplied a never-ready publication; its first presentation slot legitimately closes the session. Give that harness a separate running publication owner while preserving its undrained command fixture, and add an explicit unready-publication/1013 regression. Keep the integrated live-runtime harness, production readiness semantics, deadlines, and existing assertions unchanged. This is a test-isolation correction, not a runtime/catalogue behavior change; repeat the full final gates before checking this step.
  - ADR lockstep (2026-09-11): Dated entries in ADR 0003 record the optional exact-declaration guard and unchanged command order; ADR 0004 records tactical-only tick timing, seed/idempotence, and preserved diagnostic behavior; ADR 0006 records full declaration reconciliation and the single runtime/session catalogue. These clarify the already-approved implementation contract rather than expanding it. ADR 0008 retains the four-setting decision and deferred combat scope.
  - Promotion prerequisite passed (2026-09-11, Mac-hosted Linux/amd64 advisory): Exact `unit.controllers` selection passed 95/95 on both GCC and Clang ASan/UBSan, with 55 changed/new C++ paths hash-identical across the passing runs. New complete Chaser/HillSeeker/Racer comparisons and the original Racer projection oracle all passed before any production reader switched. Logs: `/tmp/blob-royale-step15.whkhDu/{prerequisite-gcc-copies-fixed,prerequisite-clang}.log`. Release the proven reader cutover and tactical/configuration implementation; the step and all final gates remain unfinished.
  - Validated-value correction (2026-09-11): Direct `MatchConfiguration::create` vectors bypass the text parser's existing positive-count, duplicate-declaration, and 16-term checks. Apply those same invariants at the value boundary when adding full kind/profile identity, preserving every valid legacy roster. This closes an alternate-construction bypass, not a new gameplay limit. Detailed preliminary corrections and review evidence are retained in `docs/reviews/2026-09-11-tactical-profiles-review.md`.
  - Pre-cutover review correction (2026-09-11): A read-only helper review found that raw Racer division checks and the old projection-only candidate did not exercise a complete Racer through the new observation/steering helpers. Add that full three-way comparison in unused fixtures before either prerequisite lane; preserve the original Racer oracle and all production readers until both pass. No accepted behavior or owner decision changes.
  - Preliminary web corrections (2026-09-11): Strict schema validation rejected the first non-NPC profile prohibition's `not/required` shape; express it as a false property schema under the non-NPC condition, preserving rejection semantics. Schema/example validation then passed. Initial web checks stopped at unformatted new files, then two new test-only type errors (unsupported role-query `exact` option and omitted required `controller_kind`). Apply pinned formatting, use an anchored name matcher, and complete the fixture; retain all assertions and rerun the full gate. No completion is claimed from these preliminary runs.
  - Implementation contract (2026-09-11): Follow `docs/reviews/2026-09-11-tactical-profile-contract.md`. Objective seeking is a probability of issuing unit-strength go versus coast, not analog thrust scaling; reaction/aim-error/persistence are active settings. Use one immutable bounded catalogue for runtime admission and both welcome projections; preserve legacy unprofiled output and all existing indexed human/CSV join semantics. Only reconciler-issued joins carry the optional exact-declaration guard. Tactical-only seed/idempotence applies; no diagnostic seed/draw changes. Sandbox tactical startup is explicitly unsupported, and no bare tactical choice is advertised. The contract names initial tuning, basic straight-center terrain screening and its limits, round identity, parser grammar, field ownership, and current-snapshot-only decisions.
  - Verify supplements: After the original gates, run `./scripts/verify-focused 'unit.runtime|unit.server|fixtures' && ./scripts/verify-focused 'unit.runtime|unit.server|fixtures' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e`. Runtime/server now own new catalogue admission and queued-join identity behavior; retain all original checks in full.
  - Promotion prerequisite (2026-09-11): First add unused canonical controller observation/steering helpers and independent frozen full Chaser/HillSeeker references; retain the existing independent Racer oracle and frozen RNG. Prove helpers and the still-unmodified production readers against those references on both lanes with `./scripts/verify-focused 'unit.controllers' && ./scripts/verify-focused 'unit.controllers' linux-clang-asan-ubsan` before any reader switches. Preserve raw coordinate-difference domains, player/body lookup distinctions, exact written multiply/divide/sqrt/clamp order, strict ties, command bits/address/count, spawn retries, and draw order/count, including repeated identical observations and zero jitter. This prerequisite adds no new selectable kind or wire vocabulary; root alone runs the serial gates. Do not mark Step 15 complete at this intermediate checkpoint.
  - Owner scope resolved (2026-09-11): Implement working objective-seeking, reaction timing, aim error, and target persistence settings now; add aggression/charge/shield settings only when their combat behavior exists. `docs/reviews/2026-09-11-tactical-profile-preflight.md` records the source-backed prerequisites. Preserve legacy bot seeds, draw order, and repeated-observation behavior; new seed derivation and idempotence apply only to tactical bots. Pin profile declaration identity, actual round identity, sandbox admission, and pure-move proof before reader cutover. This approval does not tick the step or approve Phase C.
  - Verify: `./scripts/verify-focused 'unit.controllers|unit.application|unit.simulation|unit.protocol' && ./scripts/verify-focused 'unit.controllers|unit.application|unit.simulation|unit.protocol' linux-clang-asan-ubsan && ./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Notes: One `tactical` controller kind with a validated profile value, following the existing personality convention (a profile is data, never a type). Extend the strict section-family parser for `[bot_profile.<name>]`; unknown profiles fail visibly. Objective providers are a closed `constexpr` table keyed by mode-state schema id, the way `controller_registry.hpp` and the client's renderer registries are keyed, each producing the same tactical candidate shape from public observation only. Propagate the selected profile through seat declaration (`seat_npc` gains a profile, this step's v3 command row), factory and seat reconciliation, the welcome catalogue, and lobby selection in the client. Derive distinct stable seeds from match, seat, profile, and round identity instead of the shared match seed (`src/application/bot_reconciliation.cpp:110`). Factor shared body lookup, geometry, and steering out of the existing kinds as pure moves; the simple kinds stay as diagnostic options. Repeated observations are idempotent. No mood subclasses, no hidden future state, no controller-to-gameplay dependency.

### Phase C: gate-dependent work (authorized by Step 5 on 2026-09-11)

- [x] **Step 16: Adopt the reviewed continuous motion kernel**
  - Completed (2026-09-11, Mac/arm64-hosted Docker Linux/amd64 advisory): Both exact original focused lanes passed 1,000/1,000 (952 unit + 48 fixtures), and both exact application/protocol/runtime/server/controller supplements passed 659/659. The required clean release benchmark runner passed all eight explicitly categorized cases plus delivery; all three pure-solver workload/correctness/work records exactly match both historical prototype artifacts, all retained grid geometry/population/density/pair-hash/work/sample fields match the pre-cutover suite, and both live cases retain every correctness value. Fixed-corpus replay passed 91 executions over seven harnesses; full web passed 754/754 with 79 schemas/29 examples, generated drift, strict typechecking/lint, formatting and production build; Chromium passed 13/13 with no retries, flakes or skips. All 126 implementation/input paths stayed hash-identical through the final sequence, all 79 changed/new C++ paths passed pinned formatting, and git diff check passed. Exact commands, corrections, deliberate wall/contact/population expectation changes, review findings and the new separate benchmark artifact are in `docs/reviews/2026-09-11-live-motion-adoption-review.md`; logs are `/tmp/blob-royale-step16.OUzlJW/final-cutover-*.log`. Frozen legacy implementations, the pure discrete wrapper, historical benchmark artifacts, and accepted gameplay replay outcomes remain unchanged. This is not native/performance/release certification; no push/deployment occurred. Production falling/race registration remains Step 17 and guarded composition remains Step 18.
  - Initial cutover verification (2026-09-11, Mac-hosted Docker Linux/amd64 advisory): Full GCC build succeeded; CTest discovery first failed because one generated server test executable lacked its executable bit. Repair only that generated file's mode and rerun. The first complete original filter passed 994/999 (all 48 fixtures passed); failures identify the obsolete 4,096-physical-body snapshot fixture, two rejection probes that now lie inside the closed envelope, one new wall arithmetic expectation, and the historical early-snapped wall direction at tick 2,000. A no-build supplemental diagnostic passed 655/659; failures identify the equivalent application-envelope probe, the unreachable v1 4,096-body encoder fixture, and two new encoder fixtures publishing tick zero. Preserve the old total entity population with bodyless identities, test the reachable live physical ceiling without changing legacy wire limits or adding a snapshot bypass, move invalid-center probes genuinely outside, and advance encoder fixtures through a real successful tick. Pin the wall/legacy chronology from evidence rather than widening tolerances. Independent review also requires entry-grid rollback observation, precise fault provenance and moving retries, and a precise legacy-oracle divergence assertion. These diagnostic runs are correction evidence, not final completion gates.
  - Owner-approved benchmark disposition (2026-09-11): The owner answered "yes" to converting the three oversized full-simulation cases into explicitly labeled grid-only diagnostics while preserving geometry and historical baselines, then continuing implementation. This releases the checkpoint below. Preserve sparse-64 and historical royale as live cases and the three pure-solver cases unchanged; keep original large-case geometry/counts as explicitly named spatial-grid diagnostics with historical-name provenance. Do not synthesize post-step snapshots, retain a legacy engine, increase the motion cap, or select a fallback based on population. The prerequisite gates already passed; proceed with live adoption and the unchanged full final verify chain against the revised explicit benchmark categories.
  - Pre-cutover checkpoint (2026-09-11): Unused promotions and trigger binding are implemented and verified: both final focused lanes passed 973/973 (925 unit + 48 fixture), and all eight existing benchmark cases plus delivery passed, all Mac-hosted Docker Linux/amd64 advisory. The three pure-solver workloads, hashes and deterministic work match both retained Step 4/4a baselines exactly; all 16 changed/new C++ paths stayed hash-identical through the final chain. Exact commands and evidence are in `docs/reviews/2026-09-11-live-motion-prerequisite-review.md`. A source-backed review found three mandatory benchmark cases construct and step 512–2,048 bodies, conflicting with the adopted 256-body solver ceiling. Pause before production reader cutover to review the proposed explicitly categorized grid-only migration; do not raise caps, silently change workload meaning, or tick this step. Existing benchmark and historical artifacts are unchanged; no Step 16 completion commit or native certification is claimed.
  - Integration contract (2026-09-11): Follow `docs/reviews/2026-09-11-live-motion-integration-contract.md`, including unused promotion proofs and borrowed trigger facts before reader cutover. This corrects the notes below: declarations are read once at `GameSimulation::create`, the frozen world is post-intake/post-PreKernel, and production falling/race registrations remain Step 17 while Step 16 proves injected support-capable triggers. Preserve the discrete wrapper test at `physics_tests.cpp:353`; invert the live crossing expectation in `game_simulation_tests.cpp` instead. Factory/grid/commit share the reviewed solver's body-envelope guard. Sparse per-body any-touch, explicit static/hazard authoring, and full v3 publication are included. Existing objects explicitly retain closing-impact policy.
  - Supplemental verify: Before reader cutover, run both original focused lanes and benchmarks, proving unchanged old-reader behavior/prototype hashes. After integration, retain the original verify below and add `./scripts/verify-focused 'unit.application|unit.protocol|unit.runtime|unit.server|unit.controllers' && ./scripts/verify-focused 'unit.application|unit.protocol|unit.runtime|unit.server|unit.controllers' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions && ./scripts/run-linux-toolchain -- ./scripts/verify-web && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e`. Run one C++ build at a time; all host results are advisory, not native certification.
  - Contact-admission amendment (2026-09-10): This step also owns live adoption of Step 4a's touch-observation/optional-impact response input and policy-aware admission. No feature step may hide that kernel contract change. Establish a canonical committed per-object effect-policy value and its explicit authoring path, with object-instance specificity rather than only a global or archetype boolean; record defaults and migrate existing objects deliberately. Any new authoritative kind/field must include the Step 13 lifetime trait where applicable and the complete Step 16 v3 authoring/validation/publication/schema row. Preserve collision filters unless separately reviewed sensor filtering is requested. Name the exact storage/input design in the supplemental prototype review before this step starts; do not fork geometry or add a ninth mode declaration by accident.
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.gameplay|fixtures' && ./scripts/verify-focused 'unit.simulation|unit.gameplay|fixtures' linux-clang-asan-ubsan && ./scripts/run-benchmarks-linux`
  - Notes: Wire Step 4's same canonical solver and pure MotionTrigger seam into `GameSimulation` under the approved fixed-step contract, replacing phases 2 through 5 with swept broad phase, stable chronological events under the Step 2 order, body/wall responses, ~~production ground-bound support-loss termination~~ support-capable injected trigger termination (production registration is Step 17), and solver-owned actual path segments. The original three kernel seam changes remain here, alongside the Step 4a admission amendment above: `ContactRule::Response` receives the frozen post-intake/post-PreKernel world under the reviewed Step 1(a) refinement, mirroring predicates; `ContactResponse` gains the per-body motion disposition; and `GameMode::motion_triggers()` becomes the eighth declaration, read once by ~~`GameSimulationSetup::of_mode`~~ `GameSimulation::create` (the factory owns declaration evaluation) and evaluated inside the solver against the immutable world and terrain with a bounded tick-local cursor. A terminated body cannot cause later contacts. Add transactional rollback and bounded-storage/work tests. Amend in the same commit: ADR 0003 § "Canonical tick" phases 2 through 5, § "Player-pair policy", § "Wall policy", and the tunnelling consequence; ADR 0004 § "The tick" (the two-sockets paragraph now names the trigger socket) and § "Contact rules"; the seven-declarations text in `game_mode.hpp` and `game_simulation_setup.hpp`. Update intentionally changed discrete-contact, wall, and replay expectations with written rationale, including ~~the deliberate inversion at `tests/unit/simulation/physics_tests.cpp:353`~~ the live crossing inversion in `game_simulation_tests.cpp`, while preserving that pure discrete wrapper oracle unchanged; do not weaken unrelated physics invariants or hide budget failures. Compare benchmarks to Step 4's baselines.

- [x] **Step 17: Unify falling, gate chronology, and safe return**
  - Completed (2026-09-12, Mac/arm64-hosted Docker Linux/amd64 advisory): Both exact core lanes passed 1,200/1,200 (1,152 unit + 48 fixtures); both application/runtime/server/controller supplements passed 494/494. Fixed-corpus replay passed 91 executions over seven sanitizer harnesses. Full web passed 769/769 with 79 schemas/29 examples and all build checks; Chromium passed 13/13 without retries, flakes or skips after deliberately updating the standings-cell locator. The clean full benchmark passed eight categorized cases plus delivery; every retained correctness/work/workload/sample-count field agrees with Step 16, and pure-solver results still match both historical prototypes. The new ground-attachment invariant is checked separately without altering historical hashes. All 78 changed/new C++ paths passed pinned formatting; 125 implementation/input paths remained hash-identical after the final browser-only correction; git diff check passed. The review and separate evidence artifact are docs/reviews/2026-09-11-falling-race-return-review.md and docs/reviews/2026-09-12-falling-race-return-baseline.json. Execution corrected a stale Sandbox comment (free play already auto-starts), preserved N+D+1 returns and hill scoring order, tightened analytic replay tolerances, and updated required v3 fields without changing legacy wire contracts. No native/performance/release claim, push or deployment.
  - Implementation contract (2026-09-11): Follow `docs/reviews/2026-09-11-falling-race-return-contract.md`. Pin explicit player ground attachment, shared support phase policy, required Sandbox return configuration (initial 2-second tuning), application-boundary checkpoint support admission, canonical live-store safe seating, and certified normalized finish offsets under complete v3 publication. No additional kernel seam or relocation fallback.
  - Supplemental verify: `./scripts/verify-focused 'unit.application|unit.runtime|unit.server|unit.controllers' && ./scripts/verify-focused 'unit.application|unit.runtime|unit.server|unit.controllers' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e && ./scripts/run-benchmarks-linux` (retain the original gates below).
  - Source preflight (2026-09-11, before execution): The existing seam is sufficient; do not add another kernel callback or event-time implementation. Preserve `CheckpointProgressSystem`'s zero-progress initialization when replacing endpoint polling, or a racer falling before gate one cannot return through `GridSpawnPolicy`. Refresh supported/actual-radius spawn occupancy against the updated live store after each successful seat, not just once before the loop. Define startup admission for checkpoint discs that overlap void/holes so return cannot defer permanently on an impossible checkpoint. Add the ADR-required explicit Sandbox return configuration and free-play support binding; its current mode ignores configuration. ~~It remains in lobby, so a copied running-only guard disables its falling~~ — execution corrected this stale-header assumption: FreePlayObjective auto-starts through countdown into running on ticks one/two. Preserve that lifecycle and use always-active support so pre-running falls work too. Pin other modes' phase eligibility in the implementation contract. Existing crossing hazards are constructed outside the envelope: keep them floating and prove the ground-bound hazard capability with typed interior bodies, not an archetype switch that immediately despawns every crossing spawn. A narrow race event must carry certified `MotionTime` in its own payload, since the live adapter publishes effect payloads rather than outer diagnostic keys. Preserve finish-intent release as well as current-tick termination. Existing return chronology remains N + D + 1. The legacy race test is now at `race_mode_tests.cpp:185`, not the stale line below. Update inherited `physics_body.hpp` radius/bounds comments when adding its capability; the Step 16 ADR/domain descriptions already supersede those old baseline statements.
  - Verify: `./scripts/verify-focused 'unit.gameplay|unit.simulation|unit.protocol|fixtures' && ./scripts/verify-focused 'unit.gameplay|unit.simulation|unit.protocol|fixtures' linux-clang-asan-ubsan && ./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Notes: Ground attachment is a `PhysicsBody` capability beside `BoundsBehavior`; players are ground-bound, existing crossing hazards are not, and a ground-bound hazard despawns on support loss through the existing `DespawnEvent`. Register the shared support-loss trigger and race's ordered-gate trigger; support loss emits the existing `EliminationEvent` for players, and mode-owned respawn/placement supplies the consequence. The race trigger replaces `TrackBoundsSystem` and the endpoint sampling of `CheckpointProgressSystem`: it advances a bounded tick-local cursor and requests immediate finish termination, and post-kernel consumers commit its event facts. Publish the within-tick finish offset beside the finish tick in standings (this step's v3 row). One canonical supported-and-unoccupied seating query with actual body radii and written-out arithmetic replaces `point_is_occupied` (`src/simulation/spawn_seating.cpp:15`), used by spawn, checkpoint return, and hill return. Body-bound cleanup goes through Step 13. Prove cliff-before-finish ties, multiple gates in one tick bounded by the authored count, no post-death or post-finish impacts, checkpoint retention, hill return, royale elimination, ground-bound versus floating hazards, actual-radius occupancy, and zero-delay return. The intentionally changed test at `tests/unit/gameplay/race/race_mode_tests.cpp:180` carries its rationale. Keep one seating/support query and one player respawn lifecycle.

- [x] **Step 18: Implement the guarded contact composition and tap shield**
  - Completed (2026-09-12, Mac/arm64-hosted Docker Linux/amd64 advisory): Both exact core lanes passed 1,361/1,361 and both application/server/controller supplements passed 408/408. Fixed-corpus replay passed 95 executions over seven sanitizer harnesses. Full web passed 814/814 with 81 schemas/31 examples and no generation drift; Chromium passed 13/13 with no retries, skips or flakes. The clean full benchmark passed eight categorized cases plus delivery, and after normalizing JSON integer/float spelling exactly one retained field differs from Step 17's evidence across every case: the frozen deployment snapshot's provenance gained `2026-09-12_required_abilities_shield_defaults`. Every correctness record, every deterministic-work counter, the delivery trace hash, and the live royale `final_snapshot_hash` `fnv1a64:36ee7ca01d83ddb5` are unchanged under the guarded row. All 72 changed/new C++ paths passed pinned formatting, 148 implementation/input paths stayed hash-identical through the final chain, and `git diff --check` passed. Execution accepted cancellation of the perfect opening with its protection, kept `lethal_hazard` as the composition's retained diagnostic name under the declared `guarded_pair` row, took rank 10 to preserve existing ranks, added the replay `shield` verb with a new `royale-shield-parry` fixture, updated the golden royale welcome to the seven kinds royale now advertises, and fixed a GCC 13 `-O3` `-Wstringop-overflow` false positive by reserving the composition's documented three-effect ceiling rather than suppressing the warning. A pre-existing whole-tree formatting drift in sixteen untouched Step 9 files is recorded for Step 24. The review and separate evidence artifact are `docs/reviews/2026-09-12-shield-composition-review.md` and `docs/reviews/2026-09-12-shield-composition-baseline.json`. No native/performance/release claim, push or deployment.
  - Implementation contract (2026-09-12): Follow `docs/reviews/2026-09-12-shield-composition-contract.md`: one authored AbilityConfiguration, captured defender stun duration, validated body-bound Shield with cooldown-preserving cancellation, shared PreKernel AbilitySystem, generation-checked pulse admission, and one live adapter over the existing pure composition including static counterparts. Complete compact v3 publication and document send-versus-activation semantics; no generic receipt channel or new kernel seam. Controls remain Step 21.
  - Supplemental verify: `./scripts/verify-focused 'unit.application|unit.server|unit.controllers' && ./scripts/verify-focused 'unit.application|unit.server|unit.controllers' linux-clang-asan-ubsan && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e && ./scripts/run-benchmarks-linux` (retain all original gates below).
  - Owner clarification (2026-09-10): The confirmed perfect opening is a very short window immediately after activation; a qualifying incoming opponent loses current momentum and is stunned, not reflected by a special parry equation. The owner subsequently confirmed ordinary protection with reduced knockback for the remaining shield duration, without momentum cancellation or parry stun after the opening. The tested pure composition is the starting point, with its quarter-impulse target as the explicitly communicated initial tuning assumption and pair-level non-closing correction taking precedence over the exact fraction. Exact durations and fraction are tuning, not newly confirmed numbers. Cliffs bypass shield. Per-object any-touch lethal eligibility does not automatically grant a perfect parry on a stationary/tangential observation; parry requires incoming opponent motion. Preserve one pair composition and immediate termination. This clarification resolves the shield-lifecycle hold, not the separate Step 5 adoption gate.
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.protocol|unit.runtime|fixtures' && ./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.protocol|unit.runtime|fixtures' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions && ./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Notes: A `Shield` component (body-bound; activation tick with Step 14 windows for shield, perfect, and cooldown) written by a shared `kPreKernel` ability system from the new `shield` command: registry arms, wire schema, decoder, client validation, and the `v3.md` row complete in this commit; admission checks phase, body, stun, and cooldown from the approved matrix. One pair-symmetric composition response for every dynamic pair, the Step 4 function, declared above the built-in rows by every mode that fields hazards or abilities: ~~immutable pair facts read from the committed world under Step 16, then the same base equation the built-in rows select today, then defense modifications, then typed effects and motion disposition~~ — project committed Shield/Step 14 windows into Step 4's frozen guard facts, call that same committed-world composition core and promoted base helpers, and adapt its typed consequences/dispositions to the live Step 16 response. No duplicate composition or timer arithmetic. Ordinary shield retains lethal-contact protection and reduced knockback for the remaining duration. Use the tested quarter-impulse target as initial tuning, with the pair-level non-closing correction taking precedence over the exact fraction; no post-opening momentum cancellation or parry stun. Perfect shield: zero the incoming dynamic body's velocity and acceleration, emit `StunRequest`, and leave the pair non-closing so a moving defender cannot generate zero-time repeats; require actual incoming motion; static walls are never stunned; simultaneous eligible perfects stun both. Fold `lethal_hazard_contact_rule` into the composition as its unguarded-lethal branch and delete the row (`src/gameplay/shared/lethal_hazard_contact_rule.cpp`), so a dynamic pair has exactly one response path; the built-in rows remain the engine baseline reachable only by a mode declaring neither. Defense state is fixed for the tick by construction because responses read the committed world; document that bounded grace. Cliffs bypass shield. Publish shield status under v3. Tests: every timing boundary, moving defenders, mutual perfects, the grace, guarded and unguarded lethal contact, dual-shield goldens, energy behavior, and human/bot/replay symmetry.

- [x] **Step 19: Implement one-shot charge**
  - Completed (2026-09-12, Mac/arm64-hosted Docker Linux/amd64 advisory): Both exact core lanes passed 1,418/1,418 and both application/server/controller supplements passed 410/410. Fixed-corpus replay passed 99 executions over seven sanitizer harnesses. Full web passed 859/859 with 83 schemas/33 examples and no generation drift; Chromium passed 13/13 with no retries, skips or flakes. The clean full benchmark passed eight categorized cases plus delivery, and after normalizing JSON integer/float spelling exactly one retained field differs from Step 18's evidence across every case: the frozen deployment snapshot's provenance gained `2026-09-12_required_abilities_charge_tuning`. Every correctness record, every deterministic-work counter, the delivery trace hash, `royale_deployed_roster`'s `fnv1a64:36ee7ca01d83ddb5`, and `continuous_motion_charge_speed`'s retained hash are unchanged. All 56 changed/new C++ paths passed pinned formatting; 127 of the 128 tracked paths stayed hash-identical through the final chain, the exception being a mid-chain correction to `src/simulation/README.md`, which no gate reads; `git diff --check` passed. A read-only adversarial preflight rejected six of eleven drafted decisions before any source edit, three of them defects that would otherwise have shipped: reusing the magnitude clamp `normalized_thrust_intent` would have let a client author its own burst strength through direction length, so charge got a new canonical `unit_direction`; the burst does NOT decay in the checked-in configuration, where `drag_per_second=0` holds in seventeen of eighteen replay fixtures, so the authored envelope rather than drag bounds repeated charges; and a throw inside a kPreKernel system stops the runtime worker permanently, so the envelope is checked in raw doubles before any Vector2 exists. The review and separate evidence artifact are `docs/reviews/2026-09-12-charge-review.md` and `docs/reviews/2026-09-12-charge-baseline.json`. Two findings are carried to Step 24: the pre-existing whole-tree formatting drift in sixteen untouched Step 9 files, and the curated `kReplayMaps` list that names neither new replay fixture. No native/performance/release claim, push or deployment.
  - Implementation contract (2026-09-12): Follow `docs/reviews/2026-09-12-charge-contract.md`, written after a read-only preflight against Step 18 and adversarially reviewed before any source edit. It corrects six drafted decisions: `charge_cooldown_seconds` must be validated positive because the shield's zero-cooldown exemption depends on a second gate charge does not have; `charge_speed_fraction` and a required `charge_safety_envelope_speed` join the same `[abilities]` owner under one cross-key rule that keeps a charge from rest admissible at any tuned ceiling; the payload's `input_generation` stays optional like `set_thrust`'s rather than required-and-nullable like `shield`'s, per the tree's own recorded discriminator; charge needs a NEW canonical `unit_direction` helper because `normalized_thrust_intent` is a magnitude clamp that would let a client author its own burst strength; the envelope must be checked in raw doubles before any Vector2 is built, because a throw inside a kPreKernel system stops the runtime worker permanently; and the burst does NOT decay in the checked-in configuration, where `drag_per_second=0`, so the envelope rather than drag is what bounds repeated charges. Every refusal stays a silent no-op with no receipt. No new system, socket, event root or mode declaration.
  - Verify: `./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.protocol|unit.runtime|fixtures' && ./scripts/verify-focused 'unit.simulation|unit.gameplay|unit.protocol|unit.runtime|fixtures' linux-clang-asan-ubsan && ./scripts/run-benchmarks-linux && ./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Notes: The `charge(direction)` command with its registry arms, schema, decoder, client validation, and `v3.md` row complete in this commit. Admission: direction normalized and validated once, zero rejected; cooldown as a Step 14 window; unavailable while shield is active; refused explicitly, never converted, when the safety envelope would be exceeded. Effect: an instantaneous additive burst of three quarters of the current normal ceiling from Step 10's tuning, decaying under drag, never a teleport, never invulnerability, never erasing lateral velocity, never crossing a hole; collisions are ordinary through the Step 18 composition and Step 16 motion. Conflict rule: eligibility first; if shield and charge are both eligible in one tick, shield wins and charge cooldown is not consumed; an ineligible shield pulse does not suppress an eligible charge. Publish charge status under v3. Tests: direction and cooldown admission, envelope refusal, conflict priority, no tunnel through bodies or holes, bot and replay symmetry, and the charge benchmark against Step 4's baseline.

- [x] **Step 20: Render cliffs and combat feedback**
  - Completed (2026-09-12, Mac/arm64-hosted Docker Linux/amd64 advisory): The full web gate passed 940/940 with prettier, generation-drift, example validation, strict typechecking, lint and the production build, and `git diff --check` passed. No schema, encoder, component, server or wire change: the 83 schemas and 33 examples are Step 19's, unchanged. A read-only adversarial preflight rejected seven of eight drafted decisions and found four blockers before any source edit. No renderer could see the snapshot tick, so `EntityRenderFrame` gained it and that is the step's one client-contract change. The cliff is the boundary of positive ground minus holes, so corridor EDGES are the only real cliffs on any runnable map -- no shipped map or fixture authors a hole -- while the arena border is not a cliff and keeps its existing stroke. Per-body void feedback was replaced by a terrain-side treatment: the client cannot decide 'this centre is over void' without a second support predicate, and a ground-bound body loses its body on the tick it loses support, so the state never reaches a snapshot. Shield draws three states, the majority one being no protection with a live cooldown; stun draws too; charge stays deliberately undrawn with its cooldown in the HUD. One `status` layer was added because the sort falls back to kind spelling. Three test expectations moved as mechanical consequences of the new terrain calls, each with written rationale. Two limits are recorded rather than papered over: a perfect mark is a state mark whose visibility depends on the configured snapshot cadence, and the readout states remaining time but never availability, since charge readiness depends on the off-wire safety envelope and the shield's authored length is never published. No ability mark is reachable in the live browser until Step 21 wires a sender. The gate does not look at pixels, so the real client was run against a real server through the pinned e2e harness and both ground kinds were captured and sent to the owner for the judgement no test can make; the temporary capture spec was deleted and the tree carries no trace of it. The review is `docs/reviews/2026-09-12-cliff-and-combat-rendering-review.md`. Colour and rim-width values are initial legibility choices, not owner-selected art direction. No native/performance/release claim, push or deployment.
  - Implementation contract (2026-09-12): Follow `docs/reviews/2026-09-12-cliff-and-combat-rendering-contract.md`, written after a read-only preflight against Step 19 and adversarially reviewed before any source edit. It corrects seven of eight drafted decisions and four blockers: no renderer can see the snapshot tick today, so `EntityRenderFrame` gains `tickSequence` and that is the step's one client-contract change; the cliff is the boundary of positive ground minus holes, so corridor EDGES are the only real cliffs on any runnable map while the arena border is not a cliff at all and keeps its existing stroke; void feedback becomes terrain-side rather than per-body, because deciding 'this centre is over void' on the client would be a second support predicate and because a ground-bound body loses its body on the tick it loses support, so the state never appears in a snapshot; shield draws THREE states, the majority one being no protection with a live cooldown; charge stays non-visual with its forward-referencing reason rewritten and its cooldown moved to the HUD; one `status` layer is added because the sort falls back to kind spelling; and the readout states remaining time but never availability, since charge readiness depends on the safety envelope and the shield's authored length is not on the wire. No schema, encoder, component or server change. The gate does not look at pixels; completion must say so and put images in front of the owner.
  - Verify: `./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Notes: Extend Step 7's terrain layer with clear cliff rims and void feedback beneath entities using the existing single camera projection; race retains gates and objectives, not a second road. Add registry-driven shield, perfect, and stun visuals and screen-space cooldown/status feedback. Test all world layers under translation, manual and follow, edge view, fractional DPR, and resize. Use the shared geometry goldens and strict v3 mutations; never clip before validation or hide unknown components.

- [x] **Step 21: Wire accessible ability controls**
  - Completed (2026-09-12, Mac/arm64-hosted Docker Linux/amd64 advisory): The full web gate passed 986/986 with prettier, generation-drift, example validation, strict typechecking, lint and the production build; the production browser gate passed 13/13 with no retries, skips or flakes (run because this step edits an e2e spec, not because the step requires it); `git diff --check` passed. No schema, encoder, component or server change. A read-only adversarial preflight rejected six of nine drafted decisions and found five blockers before any source edit: unthrottled ability keys would have DISCONNECTED players, because the per-session bucket is 30 burst / 20 refill, exhaustion closes the socket, held thrust already runs at the full refill rate, and the 50 ms floor lives in the thrust flush rather than in the sender; the shield payload requires an explicit `null` where charge's omits the member, so copying the thrust encoder would have failed silently for every never-stunned player; reusing the thrust flush would swallow a second identical pulse and delay one against an 80 ms perfect opening; a focused ability button turns Space into the ability key and held Enter is a second repeat source; and remembered aim was wiped by every stun, which is not the contracted discard set. Shift was rejected with reasons -- the go-key guard deliberately does not filter `shiftKey`, so a bare-Shift shield fires on every Shift+Tab, five presses is the Sticky Keys gesture, and a modifier cannot demonstrate the key-repeat rule -- and the bindings came from the pool ADR 0008 reserved when Step 11a retired directional steering: `KeyS` shield, `KeyD` charge, with the four arrow codes still reserved and still asserted inert. Shield and stun were captured live in a browser for the first time, including the three-state renderer showing nothing while a cooldown outlives its protection. One limitation is recorded rather than resolved and belongs to the owner: charge takes its direction from a cursor aim, so it is mouse-dependent and a touch, pen or keyboard-only player cannot use it, while shield is fully usable without a pointer. The review is `docs/reviews/2026-09-12-ability-controls-review.md`. Bindings are initial choices, reversible in one constant. No native/performance/release claim, push or deployment.
  - Implementation contract (2026-09-12): Follow `docs/reviews/2026-09-12-ability-controls-contract.md`, written after a read-only preflight against Step 20 and adversarially reviewed before any source edit. It corrects six of nine drafted decisions and five blockers. Bindings come from the pool ADR 0008 already reserved (the former direction keys), so shield is `KeyS` and charge is `KeyD`; Shift is rejected with reasons, because the go-key guard deliberately does not filter `shiftKey`, a bare-Shift shield fires on every Shift+Tab, five presses is the Sticky Keys gesture, and a modifier cannot demonstrate the key-repeat rule the ADR requires. The ability path reuses `sendCommand` but NOT the thrust path's `flush`, whose change-only gate would swallow a second identical pulse and whose 50 ms timer would delay one against an 80 ms perfect opening. A rate discipline is mandatory: the per-session bucket is 30 burst / 20 refill and exhaustion CLOSES the socket, held thrust already runs at the full refill rate, and the mailbox coalesces same-tick pulses anyway. Shield's payload needs an explicit `null` where charge's omits the member, and getting that wrong fails silently for every player who has never been stunned. Remembered aim must survive a stun, which it does not today. Each ability needs a held latch cleared on keyup and blur, buttons are a second repeat source through held Enter, and a focused button turns Space into the ability key. Availability adds the per-kind advertised capability, active shield protection blocking charge, and a live published cooldown, while still never claiming readiness. Charge is mouse-dependent and that accessibility gap goes to the owner named, not shipped quietly.
  - Mouse amendment (2026-09-10): Reuse Step 11a's canonical cursor aim/last nonzero direction and held-Space activation; the earlier proposed Space-for-charge binding is superseded, and specific charge/shield keys remain to be assigned from the available keys. Do not add another pointer listener, normalization rule, or sender for abilities. Apply fresh-activation, stun, cancellation, typing, ownership, and body-replacement rules to mouse and keyboard alike; camera interaction must not activate movement or abilities.
  - Verify: `./scripts/run-linux-toolchain -- ./scripts/verify-web`
  - Notes: Extend canonical input handling with ability bindings reviewed against the confirmed Space-for-go control and ordinary action buttons, respecting ownership and capabilities, cooldown and stun, aim direction or last nonzero steering, key repeat, typing, blur, disconnect, and a replacement body; Charge is unavailable with an explanation before any aim exists. Keep camera controls independent. Tests: no queued activation on stun expiry, no ability or thrust while editing settings, no ability from camera interaction, and unchanged steering and camera bindings.

- [x] **Step 22a: Implement the tactical decision pipeline**
  - Completed (2026-09-12, Mac/arm64-hosted Docker Linux/amd64 advisory): Both lanes of the corrected filter passed 784/784, the fixed corpus passed 99 executions over seven harnesses, full web passed 986/986 and Chromium passed 13/13 with no retries, skips or flakes. All 16 changed C++ paths passed pinned formatting, all 28 tracked paths stayed hash-identical through the chain, and `git diff --check` passed. The weighted selection stage was the whole point: before it, selection was nearest-distance-first with no profile input, so two profiles differing only in numbers picked the same candidate and the differentiation proof could not have meant anything. Weights are one key per kind so an unauthored kind is the parser's own KEY_MISSING, and the key-name and lookup switches carry no `default` so a fifth kind is a build failure rather than a silently unweighted one. All-zero weights are a named rejection because zero is what C++ fills an omitted aggregate initializer with. Risk tolerance can only scale a penalty away, never add score to a dangerous candidate, which is why it is not Step 22b's aggression knob renamed. Three stale profile call sites outside every worker's list were repaired, one of them C++ rather than a `.cfg` and therefore invisible to the parser's family closure; and the inert-combat fuzz seed needed the new required keys after all, because without them it rejects for KEY_MISSING instead of KEY_UNKNOWN on `aggression` and stops testing the rule its name states. A CTest discovery permission fault on the sanitizer preset was a stale generated `*_tests.cmake`, cleared and re-run, not a code failure. The review is `docs/reviews/2026-09-12-tactical-pipeline-review.md`. No native/performance/release claim, push or deployment.
  - Split from Step 22 on 2026-09-12 at the seam the preflight recommended, which is "what the observation can already answer". This half needs no wire change and is buildable today: the profile-weighted utility selection and objective weights ADR 0008 already names, deterministic reason codes, hysteresis and target-loss handling, escape screening, hill intercept and hold from the published `hill_motion`, and bounded candidate/prediction accounting with the 32-candidate throw resolved. Road recovery and gate advance already exist from Step 15.
  - Implementation contract (2026-09-12): Follow `docs/reviews/2026-09-12-tactical-pipeline-contract.md`.
  - Verify: `./scripts/verify-focused 'unit.controllers|unit.gameplay|unit.application|fixtures' && ./scripts/verify-focused 'unit.controllers|unit.gameplay|unit.application|fixtures' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions && ./scripts/run-linux-toolchain -- ./scripts/verify-web && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e` — the preflight established that the original line omitted every gate a `[bot_profile]` key change necessarily breaks, and included a benchmark that cannot see a controller.
  - Notes: Profiles differ only in numbers, never in code paths. The selection stage is the point of the step: without it two profiles pick the same candidate and no differentiation proof means anything. The differentiation test must hold the profile NAME constant, because `tactical_seed_for` mixes the name and would otherwise make the proof vacuous. Reason codes are a closed enum, not free text.

- [x] **Step 22b: Implement shoving and the combat abilities**
  - Completed (2026-09-12, Mac/arm64-hosted Docker Linux/amd64 advisory): Both lanes passed 799/799, the fixed corpus passed 99 executions over seven harnesses, full web passed 986/986 and Chromium passed 13/13 with no retries, skips or flakes. All 18 changed C++ paths passed pinned formatting, all 29 changed tracked paths are hash-identical between the final gate run and the commit, and `git diff --check` passed. Both C++ lanes AND the browser gate were re-run after `clang-format -i` touched source, because the verified tree must be the committed tree. **This is the step that makes Step 22a's weights matter**: every shipped mode yields at most one candidate -- one Hill, one Zone, and race returns one gate or one recovery -- so with a one-element span selection returned index 0 regardless of score and every weight was a common factor changing no production outcome, proven only against synthetic sets until an opponent-derived provider existed. Two adversarial preflight rounds ran before any source edit; the first rejected all eight drafted decisions and the second, run against the corrections, caught four defects that would otherwise have shipped: a zero-weight veto that reads out of bounds on a non-empty candidate set (corrected by the Step 22c preflight: there is no crash today because the selector short-circuits and assigns index 0, and the read would have been SILENT rather than a sanitizer abort, since the candidate vector is over-reserved), an unconditional arrival brake whose 0/0 at rest throws and leaves a bot PERMANENTLY inert in the terminal state of every successful hill capture, an ability gate on the seek draw that would stop a bot on the hill ever shielding, and a charge screen testing `.has_value()` that would have vetoed every shove the step exists to enable. The candidate target is the safe-side standing point and never the opponent, because the opponent as target makes `escape_blocked` true by construction and forces one `risk_tolerance` to do two conflicting jobs. There is no `aggression` key: its preference half IS the fifth objective weight and its danger-appetite half is what `risk_tolerance` was built one-signed to forbid, so both pinned rejection rows stayed verbatim. Three already-shipped errors were corrected: two READMEs asserted a fuzz seed was "deliberately not migrated" when `5b400cd` migrated it, that seed is not an executable guard at all because the fuzzer swallows every typed error, and ADR 0008's claim that the perfect opening's length is never published is false since every Shield value is published verbatim. Recorded rather than repaired: a hosted bot pays no command-rate cost where a human's socket would close, abilities are derated to roughly a 20% duty cycle by the reaction gate, no gate exercises combat prediction at the deployed drag, and doubled screening work is unobserved by the gate. One file no worker owned -- `controllers_validation_error.hpp` -- was a build break two workers reported before root reached it. The review is `docs/reviews/2026-09-12-tactical-combat-review.md`. No native/performance/release claim, push or deployment.
  - Owner decision (2026-09-12): **authored caution, not derived physics.** The owner chose option 2 of the two the preflight put forward: bots screen a ray of profile-authored length and raise shields on a profile-authored anticipation window, and NO ability tuning or `drag_per_second` is published on v3 for bot use. Controllers stay where ADR 0002 puts them, seeing only what a browser sees. The step must state plainly, in code and in the ADR, that these are authored numbers and not predictions of the kernel, and must accept the consequences rather than disguise them: a bot will sometimes charge into a wall or a hazard it could not predict, and at 20 snapshots per second against a 32-tick opening it will rarely land a perfect parry. Reliable bot parries would need a hosted-bot decision-cadence change, which remains a separate unraised decision. Findings are in `docs/reviews/2026-09-12-tactical-combat-preflight.md`.
  - Split again on 2026-09-12, at the seam a second read-only preflight round found. The four named personalities move to Step 22c. Root first argued against this split on two grounds and both failed against source: the Notes line below is not Step 22b's -- `git show 5b400cd` shows it as an unchanged context line, the original Step 22 Notes carried under a new heading and never re-scoped, already stale about two of its six clauses on 22a's own account -- and "hold" already has a fixed meaning in this library, stated twice ("Intercept and hold are the same computation"; "Inside the hill the heading shrinks, which is what 'hold' means here"), which 22a shipped. Reading "hold" as "brake" substituted a new meaning for a defined word. Three of the four names need mechanism rather than numbers, and each mechanism carries a defect the preflight found and did not solve: Keeper's brake is 0/0 at rest, which throws, which leaves a bot that arrives at the hill PERMANENTLY inert; Opportunist's "prefer exposed" provably cannot be a filter, because one kind makes the weight a common factor that cannot reorder candidates; and Cautious Racer's "avoid expensive fights" needs a zero weight to veto, which as drafted reads out of bounds on a non-empty candidate set -- and the Step 22c preflight then found that read would have been SILENT rather than a sanitizer abort, because the candidate vector is over-reserved. Each is a Step 22c deliverable with its own design decision, not a number.
  - Implementation contract (2026-09-12): Follow `docs/reviews/2026-09-12-tactical-combat-contract.md`, written after two read-only preflight rounds. The first rejected all eight drafted decisions; the second, run against the corrections, found a crash, a permanently-inert-bot defect and a contradiction between two corrections. This step is what survived: the fifth `kShoveSetup` objective kind and its weight, the opponent-derived shove provider running ALONGSIDE the mode provider, per-provider candidate budgets with a stable nearest-N filter and a re-derived ceiling of 96, `request_shield`/`request_charge` mirroring `request_thrust`'s suppression, an authored charge screen as a fraction of the observed arena diagonal, and an authored shield anticipation window with a bounded linear closing test over published state. Three new keys and NO `aggression` key: its preference half IS the shove weight under 22a's one-key-per-kind rule and its danger-appetite half is what `risk_tolerance`'s one-signedness was built to forbid, so both pinned rejection rows stay verbatim.
  - Verify: `./scripts/verify-focused 'unit.controllers|unit.gameplay|unit.application|fixtures' && ./scripts/verify-focused 'unit.controllers|unit.gameplay|unit.application|fixtures' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions && ./scripts/run-linux-toolchain -- ./scripts/verify-web && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e && git diff --check` -- the original line repeated verbatim the omission the first preflight already condemned. `run-benchmarks-linux` is dropped and that is now verified rather than inherited: no file under `benchmarks/` contains a `[bot_profile]` section and no replay fixture seats a tactical bot.
  - Notes: This is the step that makes 22a's selection stage live. Every shipped mode yields at most one candidate today -- one Hill, one Zone, and race returns one gate or one recovery -- so with a one-element span every objective weight is a common factor that changes no outcome in production, and the opponent-derived provider is the first thing that puts two kinds in one set. A shove candidate's target is the safe-side standing point S, never the opponent: with the opponent as target, `escape_blocked` is true for every shove by construction and one `risk_tolerance` is forced to do two conflicting jobs. The charge screen compares exit time to contact time, never `.has_value()`, which would veto every shove the step exists to enable. The alignment gate compares against the resultant, because the burst is additive. Prove both halves of the charge refusal, since a bot that never charges would pass the refusal test alone.

- [x] **Step 22c: Ship the four named tactical personalities**
  - Completed (2026-09-12, Mac/arm64-hosted Docker Linux/amd64 advisory): Both lanes passed 810/810, the fixed corpus passed 99 executions over seven harnesses, full web passed 986/986 and Chromium passed 13/13 with no retries, skips or flakes. All 17 changed C++ paths passed pinned formatting and `git diff --check` passed; both C++ lanes and the browser gate were re-run after formatting touched source. **Two headline proofs pass**: four authored personalities decide four different objectives on ONE identical observation with the profile name held constant, which is the differentiation claim the plan has asked for since Step 15 and which could not have been made before 22b; and the shipped configuration parses with exactly five authored profiles, which is **the first time anything in this repository has parsed `config/blob-royale.cfg`**. That gate hole was real: `deployment_fixture_tests.cpp` loads `deploy/ubuntu-pc`, so 22b's three new keys and the `steady` section since Step 15 shipped with zero parse coverage while the 22b contract asserted the opposite; root verified by hand after 22b that the file loads and the server reaches `running`, and this step turns that one-off into a gate. The general zero-weight veto was cancelled: there was never a crash, and 22b's review was wrong to call the hypothetical read a sanitizer abort -- the candidate vector is over-reserved, so it would have been SILENT corruption followed by an arbitrary `key.kind` byte indexing a five-element array. Both that review and this plan are corrected. What landed instead is `tactical_select_candidate` returning `std::optional` and a zero shove weight skipping the provider. The brake is deadbeat and division-free, so there is no 0/0 and no deadband -- which matters because a NaN there leaves a bot PERMANENTLY inert in the terminal state of every successful hill capture -- and it cannot overshoot, so it is stable at every drag without reading drag. Root's drafted `min(1, |v_rel|/normal_top_speed)` was rejected as three orders of magnitude too small at the fixtures' published ceiling. Exposure is booleans with no division, folded multiplicatively with a `{1.0}` default the compiler cannot catch. `road_caution_fraction` is strictly positive alone among this family, because zero inverts the recovery test rather than disabling it. Three ADR clauses are recorded as deferred rather than amended away: Keeper's stable interior, Bully's recovery path, and that nothing makes a Bully shove with ordinary thrust. The review is `docs/reviews/2026-09-12-tactical-personalities-review.md`. No native/performance/release claim, push or deployment.
  - Split from Step 22b on 2026-09-12 (see that step). Keeper, Bully, Opportunist and Cautious Racer, as profiles of the pipeline and never as code paths. Bully is numbers alone over 22b's behaviours; the other three each need one shared, value-keyed mechanism first, none of which may branch on a profile's identity: a brake law with a zero-velocity guard, a magnitude law and a deadband, whose stability depends on `drag_per_second` -- the one number the bot may not read; a per-candidate exposure quality and a score term for it, from the published `Stun`, `Shield`, `Charge`, `ZoneExposure` and `HillPresence` windows; and a zero-weight veto with an explicit all-vetoed branch, its own reason code and a stated fallback, because `tactical_select_candidate` returns `candidates.size()` for "no winner" and its one caller indexes without a size check. Also promotes the hardcoded `kDefaultRacerCautionFraction` to a per-profile key, which the diagnostic `RacerController` already authors, and adds a minimum-opening threshold, which additionally buys "abandon low-value fights" free through the verified `kLost` path.
  - Implementation contract (2026-09-12): Follow `docs/reviews/2026-09-12-tactical-personalities-contract.md`, written after a read-only preflight that corrected all nine drafted decisions and replaced root's central mechanism with a smaller one. **The general zero-weight veto is cancelled.** There is no crash today -- `tactical_select_candidate` short-circuits and assigns index 0 on the first iteration, so its sentinel is unreachable for a non-empty set -- and had a veto introduced one it would have been SILENT rather than a sanitizer abort, because the candidate vector is over-reserved so index `size()` lands inside the live allocation. Step 22b's review and completion bullet both say "sanitizer abort"; the contract corrects that. What lands instead is two smaller things: `tactical_select_candidate` returns `std::optional<std::size_t>` so the compiler closes the hole at every call site, and a zero `objective_weight_shove_setup` skips the shove provider for that profile, which needs no reason code, no fallback branch and no change to what a zero weight means for the mode kinds. Cautious Racer authors a deliberately non-zero shove weight, because its clause is "avoid EXPENSIVE fights" and `minimum_opening` is what makes a fight cheap.
  - Brake law (2026-09-12): deadbeat and division-free -- `clamp_componentwise(-v_relative / (published_acceleration * hold_seconds) * arrival_brake_fraction)`. No division by `|v|`, so no 0/0 and no NaN and no deadband; it cannot overshoot, so it is stable at every drag without reading drag, since drag only makes it undershoot and undershoot self-corrects. Root's drafted `min(1, |v_rel| / normal_top_speed)` is rejected: the reachable ceiling is `min(V, A/D)` with D unpublished, so at the e2e fixtures' published ceiling of 10000 with drag 40 it yields a brake three orders of magnitude too small -- inert in exactly the configuration whose instability motivated it. kHill only, and re-justified: a kZone candidate's arrival radius is the zone's own radius and the full zone radius is the arena half-diagonal, so a zone brake is a permanent parking brake on every bot in the checked-in default configuration.
  - Gate hole to close here (2026-09-12): **nothing in the tree parses `config/blob-royale.cfg`.** `deployment_fixture_tests.cpp` loads `deploy/ubuntu-pc`, and the shipped configuration is referenced only by `scripts/verify-linux` (Step 24's) and `scripts/assemble-release-linux`. So this step's four profiles, Step 22b's three new keys, and the `steady` section since Step 15 all ship with zero automated parse coverage, and the Step 22b contract asserts the opposite. Root verified by hand after 22b that the shipped configuration loads and the server reaches `running`, so nothing is broken -- but that is a one-off, not a gate. Add a `fixtures`-lane case and correct the 22b contract sentence.
  - Verify: `./scripts/verify-focused 'unit.controllers|unit.gameplay|unit.application|fixtures' && ./scripts/verify-focused 'unit.controllers|unit.gameplay|unit.application|fixtures' linux-clang-asan-ubsan && ./scripts/verify-fuzz-regressions && ./scripts/run-linux-toolchain -- ./scripts/verify-web && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e && git diff --check`
  - Notes: Prove profile differentiation on identical observations with the profile NAME held constant, because `tactical_seed_for` mixes the name and would make the proof vacuous. The four ship in `config/blob-royale.cfg` only: the deployment file carries no `[bot_profile]` section at all, and the tactical-profiles browser spec asserts the exact published list from its OWN fixture, which must not gain them. Amend ADR 0008 for any clause the built behaviour does not match -- "prefer distracted targets" in particular, since "distracted" is not observable and "exposed" is.

- [x] **Step 23: Prove cross-mode production behavior**
  - Completed (2026-09-12, advisory): Both exact C++ lanes passed 69/69, full web passed 986/986 and Chromium passed all 24 scenarios without retries, skips or flakes. Five new exact replays include 100 fresh snapshot sequences each; eleven new browser cases preserve the original thirteen. The review and input-hash artifact are `docs/reviews/2026-09-12-cross-mode-production-review.md` and `2026-09-12-cross-mode-production-baseline.json`. The review records the 512-to-256 backpressure migration, two corrected terrain-fixture assumptions, recursive report traversal, and the preserved host-emulator crash evidence. No production behavior changed. Arrival-contract correction and native release evidence remain Step 24 work.
  - Gate repair (2026-09-12): The first full `integration|fixtures` run exposed the omitted Step 16 backpressure-fixture migration: 512 authored bodies exceed the accepted live solver limit of 256, so startup fails before any soak assertion runs. Migrate that fixture and its exact expected payload count to 256. Preserve 60 Hz publication, the 1,024-byte unread receive buffer, twelve-second observation, mandatory actual 1013 slow-consumer close, healthy-peer continuity/readiness, and shutdown assertions. Do not raise solver limits or lengthen deadlines. See the Step 23 contract for the evidence boundary.
  - Implementation contract (2026-09-12): Follow `docs/reviews/2026-09-12-cross-mode-production-contract.md`, after read-only testineer, replay/integration, and combat preflights. Pair exact committed-tick replay assertions with real-browser observations; 20 Hz publication cannot certify unobserved 400 Hz chronology. New broad-window browser fixtures are observation tuning only. Late shield still means protected ordinary contact before expiry. Add real combat at deployed drag 2.0 and preserve all thirteen existing browser cases. No production behavior or replay-grammar change is included.
  - Mouse/shield amendment (2026-09-10): Carry the Step 11a real-browser direction-only/release/camera-isolation scenario through live room tuning and stun. Assert pointer distance does not become acceleration strength and held input does not automatically resume after stun. Ordinary/late-shield scenarios retain contact protection and reduced knockback until shield expiry, without the perfect opening's momentum cancellation or parry stun; the quarter-impulse target is initial tuning subject to non-closing composition.
  - Verify: `./scripts/verify-focused 'integration|fixtures' && ./scripts/verify-focused 'integration|fixtures' linux-clang-asan-ubsan && ./scripts/run-linux-toolchain -- ./scripts/verify-web && ./scripts/run-linux-toolchain -- ./scripts/verify-browser-e2e`
  - Specialist: testineer
  - Notes: Preserve and migrate all existing browser scenarios deliberately. Add real-session large-map roaming hill, hole fall/return, charge collision/no-gap-jump, race chronological gates/fall/finish, ordinary/perfect/late shield, bounceable-hazard stun, and profile selection/different behavior. In a running match, prove two clients receive the same live tuning values/revision/effective tick, held steering adopts the update, and another room remains unchanged. Observe actual published state/canvas/commands, not private React seams. Include keyboard/camera interaction and stale-client failure. No retries/skips, mocked production transport, endpoint-only high-speed shortcuts, or timings inferred from arbitrary sleeps.

- [ ] **Step 24: Review, run native gates, and hand off**
  - Independent source review (2026-09-12): `docs/reviews/2026-09-12-release-review.md` records a concrete counterexample to the arrival brake's no-overshoot guarantee at deployed drag and at nondivisible reaction/observation cadence. The owner selected current controls with explicitly accepted overshoot; follow `docs/reviews/2026-09-12-arrival-brake-contract.md` and add production-loop tests before closing this finding. The bound requires established cadence and excludes the first zero-delay decision, irregular delivery, moving hills, and live tuning changes. The shared ownership/authority/chronology inspection found no additional blocker in its reviewed paths. Root confirmed the sixteen-file formatting drift; two shipped-profile comments also overstate explicitly deferred behavior. The separate human agenda is `docs/reviews/2026-09-12-combat-balance-playtest-agenda.md`. No native release gate or performance certification has occurred.
  - Carried protocol review item (2026-09-11): Review the inherited browser numeric-token rounding limitation recorded in `docs/reviews/2026-09-11-random-streams-cutover-review.md`. Parsed-value schema tests and exact C++ integer emission do not establish lossless lexical validation of arbitrary decimal JSON; do not claim that stronger conformance without its own boundary decision and proof.
  - Verify: `./scripts/verify-linux release && ./scripts/run-benchmarks-linux && git diff --check`
  - Specialist: proofreader
  - Notes: Independent review of event precedence, DRY boundaries, input/room authority, wire limits, lifecycle cleanup, and plan fidelity, including that no road mirror survives, that every event time comes from the Step 2 module, that every ability window is the Step 14 value, that every body-bound kind declares the Step 13 trait, and that movement defaults have one authoring home; resolve findings before the final native Linux matrix. Record exact counts/hashes and changed fixture reasons, align ADRs/domain docs/user controls, and commit the verified implementation locally. Native evidence is required for performance/release claims; Mac results alone cannot tick that claim. No push/deployment. Leave a separate concrete human balance-playtest agenda rather than claiming fun/fairness from automated tests.

## Done criteria

All approved steps and native verification pass with no skipped/flaky/retried acceptance tests.
One geometry owner, one motion solver, one root module and one event order, one shared ability/status
implementation on one tick-window value, one body-bound component lifetime, one authoritative room
tuning value with one authoring home, and one tactical algorithm with configurable personalities
serve the modes. No derived road mirror remains and no geometry exists in two places.
Charge cannot tunnel through bodies/holes; death prevents later impact/progress; perfect shield
has tested timing, pair symmetry, and hazard behavior. Two clients agree and rooms stay isolated.
All docs and exact evidence are committed locally; pushing/deployment and human/live race playtests
remain separate authorization and verification tasks.

This plan is a proposal, not permission to improvise past the prototype gate. If that gate changes
the motion model, amend the dependent steps and ADR before proceeding. Do not turn an unproven
physics assumption into fifteen checked boxes.

## Superseded sequence (before the 2026-09-10 design review)

The original sixteen steps are struck through here with their new homes, so the renumbering is
visible without git history; the file was untracked when the review amended it.

- ~~Step 2: Prove continuous motion and contact composition~~ now Step 4, after the new Step 2 root module and Step 3 terrain queries it consumes.
- ~~Step 3: Accept the physics prototype boundary~~ now Step 5, with the gate rule.
- ~~Step 4: Cut over terrain ownership and session v3~~ split: terrain authoring into Step 3; race binding into Step 6; v3 transport, snapshot construction, and the browser reader into Step 7; the racer reader and old-field removal into Step 8. Its reserved schema vocabulary is replaced by the wire rule.
- ~~Step 5: Complete shared terrain queries and validation~~ now Step 3, before any consumer; checkpoint validation to Step 6 and roam validation to Step 12.
- ~~Step 6: Adopt the reviewed continuous motion kernel~~ now Step 16, naming the three kernel seam changes and the ADR paragraphs they amend.
- ~~Step 7: Unify falling, gate chronology, and safe return~~ now Step 17, with the protocol lane in its verify and cleanup through the Step 13 trait.
- ~~Step 8: Add authoritative movement tuning~~ now Step 10, with one shared authoring section and intent persistence named.
- ~~Step 9: Implement deterministic random hill roaming~~ now Step 12; its random streams are Step 9 on their own.
- ~~Step 10: Implement shared charge, shield, and stun~~ split into Step 14 (status windows, stun, input lock; gate-independent), Step 18 (composition and shield), and Step 19 (charge).
- ~~Step 11: Render shared terrain and combat feedback~~ now Step 20; the terrain base layer lands with Step 7.
- ~~Step 12: Wire accessible ability and tuning controls~~ split into Step 11 (tuning controls; gate-independent) and Step 21 (ability controls).
- ~~Step 13: Add tactical profiles and decision infrastructure~~ now Step 15, in Phase B.
- ~~Step 14: Implement shoving and defensive tactical choices~~ now Step 22.
- ~~Step 15: Prove cross-mode production behavior~~ now Step 23.
- ~~Step 16: Review, run native gates, and hand off~~ now Step 24.

**Amended 2026-09-10 (planning review):** Add a pure chronological motion-trigger seam for immediate finish termination, require non-closing responses for ordinary as well as perfect shields, and make terrain ownership/current reader migration part of the atomic v3 cutover. Gate command advertisement on complete authoritative behavior. No implementation or owner approval is implied.

**Amended 2026-09-10 (owner clarification):** Record confirmed adjustable numeric acceleration/top-speed controls and live room-wide application. Require atomic server-tick updates, synchronized authoritative values/revision/effective tick, held-intent recomputation, and running-match two-client/two-room verification. Other design proposals and the prototype gate remain pending; implementation has not started.

**Amended 2026-09-10 (design review):** Renumbered into three phases with a gate rule, so gate-independent work can proceed while Step 5 awaits the owner and Phase C stays provisional. The single-commit terrain cutover became expand and contract (Steps 6 through 8) behind the pure terrain step (Step 3) and a shared swept-geometry root module with one event order (Step 2), because the one-commit checkpoint could not be reviewed or bisected and because contacts and terrain triggers must derive tied times from one arithmetic. The kernel seam changes the abilities require, committed-world access for responses, a per-body motion disposition, and a trigger declaration, are named in Step 16 and proven in Step 4 instead of arriving inside a feature step. Named random streams (Step 9) and the body-bound component lifetime (Step 13) became explicit steps because each changes world state or the registry. Movement tuning takes one shared authoring section and intent persistence (Step 10). The abilities step split by mechanic (Steps 14, 18, 19) and the controls step by gate dependency (Steps 11, 21). Execution constraints restate the previous plan's baseline, two-lane, one-commit, and pure-move rules and add wire, kernel-seam, ADR-lockstep, body-bound, timer, and root rules. Four owner decisions were added to Step 1; ADR 0008 does not yet record them. No implementation or owner approval is implied.

**Amended 2026-09-10 (Step 3 execution):** Correct the nonexistent shared map section-family API, the contradictory solid/corridor migration wording, and the missing production-map fuzz harness with visible replacements in Step 3. Clarify point recovery versus radius-aware clearance; promote the unchanged bounds value to break the map/terrain dependency cycle. The derived boundary cache is internal to the one immutable terrain value. Analytic boundary selection precedes a bounded supported-side binary64 witness correction, documented in ADR 0008; this is not time snapping or exact-real interval certification. No live solver, contact signature, ability registry, protocol, race/controller reader, or accepted expectation changes are authorized by this amendment.

**Amended 2026-09-10 (Step 4 execution):** Correct the direct-equation-reuse and exact-live-signature clauses before implementation: legacy solvers re-detect contact, while committed-world responses and Shield/Stun event types land later. Use a two-lane-proven pure arithmetic promotion and one permanent composition core over frozen facts; Step 18 adds its live adapter. The canonical prototype remains unwired. Fixed motion epochs, typed bounded trigger cursors, same-time certificates, closing-only admission, and post-response repeat keys make the proposed numerical contract explicit. No Phase C adoption, accepted expectation change, or human/native gate approval is implied.

**Amended 2026-09-10 (Step 4 numerical review):** Add exact circle topology/membership metadata to the canonical root owner after review showed that rounded normal speed can turn an exact high-speed tangent into a contact. Preserve root arithmetic and prove old/new root bits before delegation on both lanes. This is an unwired prototype correction, not an accepted-baseline or event-order change.

**Amended 2026-09-10 (Step 4 release build):** Include a behavior-preserving `GameSimulationSetup::of_mode` value-construction correction after the required optimized benchmark build diagnosed the temporary optional chain. No live declaration, signature, kernel phase, or accepted expectation changes.

**Amended 2026-09-10 (Step 4 retained-contact review):** Refine the shared circle result with existing exact radial/coincidence facts after review exposed a retained-certificate tangent bypass. Prove unchanged roots/new metadata on both lanes before consumer adoption, preserve certified geometry, and keep exact coincidence distinct from the legacy proximity fallback. No new root arithmetic or live detector change.

**Amended 2026-09-10 (Step 4 benchmark input):** The canonical runner's royale-only deployment assumption became stale when the deployment switched to king of the hill. Freeze the former royale configuration as an explicitly historical benchmark reference and amend its input/output/docs and ADR 0006 interpretation. Preserve the workload and all existing cases without touching live configuration or accepted expectations.

**Amended 2026-09-10 (owner Step 5 clarification):** The owner requires per-object effect eligibility, including grazes for some objects and player-center support loss for cliffs/darkness. Add the unwired Step 4a proof because the verified Step 4 driver rejects grazes globally; explicitly reserve its live observation/admission changes for Step 16. Confirm only a short perfect-shield opening that cancels incoming momentum and stuns; reopen ordinary/post-window behavior instead of treating the tested quarter-impulse proposal as approved. Add direction-only, fixed-strength mouse intent as the atomic Step 11a, reusing shared tuning, projection, and input ownership; activation/source details remain pending and future fast/slow modifiers are not implemented. The owner defers native capacity/performance selection, so remove it as a prerequisite of the current Step 5 decision without claiming certification or changing the clock, geometry, or numeric representation. ADR 0008 carries the same clarification. This is a documentation-only amendment; no new proof, live adoption, or human gate acceptance is implied.

**Amended 2026-09-10 (owner mouse activation choice):** ~~Mouse activation is pending~~ — the owner selected right-button hold to activate direction-only mouse thrust while preserving left-drag camera panning. Step 11a and ADR 0008 now record that binding and release-to-coast behavior. Other source-arbitration/dead-zone defaults remain proposals. No control implementation, build result, commit, or Step 5 acceptance is implied by recording this choice.

**Amended 2026-09-10 (owner post-perfect shield choice):** The owner confirmed ordinary contact protection with reduced knockback for the remaining shield duration. This supersedes the earlier deferral and resolves Step 18's gameplay hold. The tested quarter-impulse target is the communicated initial tuning assumption, not a user-selected exact fraction; non-closing composition still takes precedence. Perfect momentum cancellation/stun ends with the short opening, and cliffs bypass shield. ADR 0008 records the same decision. Step 5 remains pending; this documentation update does not implement shield or approve live physics adoption.

**Amended 2026-09-10 (Steps 7/8 verification and construction):** Preserve the newly exposed original clipped-corner terrain admission failure as an unresolved numerical issue at the existing Step 5 gate; calibrate only the new publication fixture using the established exact-effective-radius convention, without claiming the original invalid or fixed. Step 8's selected identity requires explicit race-owned initialization and a non-default validated road name, not a new generic kernel validation seam. Name the existing bounded-name consolidation prerequisite and add its proof-before-delegation commands plus simulation coverage to the final two-lane verification. No accepted geometry, lifecycle order, public wire baseline, or physics gate is changed by this clarification.

**Amended 2026-09-10 (owner-approved terrain repair):** ~~A separate geometry prerequisite needs direction before execution resumes~~ — the owner approved continuing with provenance-preserving boundary classification after online research. Insert Step 7a before the still-unverified Step 8 cutover. The earlier pause remains historical evidence; execution resumes at this dedicated prerequisite, not by weakening its proof or treating fractional input as invalid. Broader exact-geometry replacement is not assumed necessary; any unresolved representability limit stays visible. No Step 5 approval or Phase C authority is implied.

**Amended 2026-09-11 (Step 9 proof and publication):** Name the missing pre-delegation generator and actual-hazard proofs before switching readers. Fix stable stream ordinals, unsigned seed derivation, no initialization draws, existing whole-world rollback, and checked runtime lookup. Correct the wire premise: the old count was C++-only, so v3 adds a required closed per-stream count object; exact uint64-to-safe-integer rejection belongs to the production encoder. Preserve the inherited browser decimal-token rounding limitation for final protocol review instead of claiming parsed-number tests establish lexical precision. These changes do not alter accepted hazard outcomes, bot seeds, or the separate physics gate.

**Amended 2026-09-11 (Step 10 preflight checkpoint):** Record the movement-tuning ownership and truthful-completion gaps as a proposed design review, not an inline kernel/result-path rewrite. Pause for the requested review under the execution skill's successive-amendment rule. The existing Step 10 verify remains required and the checkbox remains unticked; no proposed delivery mechanism or exact rate is implemented or accepted by this note.

**Amended 2026-09-11 (owner-approved Step 10 feedback):** The owner approved correlated commit-confirmed outcomes and interrupted-request unknown status. Resolve the pending checkpoint with explicit lower-layer value ownership, a proven intent extraction, finite-step propulsion constraints, the existing closed phase-0 handler's bounded commit-only result, and mailbox-atomic claim-before-write delivery. Fix the command/result/ID/rate contracts and preserve permissions, other command ordering, integration/drag, and accepted replay bits. Release only unused candidates before the new two-lane pre-delegation proof; final full C++/web and fixed fuzz gates remain required. No Step 5 approval, source-completion claim, push, or deployment follows from this amendment.

**Amended 2026-09-11 (owner UI and control decisions):** Release Step 11 with preserved dirty drafts and explicit review of newer room values before Apply. Replace Step 11a's right-mouse/WASD arbitration proposal with cursor-only direction and owner-selected held Space propulsion; preserve left-drag camera panning and fixed-strength thrust. Space is no longer a proposed charge binding, and former directional keys are available for later abilities without implementing those abilities now. Exact-center zero is the directionless default, not distance-sensitive acceleration. The input migration and its browser evidence remain Step 11a; no Step 5 approval or Phase C adoption is implied.

**Amended 2026-09-11 (Step 11a browser proof boundary):** Correct the new test's unsupported assumption that bringing another pinned headless Chromium tab forward produces window blur. Keep the actual browser cursor/release/camera and other cancellation scenarios, all eight existing flows, and mandatory full gates unchanged. Blur remains a unit/integration proof, with no synthetic browser substitute or native-focus claim.

**Amended 2026-09-11 (Step 12 preflight checkpoint):** Record the missing continuous disc-clearance/contact and roam-admission prerequisites without implementing an unapproved boundary behavior. Request the two gameplay clarifications in the independent hill-roaming review, retaining the accepted Boolean terrain ambition and canonical geometry ownership. Step 12 stays unchecked; no source, fixture, configuration, wire, live kernel, or Step 5 status changes.

**Amended 2026-09-11 (Step 12 resolved implementation contract):** Record the owner's outer-boundary cancellation decision and its precise center-bound, per-axis interpretation. Replace unconditional no-stall/positive-actual-speed assumptions with sampled-speed bounds and permitted boundary rests; scheduled retargeting continues without guaranteed escape time. Specify strict authoring, deterministic sampling order, private committed schedule/public velocity, lifecycle and rollback proofs before implementation. Add protocol and fixed-corpus gates without removing the original verifies. Existing tour arithmetic and accepted fixtures remain unchanged; no live kernel, player terrain, Step 5, or Phase C contract changes.

**Amended 2026-09-11 (owner terrain-independent hill correction):** Supersede the previous Step 12 containment/reflection prerequisite. The hill itself does not bounce and may move over restricted/dangerous cliff areas; no spawn-reachability constraint applies. The owner's second answer permits disconnected locally safe regions without undoing the first answer's permission to cross unsafe terrain. Remove hill-motion clearance and unreachable-hill acceptance assumptions from ADR 0008, and label the prior review historical. Player terrain/fall rules are unchanged; the hill does not create safe ground. The subsequent outer-map answer requires canceling outward velocity and displacement, not steering or bouncing; all routing choices are resolved. No roaming implementation, verification result, or Step 5 approval follows from this documentation amendment.

**Amended 2026-09-11 (Step 13 lifetime ownership):** Clarify that the single registry sweep owns already-bodyless cleanup as well as same-tick elimination cleanup. Declare `ZoneExposure`'s existing body-bound semantics without changing royale's entity-destruction lifecycle. This is an ownership refactor; no accepted value, wire contract, or Phase C adoption changes.

**Amended 2026-09-11 (owner-approved missed-stun cancellation):** Extend Step 14 explicitly with persistent authoritative input generations, exact command echoes, browser activation-token lifetimes, shared active-bot authoring, and complete v3 validation/publication. Correct the value layer and missing lock predicate, define window/zero/invalid-target behavior, and record the bounded StunRequest foundation exception. Existing fixtures remain never-invalidated with absent fields and unchanged output. Preserve original verification and add both controller lanes. Step 5 remains unapproved; this does not implement Phase C or guarantee invisible entity-replacement detection.

**Amended 2026-09-11 (owner-approved tactical foundation):** Resolve Step 15's initial setting scope and pin its concrete contract before source work. Add a pre-cutover two-lane promotion proof; limit new seed/idempotence behavior to tactical; preserve fixed go acceleration and legacy indexed joins. Explicitly name the bounded NPC catalogue/profile values and optional declaration guard in the existing phase-0 join path, without adding a policy socket. Add runtime/server/fixture, corpus, and browser verification without removing the original gates. Combat settings, Step 5 approval, and Phase C remain deferred.

**Amended 2026-09-11 (Step 15 verification correction):** Preserve the supplementary sanitizer failures as evidence of an existing never-ready session-test publication, and repair the boundary fixture without weakening readiness, deadlines, or admission assertions. Add the explicit unready close regression and require fresh final gates. Also correct Step 5's historical clipped-corner note to the already-verified Step 7a disposition; this does not accept the physics gate.

**Amended 2026-09-11 (Step 5 human acceptance):** Record the owner's explicit approval of the revised physics prototype boundary and release Phase C. The accepted motion model and existing per-object contact/shield/input decisions are unchanged. Preserve documented numerical/resource limitations and the deferred native-performance/release gate; proceed with Step 16's preflight and exact verification, not a claim of live adoption.

**Amended 2026-09-11 (Step 16 integration preflight):** Pin the supplemental integration contract before source edits: frozen-world boundary, independently owned eighth declarations with borrowed per-binding facts, canonical body-envelope promotion, explicit sparse contact-policy authoring/publication, and two-lane pre-cutover proofs. Correct the stale discrete-wrapper test target and declaration-read location; keep production falling/race registration in Step 17. Add boundary/web/corpus/browser verification without removing the original gates. This amendment does not tick Step 16 or claim native evidence.

**Amended 2026-09-11 (Step 16 benchmark checkpoint):** Record the newly discovered conflict between mandatory full-simulation benchmark populations and the reviewed solver envelope. Pause before reader cutover under the executing-plans amendment guard; the proposed explicit grid-only migration is pending review, not implemented or silently substituted into the existing gate. Preserve current benchmark inputs and historical artifacts, finish verifying the unused prerequisites, and leave Step 16 unchecked.

**Amended 2026-09-11 (owner-approved benchmark migration):** The owner approved explicitly categorized grid-only diagnostics for the three oversized cases, retaining original geometry/provenance and historical baselines. Release Step 16 live adoption after its verified prerequisites. Keep the required runner and all applicable assertions, separate retired stepping/snapshot/encoding measurements from retained grid comparisons, and preserve the pure-solver workload/hash/work comparisons. This is not a capacity increase, a completion claim, or native certification.

**Amended 2026-09-11 (Step 17 implementation contract):** Resolve the source preflight using the existing gameplay and application boundaries. Specify body defaults, support phase eligibility, Sandbox's explicit configurable return delay, safe checkpoint startup rejection, same-tick spawn occupancy refresh, and certified normalized finish offsets/input release. Add boundary/corpus/browser/benchmark verification without removing original gates; no solver seam change or completion claim.

**Amended 2026-09-12 (Step 18 implementation contract):** Pin configured stun ownership for the noncapturing response, window-preserving cancellation, pulse-generation validation, static contact coverage, and complete v3 publication before source edits. Preserve the existing pure composition and command-refusal semantics, with no new acknowledgment channel or kernel seam. Add boundary/browser/benchmark verification for the crossed configuration and command surfaces; Step 18 remains unchecked.

**Amended 2026-09-12 (Step 22b split and implementation contract):** Split Step 22b again after a second read-only preflight round, moving the four named personalities to a new Step 22c. Root argued against the split on two grounds and both failed against source: the Step 22b Notes line is the original Step 22 Notes carried under a new heading and never re-scoped, already stale about two of its six clauses; and "hold" has a fixed meaning in this library, stated twice and shipped by Step 22a, so reading it as "brake" substituted a new meaning for a defined word. Three of the four names need shared mechanism rather than numbers, and each mechanism carries a defect the preflight found: a brake is 0/0 at rest and leaves an arrived bot permanently inert, "prefer exposed" provably cannot be a filter, and a zero-weight veto is an out-of-bounds read of a non-empty candidate set. Step 22b keeps the three combat behaviours, adds the fifth objective kind that first makes 22a's weights non-inert in production, and adds no `aggression` key because its preference half is that kind's weight and its danger-appetite half is what `risk_tolerance` was built one-signed to forbid. Both steps take Step 22a's corrected Verify shape and drop `run-benchmarks-linux`, now verified rather than inherited. No native, performance or release claim.


**Amended 2026-09-12 (owner-selected arrival-brake contract):** The owner chose “Keep current controls; define and test acceptable overshoot.” Step 22c's no-overshoot/all-drag stability statements above are historical and disproved, not current guarantees. Keep the algorithm and wire unchanged; accept the conditional velocity-reversal envelope in `docs/reviews/2026-09-12-arrival-brake-contract.md`, including its first-observation and timing limitations. Step 24 adds actual controller/steering/physics tests, comment and documentation corrections, and the carried formatting cleanup. Verify both full C++ lanes, fixed corpus, full web/browser, and formatting on the final inputs; this advisory supplement does not replace or tick the native release/benchmark gate.
