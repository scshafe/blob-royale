{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.blob-royale.invalid/protocol/v2/team-component.schema.json",
  "title": "Blob Royale protocol v2 team component",
  "description": "Optional side membership. An entity without this component is unaligned; team ids start at 1 so no value is a sentinel.",
  "x-status": "Historical",
  "type": "object",
  "additionalProperties": false,
  "required": ["team_id"],
  "properties": {
    "team_id": {
      "$ref": "common.schema.json#/$defs/team_id"
    }
  }
}
