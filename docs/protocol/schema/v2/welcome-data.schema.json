{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.blob-royale.invalid/protocol/v2/welcome-data.schema.json",
  "title": "Blob Royale protocol v2 welcome data",
  "description": "Everything a session learns about itself, sent exactly once immediately after the upgrade and before any snapshot. accepted_command_kinds is an advertisement of what the boundary will accept from this client, not a grant: the server enforces the same set independently and InputBatch::create filters it a second time. npc_controller_kinds is the same kind of advertisement for the one closed vocabulary a client is allowed to name a value from.",
  "x-status": "Accepted",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "entity_id",
    "controller_id",
    "display_name",
    "mode",
    "map",
    "accepted_command_kinds",
    "npc_controller_kinds",
    "lobby_id",
    "seat_count_maximum"
  ],
  "properties": {
    "entity_id": {
      "$ref": "common.schema.json#/$defs/entity_id",
      "$comment": "The session's FIRST body only. An eliminated or match-reset body is destroyed and the server seats the same controller_id on a new entity_id, so a client that caches this value renders the wrong blob after one match. Resolve the own body by controller_id."
    },
    "controller_id": {
      "$ref": "common.schema.json#/$defs/controller_id",
      "$comment": "Durable for the whole connection and the only stable self-identifier. A reconnect is a new join and receives a new controller_id."
    },
    "display_name": {
      "$ref": "common.schema.json#/$defs/display_name"
    },
    "mode": {
      "$ref": "common.schema.json#/$defs/mode_name"
    },
    "map": {
      "$ref": "common.schema.json#/$defs/map_name"
    },
    "accepted_command_kinds": {
      "type": "array",
      "maxItems": 5,
      "uniqueItems": true,
      "items": {
        "$ref": "common.schema.json#/$defs/command_kind"
      },
      "$comment": "maxItems tracks the size of common.schema.json#/$defs/command_kind, which went from one to five in 2.3. The published set is the intersection of the mode's accepted kinds with the client-sendable vocabulary, so it can never name a server-issued kind such as spawn or despawn, and a mode without a lobby publishes only set_thrust."
    },
    "npc_controller_kinds": {
      "type": "array",
      "maxItems": 64,
      "uniqueItems": true,
      "items": {
        "$ref": "common.schema.json#/$defs/controller_kind"
      },
      "$comment": "The exact set of NPC kinds a seat_npc command may name, read from the server's controller registry. maxItems is deliberately NOT the number of registered bots: it is a protocol constant, so registering a bot changes this array's contents and not this schema, which is what makes a new bot appear in every client's seat menu with no client change and no protocol version. Empty is legal and means no bot kind is registered, in which case no seat can be filled with one. Added in 2.3."
    },
    "lobby_id": {
      "$ref": "common.schema.json#/$defs/lobby_id",
      "$comment": "The room this session was admitted into: room 1 on /api/v2/session, the id in the path on /api/v2/lobbies/<lobby_id>/session. Added in 2.4."
    },
    "seat_count_maximum": {
      "$ref": "common.schema.json#/$defs/seat_count",
      "$comment": "The most seats this room's map can seat -- its spawn-marker count capped at the seat bound -- so a client can floor its seat-count control without a second round trip. A set_seat_count above it is ignored by the tick. Added in 2.4."
    }
  }
}
