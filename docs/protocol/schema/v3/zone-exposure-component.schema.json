{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.blob-royale.invalid/protocol/v3/zone-exposure-component.schema.json",
  "title": "Blob Royale protocol v3 zone_exposure component",
  "description": "Consecutive committed ticks an entity's center has been outside the zone. An absent component reads as zero.",
  "x-status": "Accepted",
  "type": "object",
  "additionalProperties": false,
  "required": ["outside_ticks"],
  "properties": {
    "outside_ticks": {
      "$ref": "common.schema.json#/$defs/safe_integer"
    }
  }
}
