// @generated by scripts/generateProtocolV3Types.mjs.
// Canonical source: ../../../../../docs/protocol/schema/v3/*.schema.json.
// Do not edit this file directly; run npm run generate:protocol.

export const protocolV3Schemas = {
  clearSeatCommand: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/clear-seat-command.schema.json',
    title: 'Blob Royale protocol v3 clear_seat command payload',
    description:
      "Empty one seat that holds a declared NPC. The payload names no sender: the server stamps the sending session's controller id, so a controller id is not merely ignored here, it is a closed-schema violation that closes the connection.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['seat_index'],
    properties: {
      seat_index: {
        $ref: 'common.schema.json#/$defs/seat_index',
      },
    },
    $comment:
      'Clears a seat whose kind is npc and leaves a seat whose kind is controller alone: a controller seat belongs to a live session and only the runtime may give or take it. Clearing an already-empty seat is a no-op, so pressing the same control twice is one decision. Added in 2.3.',
  },
  commandEnvelope: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/command-envelope.schema.json',
    title: 'Blob Royale protocol v3 client command envelope',
    description:
      'Every application data message a client may send. The envelope carries no entity id, no protocol version, no request id, and no client sequence number: identity and version are properties of the connection, and a client-supplied version would be a downgrade lever.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['kind', 'payload'],
    properties: {
      kind: {
        $ref: 'common.schema.json#/$defs/command_kind',
      },
      payload: {
        type: 'object',
      },
    },
    allOf: [
      {
        if: {
          properties: {
            kind: {
              const: 'set_thrust',
            },
          },
          required: ['kind'],
        },
        then: {
          properties: {
            payload: {
              $ref: 'set-thrust-command.schema.json',
            },
          },
        },
      },
      {
        if: {
          properties: {
            kind: {
              const: 'set_seat_count',
            },
          },
          required: ['kind'],
        },
        then: {
          properties: {
            payload: {
              $ref: 'set-seat-count-command.schema.json',
            },
          },
        },
      },
      {
        if: {
          properties: {
            kind: {
              const: 'clear_seat',
            },
          },
          required: ['kind'],
        },
        then: {
          properties: {
            payload: {
              $ref: 'clear-seat-command.schema.json',
            },
          },
        },
      },
      {
        if: {
          properties: {
            kind: {
              const: 'seat_npc',
            },
          },
          required: ['kind'],
        },
        then: {
          properties: {
            payload: {
              $ref: 'seat-npc-command.schema.json',
            },
          },
        },
      },
      {
        if: {
          properties: {
            kind: {
              const: 'start_match',
            },
          },
          required: ['kind'],
        },
        then: {
          properties: {
            payload: {
              $ref: 'start-match-command.schema.json',
            },
          },
        },
      },
    ],
    $comment:
      '@extension-point command_kind -- a new client command adds one enum member in common.schema.json#/$defs/command_kind, one <kind>-command.schema.json file, one if/then row here, and one maxItems bump in welcome-data.schema.json, and is a protocol minor version.',
  },
  common: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/common.schema.json',
    title: 'Blob Royale protocol v3 common definitions',
    description:
      'Canonical scalar, identifier, vector, kind-name, error, and metadata definitions shared by protocol v3 messages. protocol_version is a const rather than a grammar so an older decoder fails closed on a newer minor instead of silently ignoring an unknown kind or an unread member.',
    'x-status': 'Accepted',
    $defs: {
      protocol_version: {
        type: 'string',
        const: '3.0',
        $comment:
          'One session major opens with required welcome terrain and explicit v2 retirement. Later planned authoritative behaviors land under 3.0 with their complete contracts; development commits are not independent deployable releases. See docs/protocol/v3.md.',
      },
      request_id: {
        type: 'string',
        minLength: 1,
        maxLength: 64,
        pattern: '^[A-Za-z0-9._:-]+$',
      },
      schema_id: {
        type: 'string',
        minLength: 1,
        maxLength: 128,
        pattern: '^blob-royale://protocol/v3/[a-z][a-z0-9-]*$',
      },
      mode_state_schema_id: {
        type: 'string',
        enum: [
          'blob-royale://protocol/v3/mode-state/none',
          'blob-royale://protocol/v3/mode-state/royale',
          'blob-royale://protocol/v3/mode-state/king-of-the-hill',
          'blob-royale://protocol/v3/mode-state/race',
        ],
        $comment:
          'Closed, not a grammar: an unregistered mode-state schema id is a decode failure, not a value a client may skip.',
      },
      utc_timestamp: {
        type: 'string',
        format: 'date-time',
        minLength: 20,
        maxLength: 32,
        pattern: 'Z$',
      },
      safe_integer: {
        type: 'integer',
        minimum: 0,
        maximum: 9007199254740991,
      },
      positive_safe_integer: {
        type: 'integer',
        minimum: 1,
        maximum: 9007199254740991,
      },
      signed_safe_integer: {
        type: 'integer',
        minimum: -9007199254740991,
        maximum: 9007199254740991,
      },
      entity_id: {
        $ref: '#/$defs/positive_safe_integer',
      },
      controller_id: {
        $ref: '#/$defs/positive_safe_integer',
      },
      team_id: {
        $ref: '#/$defs/positive_safe_integer',
      },
      tick_sequence: {
        $ref: '#/$defs/positive_safe_integer',
      },
      phase_start_tick: {
        $ref: '#/$defs/safe_integer',
        $comment:
          "A tick sequence that also admits 0. Zero is the truthful value for 'no phase transition has been committed yet': a match begins in lobby at load, before tick 1 exists, and MatchState holds TickSequence::zero() for that whole lobby. Every other tick-valued member is a tick_sequence, whose minimum is 1.",
      },
      message_sequence: {
        $ref: '#/$defs/positive_safe_integer',
      },
      snapshot_entity_limit: {
        type: 'integer',
        const: 1024,
        $comment:
          "Protocol constant. Every published entity counts, including map static bodies and the zone entity. Sized so the worst-case encoded frame fits the 2,097,152-byte ceiling with headroom; see docs/protocol/v3.md section 'Limits'.",
      },
      placement_rank: {
        type: 'integer',
        minimum: 1,
        maximum: 1024,
      },
      finite_world_scalar: {
        type: 'number',
        minimum: -1000000000000,
        maximum: 1000000000000,
      },
      nonnegative_world_scalar: {
        type: 'number',
        minimum: 0,
        maximum: 1000000000000,
      },
      positive_world_scalar: {
        type: 'number',
        exclusiveMinimum: 0,
        maximum: 1000000000000,
      },
      unit_interval_scalar: {
        type: 'number',
        minimum: -1,
        maximum: 1,
      },
      collision_bitmask: {
        type: 'integer',
        minimum: 0,
        maximum: 4294967295,
      },
      vector2: {
        type: 'object',
        additionalProperties: false,
        required: ['x', 'y'],
        properties: {
          x: {
            $ref: '#/$defs/finite_world_scalar',
          },
          y: {
            $ref: '#/$defs/finite_world_scalar',
          },
        },
      },
      kind_name: {
        type: 'string',
        minLength: 1,
        maxLength: 64,
        pattern: '^[a-z][a-z0-9_]*$',
      },
      mode_name: {
        $ref: '#/$defs/kind_name',
      },
      controller_kind: {
        $ref: '#/$defs/kind_name',
      },
      map_name: {
        type: 'string',
        minLength: 1,
        maxLength: 64,
        pattern: '^[a-z0-9][a-z0-9._-]*$',
      },
      display_name: {
        type: 'string',
        minLength: 1,
        maxLength: 64,
        pattern: "^[A-Za-z0-9](?:[A-Za-z0-9 ._'@-]{0,62}[A-Za-z0-9])?$",
        $comment:
          'Printable ASCII only, so maxLength is simultaneously a 64-byte bound; no leading or trailing space; no control byte, so the value cannot inject a log line or a terminal escape. Derived from Tailscale-User-Name by exact match after trimming, never by character substitution.',
      },
      component_kind: {
        type: 'string',
        enum: [
          'controllable',
          'hill',
          'hill_presence',
          'lethal_on_contact',
          'lifetime',
          'physics_body',
          'race_progress',
          'respawn_timer',
          'score',
          'team',
          'zone',
          'zone_exposure',
        ],
      },
      command_kind: {
        type: 'string',
        enum: [
          'clear_seat',
          'seat_npc',
          'set_seat_count',
          'set_thrust',
          'start_match',
        ],
        $comment:
          'The client-sendable vocabulary only. spawn and despawn are server-issued on session admission and close and are deliberately absent from the wire. Four of the five operate the pre-match lobby and were added in 2.3; a mode that has no lobby to operate omits them from welcome.accepted_command_kinds rather than accepting a command it cannot honour.',
      },
      lobby_id: {
        type: 'integer',
        minimum: 1,
        maximum: 8,
        $comment:
          "Which room, 1..N in the order the process built them. The maximum is the protocol's directory limit, kLobbyDirectoryLimit, which is also how many rooms one process may run. Added in 2.4.",
      },
      seat_count: {
        type: 'integer',
        minimum: 1,
        maximum: 64,
        $comment:
          "How many seats one lobby may declare. The maximum is a protocol constant mirroring the engine's own bound; the tighter and more meaningful ceiling is the running map's spawn-marker count, which the server enforces at startup and which no client can read from this schema.",
      },
      seat_index: {
        type: 'integer',
        minimum: 0,
        maximum: 63,
        $comment:
          "Zero-based, so the maximum is one below seat_count's. It bounds the value rather than the running lobby: an index inside this bound that names no seat in the current roster is ignored by the server, because a client's view of the roster lags by a frame and racing it must not close a connection.",
      },
      seat_kind: {
        type: 'string',
        enum: ['controller', 'empty', 'npc'],
        $comment:
          'Closed. A seat is empty, held by a live controller, or declared for an NPC of a named kind; the three are exclusive and every published seat names exactly one.',
      },
      match_phase: {
        type: 'string',
        enum: ['lobby', 'countdown', 'running', 'ended'],
      },
      outcome_kind: {
        type: 'string',
        enum: ['none', 'won_by_entity', 'won_by_team', 'drawn'],
      },
      error_details: {
        type: 'object',
        additionalProperties: false,
        properties: {
          required_protocol_version: {
            const: '3.0',
            $comment:
              'Present only for PROTOCOL.SESSION_VERSION_UPGRADE_REQUIRED.',
          },
          lobby_id: {
            $ref: '#/$defs/lobby_id',
            $comment:
              'Present on LOBBY.FULL and LOBBY.UNAVAILABLE, naming the room the request named. Absent on LOBBY.NOT_FOUND, which by definition names no room. Added in 2.4.',
          },
          allowed_methods: {
            type: 'array',
            minItems: 1,
            maxItems: 4,
            uniqueItems: true,
            items: {
              type: 'string',
              enum: ['GET'],
            },
          },
          expected_websocket_version: {
            type: 'string',
            const: '13',
          },
          forwarded_client_reason: {
            type: 'string',
            enum: ['absent', 'multiple_values', 'not_canonical'],
            $comment:
              'A closed enum, not free text, so no byte of an attacker-supplied forwarding header can ever reach a response body or a log line.',
          },
          limit: {
            type: 'integer',
            minimum: 0,
            maximum: 2097152,
          },
          reason: {
            type: 'string',
            minLength: 1,
            maxLength: 128,
          },
          retry_after_ms: {
            type: 'integer',
            minimum: 1,
            maximum: 60000,
          },
        },
      },
      error: {
        type: 'object',
        additionalProperties: false,
        required: ['code', 'message', 'retryable', 'details'],
        allOf: [
          {
            if: {
              properties: {
                code: {
                  const: 'PROTOCOL.SESSION_VERSION_UPGRADE_REQUIRED',
                },
              },
              required: ['code'],
            },
            then: {
              properties: {
                retryable: {
                  const: false,
                },
                details: {
                  type: 'object',
                  properties: {
                    required_protocol_version: {
                      const: '3.0',
                    },
                  },
                  required: ['required_protocol_version'],
                  maxProperties: 1,
                },
              },
            },
            else: {
              properties: {
                details: {
                  type: 'object',
                  properties: {
                    required_protocol_version: false,
                  },
                },
              },
            },
          },
        ],
        properties: {
          code: {
            type: 'string',
            enum: [
              'LOBBY.FULL',
              'LOBBY.NOT_FOUND',
              'LOBBY.UNAVAILABLE',
              'PROTOCOL.CONNECTION_LIMIT_REACHED',
              'PROTOCOL.HEADER_TOO_LARGE',
              'PROTOCOL.INVALID_FORWARDED_CLIENT',
              'PROTOCOL.INVALID_REQUEST',
              'PROTOCOL.INVALID_REQUEST_ID',
              'PROTOCOL.METHOD_NOT_ALLOWED',
              'PROTOCOL.ORIGIN_REJECTED',
              'PROTOCOL.PAYLOAD_TOO_LARGE',
              'PROTOCOL.RATE_LIMITED',
              'PROTOCOL.ROUTE_NOT_FOUND',
              'PROTOCOL.SESSION_VERSION_UPGRADE_REQUIRED',
              'PROTOCOL.SUBPROTOCOL_REQUIRED',
              'PROTOCOL.UPGRADE_REQUIRED',
              'PROTOCOL.WEBSOCKET_VERSION_UNSUPPORTED',
              'SERVICE.INTERNAL_FAILURE',
              'SERVICE.NOT_READY',
            ],
          },
          message: {
            type: 'string',
            minLength: 1,
            maxLength: 256,
          },
          retryable: {
            type: 'boolean',
          },
          details: {
            $ref: '#/$defs/error_details',
          },
        },
      },
    },
  },
  controllableComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/controllable-component.schema.json',
    title: 'Blob Royale protocol v3 controllable component',
    description:
      "Links an entity to the controller that decides for it. controller_id is the durable identity of the deciding agent and is how a client recognizes its own body across matches; entity_id is the identity of one body and does not survive elimination or a match restart. controller_kind is 'session' for a network player and the registered bot kind otherwise. display_name is a sanitized label, never a credential and never an authorization input.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['controller_id', 'controller_kind', 'display_name'],
    properties: {
      controller_id: {
        $ref: 'common.schema.json#/$defs/controller_id',
      },
      controller_kind: {
        $ref: 'common.schema.json#/$defs/controller_kind',
      },
      display_name: {
        $ref: 'common.schema.json#/$defs/display_name',
      },
    },
    $comment:
      "Controllable::commands_this_tick (ADR 0004) is deliberately NOT published. Publishing this tick's commands would disclose every player's live input to every other peer, which is a same-frame reaction advantage and a class of leak no rate limit can undo.",
  },
  entitySnapshot: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/entity-snapshot.schema.json',
    title: 'Blob Royale protocol v3 entity snapshot',
    description:
      'One entity of one committed tick: its stable id and the closed map of the components it carries, keyed by component kind. An entity is its component set and nothing else (ADR 0004). This file is the registration point for a component kind on the wire.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['entity_id', 'components'],
    properties: {
      entity_id: {
        $ref: 'common.schema.json#/$defs/entity_id',
      },
      components: {
        type: 'object',
        additionalProperties: false,
        minProperties: 1,
        properties: {
          controllable: {
            $ref: 'controllable-component.schema.json',
          },
          hill: {
            $ref: 'hill-component.schema.json',
          },
          hill_presence: {
            $ref: 'hill-presence-component.schema.json',
          },
          lethal_on_contact: {
            $ref: 'lethal-on-contact-component.schema.json',
          },
          lifetime: {
            $ref: 'lifetime-component.schema.json',
          },
          physics_body: {
            $ref: 'physics-body-component.schema.json',
          },
          race_progress: {
            $ref: 'race-progress-component.schema.json',
          },
          respawn_timer: {
            $ref: 'respawn-timer-component.schema.json',
          },
          score: {
            $ref: 'score-component.schema.json',
          },
          team: {
            $ref: 'team-component.schema.json',
          },
          zone: {
            $ref: 'zone-component.schema.json',
          },
          zone_exposure: {
            $ref: 'zone-exposure-component.schema.json',
          },
        },
        $comment:
          '@extension-point snapshot_component_kind -- a new component kind adds one property here, one <kind>-component.schema.json file, and one enum member in common.schema.json#/$defs/component_kind, and is a protocol minor version. additionalProperties is false so a 2.0 decoder fails closed on a kind it does not know rather than rendering an incomplete world.',
      },
    },
  },
  errorResponse: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/error-response.schema.json',
    title: 'Blob Royale protocol v3 HTTP error response',
    description:
      'Current session failure envelope for representable errors on parsed /api/v3/ and retired /api/v2/ targets. Only grammar-valid retired routes receive SESSION_VERSION_UPGRADE_REQUIRED after global checks; earlier security failures retain their original code and precedence. v1 routes retain their immutable envelope. There is no in-band WebSocket error frame: a session failure is a close code.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['data', 'error', 'meta'],
    properties: {
      data: {
        type: 'null',
      },
      error: {
        $ref: 'common.schema.json#/$defs/error',
      },
      meta: {
        type: 'object',
        additionalProperties: false,
        required: ['protocol_version', 'schema_id', 'request_id'],
        properties: {
          protocol_version: {
            $ref: 'common.schema.json#/$defs/protocol_version',
          },
          schema_id: {
            const: 'blob-royale://protocol/v3/error-response',
          },
          request_id: {
            $ref: 'common.schema.json#/$defs/request_id',
          },
        },
      },
    },
  },
  hillComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/hill-component.schema.json',
    title: 'Blob Royale protocol v3 hill component',
    description:
      'The scoring circle carried by the hill entity, in wu. The hill entity owns no physics_body and no controllable. Same shape as the zone component and a different meaning: inside a hill is where a player scores, inside a zone is where a player is safe, and a client draws the two apart by kind. This component is the only place the hill is published.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['center', 'radius'],
    properties: {
      center: {
        $ref: 'common.schema.json#/$defs/vector2',
      },
      radius: {
        $ref: 'common.schema.json#/$defs/nonnegative_world_scalar',
      },
    },
    $comment: 'Added in 2.5.',
  },
  hillPresenceComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/hill-presence-component.schema.json',
    title: 'Blob Royale protocol v3 hill_presence component',
    description:
      "How many consecutive committed ticks an entity has held the hill toward its next point. An absent component reads as zero; the bound it counts toward is the king_of_the_hill mode-state block's point_interval_ticks.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['inside_ticks'],
    properties: {
      inside_ticks: {
        $ref: 'common.schema.json#/$defs/safe_integer',
        $comment: 'Added in 2.5.',
      },
    },
  },
  kingOfTheHillModeState: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/king-of-the-hill-mode-state.schema.json',
    title: 'Blob Royale protocol v3 king of the hill mode state',
    description:
      "The king_of_the_hill mode's non-entity-shaped match state (ADR 0007): three declared constants and nothing observed. Scores are the score components of the player entities, the hill is the hill component of its entity, and progress toward the next point is each entity's hill_presence; this block carries the denominators a client cannot compute from the frame.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['points_to_win', 'point_interval_ticks', 'time_limit_ticks'],
    properties: {
      points_to_win: {
        $ref: 'common.schema.json#/$defs/safe_integer',
        $comment:
          'At least one; the score at which the match is decided. Added in 2.5.',
      },
      point_interval_ticks: {
        $ref: 'common.schema.json#/$defs/safe_integer',
        $comment:
          'The consecutive inside ticks that earn one point: the denominator of hill_presence.inside_ticks. Zero is legal and means a point on the first inside tick, so a client dividing by it must guard rather than treat zero as absent. Added in 2.5.',
      },
      time_limit_ticks: {
        $ref: 'common.schema.json#/$defs/safe_integer',
        $comment:
          'Running ticks after which the leader wins; the elapsed running time is tick_sequence minus phase_started_tick while running. Added in 2.5.',
      },
    },
  },
  lethalOnContactComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/lethal-on-contact-component.schema.json',
    title: 'Blob Royale protocol v3 lethal_on_contact component',
    description:
      "Touching this entity eliminates a player. The component is a marker: its presence under an entity's components map is the entire message, so the object carries no member and an absent component is the whole of 'harmless'. A synthetic boolean member was rejected because it could only ever hold true - an entity that is not lethal does not carry the kind at all - and a field with one possible value is a fact a client must trust rather than read.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    $comment:
      "No 'required' and no 'properties': the empty object is the complete value, and additionalProperties:false keeps it that way, so a later member cannot be added without a minor version. Added in 2.1.",
  },
  lifetimeComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/lifetime-component.schema.json',
    title: 'Blob Royale protocol v3 lifetime component',
    description: "A self-expiring entity's remaining committed ticks.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['ticks_remaining'],
    properties: {
      ticks_remaining: {
        $ref: 'common.schema.json#/$defs/safe_integer',
      },
    },
  },
  lobbyDirectoryData: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/lobby-directory-data.schema.json',
    title: 'Blob Royale protocol v3 lobby directory data',
    description:
      "Every room this process runs, in lobby-id order, as a client chooses one to join. It is read from each room's latest published snapshot and its admitted-session count, so it is exactly as current as the newest snapshot and never more: a client that joins on it may still find the room changed by the time it is admitted, which is what the 409 and 503 refusals and the lobby_full close are for. Added in 2.4.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['lobbies'],
    properties: {
      lobbies: {
        type: 'array',
        minItems: 1,
        maxItems: 8,
        items: {
          $ref: '#/$defs/lobby_listing',
        },
        $comment:
          'Rooms are numbered 1..N in the order the process built them and the directory is fixed for the process lifetime, so the i-th entry always has lobby_id i+1. maxItems mirrors kLobbyDirectoryLimit in src/protocol/protocol_v3_constants.hpp, the most rooms one process may run.',
      },
    },
    $defs: {
      lobby_listing: {
        type: 'object',
        additionalProperties: false,
        required: [
          'lobby_id',
          'mode',
          'map',
          'phase',
          'phase_started_tick',
          'tick_sequence',
          'seat_count',
          'seat_count_maximum',
          'filled_seat_count',
          'npc_seat_count',
          'session_count',
          'healthy',
        ],
        properties: {
          lobby_id: {
            $ref: 'common.schema.json#/$defs/lobby_id',
          },
          mode: {
            $ref: 'common.schema.json#/$defs/mode_name',
          },
          map: {
            $ref: 'common.schema.json#/$defs/map_name',
          },
          phase: {
            $ref: 'common.schema.json#/$defs/match_phase',
          },
          phase_started_tick: {
            $ref: 'common.schema.json#/$defs/phase_start_tick',
          },
          tick_sequence: {
            $ref: 'common.schema.json#/$defs/safe_integer',
            $comment:
              "The committed tick the rest of this entry was read from. Zero means the room has published no tick at all -- its runtime is not ready or has failed -- and every count below is then zero too; a snapshot's own tick_sequence starts at 1.",
          },
          seat_count: {
            type: 'integer',
            minimum: 0,
            maximum: 64,
            $comment:
              "The lobby's current size, which anyone in it may resize. Zero is a room whose mode declares no lobby and never starts a match.",
          },
          seat_count_maximum: {
            $ref: 'common.schema.json#/$defs/seat_count',
            $comment:
              "The most seats this room's map can seat: its spawn-marker count capped at the seat bound. A set_seat_count above it is ignored by the tick, so a client floors its control here.",
          },
          filled_seat_count: {
            type: 'integer',
            minimum: 0,
            maximum: 64,
            $comment:
              'Seats that hold a controller: a person, or an NPC seat whose bot the server has created. A match starts once this equals seat_count and somebody has pressed Start.',
          },
          npc_seat_count: {
            type: 'integer',
            minimum: 0,
            maximum: 64,
            $comment:
              'Seats declared for an NPC, whether or not its bot exists yet.',
          },
          session_count: {
            type: 'integer',
            minimum: 0,
            maximum: 64,
            $comment:
              'Sessions admitted into this room right now, whether or not each holds a seat. A join is refused 409 LOBBY.FULL once this equals seat_count.',
          },
          healthy: {
            type: 'boolean',
            $comment:
              "False while the room's runtime has failed or is not publishing, or while its last minute holds a tick overrun; a join to an unhealthy room may be refused 503 LOBBY.UNAVAILABLE.",
          },
        },
      },
    },
  },
  lobbyDirectoryMessage: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/lobby-directory-message.schema.json',
    title: 'Blob Royale protocol v3 lobby directory response',
    description:
      'The body of a successful GET /api/v3/lobbies: the v3 envelope around lobby-directory-data. An HTTP document rather than a WebSocket frame, so its meta carries no message_sequence and no sent_at_utc; the per-room tick_sequence is its freshness. Added in 2.4.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['data', 'error', 'meta'],
    properties: {
      data: {
        $ref: 'lobby-directory-data.schema.json',
      },
      error: {
        type: 'null',
      },
      meta: {
        type: 'object',
        additionalProperties: false,
        required: ['protocol_version', 'schema_id', 'request_id'],
        properties: {
          protocol_version: {
            $ref: 'common.schema.json#/$defs/protocol_version',
          },
          schema_id: {
            const: 'blob-royale://protocol/v3/lobby-directory',
          },
          request_id: {
            $ref: 'common.schema.json#/$defs/request_id',
          },
        },
      },
    },
  },
  matchData: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/match-data.schema.json',
    title: 'Blob Royale protocol v3 match section',
    description:
      'The generic match lifecycle header, the pre-match seat roster the first transition reads, plus one mode-state block identified by a schema id. Entity-shaped mode state is published only as components and never duplicated here; the royale safe zone is the zone component of the zone entity (ADR 0005). Alive count is not a field: it is the number of published entities carrying both physics_body and controllable.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: [
      'mode',
      'phase',
      'phase_started_tick',
      'seats',
      'start_requested',
      'outcome',
      'placements',
      'mode_state',
    ],
    properties: {
      mode: {
        $ref: 'common.schema.json#/$defs/mode_name',
      },
      phase: {
        $ref: 'common.schema.json#/$defs/match_phase',
      },
      phase_started_tick: {
        $ref: 'common.schema.json#/$defs/phase_start_tick',
      },
      seats: {
        type: 'array',
        maxItems: 64,
        items: {
          $ref: '#/$defs/seat',
        },
        $comment:
          'The pre-match lobby in index order, which is the order a client renders and the order every tie-break over seats resolves in. It sits with the lifecycle header it gates rather than beside outcome and placements, which describe a match already played. A world that declared no lobby publishes an empty array rather than a null, so a decoder has one code path; an empty roster is never full, so no client can read it as a startable match. Added in 2.3.',
      },
      start_requested: {
        type: 'boolean',
        $comment:
          'Whether somebody has pressed Start for this lobby. It is one-shot and the server clears it on every arrival in lobby, so a finished match cannot restart itself; a client reads it together with seats to explain why Start is or is not going to fire. Added in 2.3.',
      },
      outcome: {
        $ref: '#/$defs/outcome',
      },
      placements: {
        type: 'array',
        maxItems: 1024,
        items: {
          $ref: '#/$defs/placement',
        },
      },
      mode_state: {
        $ref: '#/$defs/mode_state',
      },
    },
    $defs: {
      outcome: {
        type: 'object',
        additionalProperties: false,
        required: ['kind', 'winner_entity_id', 'winner_team_id'],
        description:
          'Two named winner members rather than one polymorphic winner, so a reader can never mistake a team id for an entity id. Both are always present; the member that does not apply is null.',
        properties: {
          kind: {
            $ref: 'common.schema.json#/$defs/outcome_kind',
          },
          winner_entity_id: {
            oneOf: [
              {
                $ref: 'common.schema.json#/$defs/entity_id',
              },
              {
                type: 'null',
              },
            ],
          },
          winner_team_id: {
            oneOf: [
              {
                $ref: 'common.schema.json#/$defs/team_id',
              },
              {
                type: 'null',
              },
            ],
          },
        },
        allOf: [
          {
            if: {
              properties: {
                kind: {
                  enum: ['none', 'drawn'],
                },
              },
              required: ['kind'],
            },
            then: {
              properties: {
                winner_entity_id: {
                  type: 'null',
                },
                winner_team_id: {
                  type: 'null',
                },
              },
            },
          },
          {
            if: {
              properties: {
                kind: {
                  const: 'won_by_entity',
                },
              },
              required: ['kind'],
            },
            then: {
              properties: {
                winner_entity_id: {
                  $ref: 'common.schema.json#/$defs/entity_id',
                },
                winner_team_id: {
                  type: 'null',
                },
              },
            },
          },
          {
            if: {
              properties: {
                kind: {
                  const: 'won_by_team',
                },
              },
              required: ['kind'],
            },
            then: {
              properties: {
                winner_entity_id: {
                  type: 'null',
                },
                winner_team_id: {
                  $ref: 'common.schema.json#/$defs/team_id',
                },
              },
            },
          },
        ],
      },
      seat: {
        type: 'object',
        additionalProperties: false,
        required: ['kind', 'controller_id', 'npc_kind'],
        description:
          'One seat in the lobby. Every member is always present and the member that does not apply is null, which is the shape outcome uses and for the same reason: a decoder reads one known member set rather than branching on which keys exist.',
        properties: {
          kind: {
            $ref: 'common.schema.json#/$defs/seat_kind',
          },
          controller_id: {
            oneOf: [
              {
                $ref: 'common.schema.json#/$defs/controller_id',
              },
              {
                type: 'null',
              },
            ],
          },
          npc_kind: {
            oneOf: [
              {
                $ref: 'common.schema.json#/$defs/controller_kind',
              },
              {
                type: 'null',
              },
            ],
          },
        },
        allOf: [
          {
            if: {
              properties: {
                kind: {
                  const: 'empty',
                },
              },
              required: ['kind'],
            },
            then: {
              properties: {
                controller_id: {
                  type: 'null',
                },
                npc_kind: {
                  type: 'null',
                },
              },
            },
          },
          {
            if: {
              properties: {
                kind: {
                  const: 'controller',
                },
              },
              required: ['kind'],
            },
            then: {
              properties: {
                controller_id: {
                  $ref: 'common.schema.json#/$defs/controller_id',
                },
                npc_kind: {
                  type: 'null',
                },
              },
            },
          },
          {
            if: {
              properties: {
                kind: {
                  const: 'npc',
                },
              },
              required: ['kind'],
            },
            then: {
              properties: {
                npc_kind: {
                  $ref: 'common.schema.json#/$defs/controller_kind',
                },
              },
            },
          },
        ],
        $comment:
          'An npc seat leaves controller_id nullable on purpose: the seat is a declaration the moment somebody chooses a kind, and the server fills in the controller once it has built that bot. A null there is the joining state, not a missing value, and a client renders it as such.',
      },
      placement: {
        type: 'object',
        additionalProperties: false,
        required: [
          'entity_id',
          'controller_id',
          'placement',
          'eliminated_tick',
        ],
        description:
          'One finished rank. controller_id is carried so a client can recognize its own result after its entity_id has been destroyed.',
        properties: {
          entity_id: {
            $ref: 'common.schema.json#/$defs/entity_id',
          },
          controller_id: {
            $ref: 'common.schema.json#/$defs/controller_id',
          },
          placement: {
            $ref: 'common.schema.json#/$defs/placement_rank',
          },
          eliminated_tick: {
            $ref: 'common.schema.json#/$defs/tick_sequence',
          },
        },
      },
      mode_state: {
        type: 'object',
        additionalProperties: false,
        required: ['schema_id', 'value'],
        description:
          'One shape for every mode. A mode with no non-entity-shaped state publishes the none schema id and an empty value object rather than a null, so a decoder has exactly one code path.',
        properties: {
          schema_id: {
            $ref: 'common.schema.json#/$defs/mode_state_schema_id',
          },
          value: {
            type: 'object',
          },
        },
        allOf: [
          {
            if: {
              properties: {
                schema_id: {
                  const: 'blob-royale://protocol/v3/mode-state/none',
                },
              },
              required: ['schema_id'],
            },
            then: {
              properties: {
                value: {
                  type: 'object',
                  additionalProperties: false,
                  properties: {},
                },
              },
            },
          },
          {
            if: {
              properties: {
                schema_id: {
                  const: 'blob-royale://protocol/v3/mode-state/royale',
                },
              },
              required: ['schema_id'],
            },
            then: {
              properties: {
                value: {
                  $ref: 'royale-mode-state.schema.json',
                },
              },
            },
          },
          {
            if: {
              properties: {
                schema_id: {
                  const:
                    'blob-royale://protocol/v3/mode-state/king-of-the-hill',
                },
              },
              required: ['schema_id'],
            },
            then: {
              properties: {
                value: {
                  $ref: 'king-of-the-hill-mode-state.schema.json',
                },
              },
            },
          },
          {
            if: {
              properties: {
                schema_id: {
                  const: 'blob-royale://protocol/v3/mode-state/race',
                },
              },
              required: ['schema_id'],
            },
            then: {
              properties: {
                value: {
                  $ref: 'race-mode-state.schema.json',
                },
              },
            },
          },
        ],
        $comment:
          '@extension-point snapshot_mode_state -- a new mode-state block adds one enum member in common.schema.json#/$defs/mode_state_schema_id, one <mode>-mode-state.schema.json file, and one if/then row here, and is a protocol minor version.',
      },
    },
  },
  physicsBodyComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/physics-body-component.schema.json',
    title: 'Blob Royale protocol v3 physics_body component',
    description:
      'The one physical body value of an entity: everything the physics kernel reads or writes. Units follow ADR 0003: position in wu, velocity in wu/s, acceleration in wu/s^2, radius in wu.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: [
      'position',
      'velocity',
      'acceleration',
      'radius',
      'mass',
      'collision_layer',
      'collision_mask',
      'is_static',
    ],
    properties: {
      position: {
        $ref: 'common.schema.json#/$defs/vector2',
      },
      velocity: {
        $ref: 'common.schema.json#/$defs/vector2',
      },
      acceleration: {
        $ref: 'common.schema.json#/$defs/vector2',
      },
      radius: {
        $ref: 'common.schema.json#/$defs/positive_world_scalar',
      },
      mass: {
        $ref: 'common.schema.json#/$defs/nonnegative_world_scalar',
      },
      collision_layer: {
        $ref: 'common.schema.json#/$defs/collision_bitmask',
      },
      collision_mask: {
        $ref: 'common.schema.json#/$defs/collision_bitmask',
      },
      is_static: {
        type: 'boolean',
      },
    },
  },
  raceModeState: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/race-mode-state.schema.json',
    title: 'Blob Royale protocol v3 race mode state',
    description:
      "The race's declared course and durations, followed by its observed finish standings. Each racer's next gate and return countdown are entity components. The generic match.placements array is empty for race because its eliminated_tick does not describe a finish.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: [
      'track_half_width',
      'checkpoint_radius',
      'track',
      'checkpoints',
      'time_limit_ticks',
      'finish_window_ticks',
      'standings',
    ],
    properties: {
      track_half_width: {
        $ref: 'common.schema.json#/$defs/positive_world_scalar',
      },
      checkpoint_radius: {
        $ref: 'common.schema.json#/$defs/positive_world_scalar',
        $comment:
          'No greater than track_half_width, enforced at configuration validation.',
      },
      track: {
        type: 'array',
        minItems: 2,
        maxItems: 4096,
        items: {
          $ref: 'common.schema.json#/$defs/vector2',
        },
        description:
          'Centreline nodes in declared map order; there is no closing segment. The bound mirrors the map marker ceiling.',
      },
      checkpoints: {
        type: 'array',
        minItems: 1,
        maxItems: 4096,
        items: {
          $ref: 'common.schema.json#/$defs/vector2',
        },
        description:
          'Gate centres in declared order. The last gate is the finish.',
      },
      time_limit_ticks: {
        $ref: 'common.schema.json#/$defs/safe_integer',
      },
      finish_window_ticks: {
        $ref: 'common.schema.json#/$defs/safe_integer',
      },
      standings: {
        type: 'array',
        maxItems: 1024,
        items: {
          $ref: '#/$defs/standing',
        },
        description:
          'Recorded finish order; simultaneous finishers share a placement and are appended in ascending entity order. Empty until a racer finishes.',
      },
    },
    $defs: {
      standing: {
        type: 'object',
        additionalProperties: false,
        required: ['entity_id', 'controller_id', 'placement', 'finished_tick'],
        properties: {
          entity_id: {
            $ref: 'common.schema.json#/$defs/entity_id',
          },
          controller_id: {
            $ref: 'common.schema.json#/$defs/controller_id',
          },
          placement: {
            $ref: 'common.schema.json#/$defs/placement_rank',
          },
          finished_tick: {
            $ref: 'common.schema.json#/$defs/tick_sequence',
          },
        },
      },
    },
    $comment:
      'Added in 2.5. Course geometry and standing order are gameplay invariants; arrays and every nested object are closed and bounded here.',
  },
  raceProgressComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/race-progress-component.schema.json',
    title: 'Blob Royale protocol v3 race_progress component',
    description:
      "The zero-based index of the next gate a racer must reach in declared order. Zero means no gate taken; the course's checkpoint count means finished. Progress persists while the racer's body is absent so the last gate determines its return location.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['next_checkpoint'],
    properties: {
      next_checkpoint: {
        $ref: 'common.schema.json#/$defs/safe_integer',
        $comment:
          "At most the course's checkpoint count, enforced by gameplay. Added in 2.5.",
      },
    },
  },
  respawnTimerComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/respawn-timer-component.schema.json',
    title: 'Blob Royale protocol v3 respawn_timer component',
    description:
      "The committed ticks left before an entity that was knocked out of play is offered a seat again. An entity carrying it has no physics_body; an entity without it is not respawning, so absence is the whole of 'in play' and no member can hold zero on a committed tick.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['ticks_remaining'],
    properties: {
      ticks_remaining: {
        $ref: 'common.schema.json#/$defs/safe_integer',
        $comment:
          'Ticks, not seconds, because the simulation stores ticks and nothing else on this wire is expressed in seconds. Added in 2.5.',
      },
    },
  },
  royaleModeState: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/royale-mode-state.schema.json',
    title: 'Blob Royale protocol v3 royale mode state',
    description:
      "The royale mode's non-entity-shaped match state (ADR 0005). Placements are generic and travel in match.placements; the zone is the zone component of its entity. What remains is previous_phase, which royale observed, and elimination_grace_ticks, which royale was configured with.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['previous_phase', 'elimination_grace_ticks'],
    properties: {
      previous_phase: {
        $ref: 'common.schema.json#/$defs/match_phase',
      },
      elimination_grace_ticks: {
        $ref: 'common.schema.json#/$defs/safe_integer',
        $comment:
          'Consecutive outside ticks the mode allows before it eliminates: the denominator of zone_exposure.outside_ticks, which every snapshot already carries. Required rather than optional, because a counter published without its bound is what made elimination read as arbitrary; a client that reads one always reads the other. Ticks, not seconds, because the mode stores ticks and nothing else on this wire is expressed in seconds. Zero is legal and means elimination on the first outside tick, so a client dividing by it must guard rather than treat zero as absent. Added in 2.2.',
      },
    },
  },
  scoreComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/score-component.schema.json',
    title: 'Blob Royale protocol v3 score component',
    description:
      'One integer scoreboard cell carried by a player entity or a team entity.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['points'],
    properties: {
      points: {
        $ref: 'common.schema.json#/$defs/signed_safe_integer',
      },
    },
  },
  seatNpcCommand: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/seat-npc-command.schema.json',
    title: 'Blob Royale protocol v3 seat_npc command payload',
    description:
      "Fill one empty seat with a declaration for a named NPC kind. The payload names no sender: the server stamps the sending session's controller id, so a controller id is not merely ignored here, it is a closed-schema violation that closes the connection.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['seat_index', 'npc_kind'],
    properties: {
      seat_index: {
        $ref: 'common.schema.json#/$defs/seat_index',
      },
      npc_kind: {
        $ref: 'common.schema.json#/$defs/controller_kind',
        $comment:
          "MUST be one of the names welcome.npc_controller_kinds published to this session. The grammar here is the whole schema check; membership is enforced by the server against the same list it published, and a name outside it is command_payload_invalid. The vocabulary is deliberately not an enum: it is read from the server's controller registry, so registering a bot costs no schema revision and no client change.",
      },
    },
    $comment:
      "Fills an empty seat and never replaces an occupied one, so two clients seating one seat in one tick resolve by the batch's order and the second press is a no-op rather than an eviction. Replacing an occupant is clear_seat followed by seat_npc, which the server applies in that order within one tick. Added in 2.3.",
  },
  setSeatCountCommand: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/set-seat-count-command.schema.json',
    title: 'Blob Royale protocol v3 set_seat_count command payload',
    description:
      "How many seats the pre-match lobby should have. The payload names no sender: the server stamps the sending session's controller id, so a controller id is not merely ignored here, it is a closed-schema violation that closes the connection.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['seat_count'],
    properties: {
      seat_count: {
        $ref: 'common.schema.json#/$defs/seat_count',
      },
    },
    $comment:
      'A count, not a delta, so two clients who both choose four agree rather than compounding. A shrink that would remove a filled seat is refused by the tick and changes nothing; a client renders that rule by flooring its control one above the highest filled seat. Added in 2.3.',
  },
  setThrustCommand: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/set-thrust-command.schema.json',
    title: 'Blob Royale protocol v3 set_thrust command payload',
    description:
      "A steering direction in the arena frame, each component in [-1, 1]. The payload names no entity: the server stamps the sending session's current body, so an entity id is not merely ignored here, it is a closed-schema violation that closes the connection.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['x', 'y'],
    properties: {
      x: {
        $ref: 'common.schema.json#/$defs/unit_interval_scalar',
      },
      y: {
        $ref: 'common.schema.json#/$defs/unit_interval_scalar',
      },
    },
    $comment:
      'The per-component bound admits (1, 1), whose magnitude is sqrt(2). Magnitude clamping to 1 is a mode rule applied by thrust_steering (ADR 0005), not a wire rule, so a client cannot gain 41 percent acceleration by thrusting diagonally. A decoder MUST reject non-standard JSON number literals such as NaN and Infinity.',
  },
  snapshotData: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/snapshot-data.schema.json',
    title: 'Blob Royale protocol v3 world snapshot data',
    description:
      'One complete immutable simulation tick: every published entity in ascending entity_id order with the components it carries, plus the generic match section. Never a delta, never partial.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['tick_sequence', 'entities', 'match'],
    properties: {
      tick_sequence: {
        $ref: 'common.schema.json#/$defs/tick_sequence',
      },
      entities: {
        type: 'array',
        maxItems: 1024,
        items: {
          $ref: 'entity-snapshot.schema.json',
        },
        $comment:
          'Every published entity counts against this bound, including map static bodies and the zone entity. Ascending unique entity_id is a normative invariant that JSON Schema cannot express; docs/protocol/v3.md owns it and C++ conformance tests assert it.',
      },
      match: {
        $ref: 'match-data.schema.json',
      },
    },
  },
  snapshotMessage: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/snapshot-message.schema.json',
    title: 'Blob Royale protocol v3 WebSocket snapshot message',
    description:
      'A world snapshot frame on an established session. message_sequence starts at 2 because the welcome message is always message 1 of a session.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['data', 'error', 'meta'],
    properties: {
      data: {
        $ref: 'snapshot-data.schema.json',
      },
      error: {
        type: 'null',
      },
      meta: {
        type: 'object',
        additionalProperties: false,
        required: [
          'protocol_version',
          'schema_id',
          'request_id',
          'message_sequence',
          'sent_at_utc',
        ],
        properties: {
          protocol_version: {
            $ref: 'common.schema.json#/$defs/protocol_version',
          },
          schema_id: {
            const: 'blob-royale://protocol/v3/snapshot-message',
          },
          request_id: {
            $ref: 'common.schema.json#/$defs/request_id',
          },
          message_sequence: {
            type: 'integer',
            minimum: 2,
            maximum: 9007199254740991,
          },
          sent_at_utc: {
            $ref: 'common.schema.json#/$defs/utc_timestamp',
          },
        },
      },
    },
  },
  startMatchCommand: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/start-match-command.schema.json',
    title: 'Blob Royale protocol v3 start_match command payload',
    description:
      'Somebody pressed Start. The payload is empty and the emptiness is the whole schema: which match, whose start, and from which tick are not questions a client gets to answer, and the closed shape is the enforcement rather than a preamble to it.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    properties: {},
    $comment:
      "It records a request; it does not start a match. The server commits lobby -> countdown only when the running mode's objective also says the field is complete, so a press into a lobby with an empty seat is remembered and changes no phase. The request is one-shot and is cleared on every arrival in lobby, so a finished match cannot restart itself. Added in 2.3.",
  },
  teamComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/team-component.schema.json',
    title: 'Blob Royale protocol v3 team component',
    description:
      'Optional side membership. An entity without this component is unaligned; team ids start at 1 so no value is a sentinel.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['team_id'],
    properties: {
      team_id: {
        $ref: 'common.schema.json#/$defs/team_id',
      },
    },
  },
  terrainData: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/terrain-data.schema.json',
    title: 'Blob Royale protocol v3 authored terrain',
    description:
      'Complete immutable terrain published once in welcome, in authored order. Support is the bounded positive ground minus the union of open circular holes. Compiled arrangements are not wire data.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['bounds', 'ground', 'corridors', 'holes'],
    properties: {
      bounds: {
        type: 'object',
        additionalProperties: false,
        required: ['width_world_units', 'height_world_units'],
        properties: {
          width_world_units: {
            $ref: '#/$defs/positive_world_scalar',
          },
          height_world_units: {
            $ref: '#/$defs/positive_world_scalar',
          },
        },
      },
      ground: {
        type: 'string',
        enum: ['solid', 'corridors'],
      },
      corridors: {
        type: 'array',
        maxItems: 8,
        items: {
          $ref: '#/$defs/corridor',
        },
      },
      holes: {
        type: 'array',
        maxItems: 32,
        items: {
          $ref: '#/$defs/hole',
        },
      },
    },
    allOf: [
      {
        if: {
          properties: {
            ground: {
              const: 'solid',
            },
          },
          required: ['ground'],
        },
        then: {
          properties: {
            corridors: {
              type: 'array',
              maxItems: 0,
            },
          },
        },
        else: {
          properties: {
            corridors: {
              type: 'array',
              minItems: 1,
            },
          },
        },
      },
    ],
    $comment:
      'Semantic validation additionally requires unique names per family, at most 32 segments and 40 points across all corridors, finite positive squared lengths for consecutive segments, centers and points inside the closed bounds, and exact bounds agreement with validated v1 configuration. Width and radius may extend outside the envelope. No clipping or fallback is allowed.',
    $defs: {
      positive_world_scalar: {
        type: 'number',
        exclusiveMinimum: 0,
        maximum: 1000000000,
      },
      point: {
        type: 'object',
        additionalProperties: false,
        required: ['x', 'y'],
        properties: {
          x: {
            type: 'number',
            minimum: 0,
            maximum: 1000000000,
          },
          y: {
            type: 'number',
            minimum: 0,
            maximum: 1000000000,
          },
        },
      },
      corridor: {
        type: 'object',
        additionalProperties: false,
        required: ['name', 'half_width', 'points'],
        properties: {
          name: {
            $ref: 'common.schema.json#/$defs/kind_name',
          },
          half_width: {
            $ref: '#/$defs/positive_world_scalar',
          },
          points: {
            type: 'array',
            minItems: 2,
            maxItems: 33,
            items: {
              $ref: '#/$defs/point',
            },
          },
        },
      },
      hole: {
        type: 'object',
        additionalProperties: false,
        required: ['name', 'center', 'radius'],
        properties: {
          name: {
            $ref: 'common.schema.json#/$defs/kind_name',
          },
          center: {
            $ref: '#/$defs/point',
          },
          radius: {
            $ref: '#/$defs/positive_world_scalar',
          },
        },
      },
    },
  },
  welcomeData: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/welcome-data.schema.json',
    title: 'Blob Royale protocol v3 welcome data',
    description:
      'Everything a session learns about itself, sent exactly once immediately after the upgrade and before any snapshot. accepted_command_kinds is an advertisement of what the boundary will accept from this client, not a grant: the server enforces the same set independently and InputBatch::create filters it a second time. npc_controller_kinds is the same kind of advertisement for the one closed vocabulary a client is allowed to name a value from.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: [
      'entity_id',
      'controller_id',
      'display_name',
      'mode',
      'map',
      'accepted_command_kinds',
      'npc_controller_kinds',
      'lobby_id',
      'seat_count_maximum',
      'terrain',
    ],
    properties: {
      terrain: {
        $ref: 'terrain-data.schema.json',
        $comment:
          'Required complete immutable authored terrain. Never repeated in snapshot frames.',
      },
      entity_id: {
        $ref: 'common.schema.json#/$defs/entity_id',
        $comment:
          "The session's FIRST body only. An eliminated or match-reset body is destroyed and the server seats the same controller_id on a new entity_id, so a client that caches this value renders the wrong blob after one match. Resolve the own body by controller_id.",
      },
      controller_id: {
        $ref: 'common.schema.json#/$defs/controller_id',
        $comment:
          'Durable for the whole connection and the only stable self-identifier. A reconnect is a new join and receives a new controller_id.',
      },
      display_name: {
        $ref: 'common.schema.json#/$defs/display_name',
      },
      mode: {
        $ref: 'common.schema.json#/$defs/mode_name',
      },
      map: {
        $ref: 'common.schema.json#/$defs/map_name',
      },
      accepted_command_kinds: {
        type: 'array',
        maxItems: 5,
        uniqueItems: true,
        items: {
          $ref: 'common.schema.json#/$defs/command_kind',
        },
        $comment:
          "maxItems tracks the size of common.schema.json#/$defs/command_kind, which went from one to five in 2.3. The published set is the intersection of the mode's accepted kinds with the client-sendable vocabulary, so it can never name a server-issued kind such as spawn or despawn, and a mode without a lobby publishes only set_thrust.",
      },
      npc_controller_kinds: {
        type: 'array',
        maxItems: 64,
        uniqueItems: true,
        items: {
          $ref: 'common.schema.json#/$defs/controller_kind',
        },
        $comment:
          "The exact set of NPC kinds a seat_npc command may name, read from the server's controller registry. maxItems is deliberately NOT the number of registered bots: it is a protocol constant, so registering a bot changes this array's contents and not this schema, which is what makes a new bot appear in every client's seat menu with no client change and no protocol version. Empty is legal and means no bot kind is registered, in which case no seat can be filled with one. Added in 2.3.",
      },
      lobby_id: {
        $ref: 'common.schema.json#/$defs/lobby_id',
        $comment:
          'The room this session was admitted into, named by the canonical /api/v3/lobbies/<lobby_id>/session path. There is no room-one alias.',
      },
      seat_count_maximum: {
        $ref: 'common.schema.json#/$defs/seat_count',
        $comment:
          "The most seats this room's map can seat -- its spawn-marker count capped at the seat bound -- so a client can floor its seat-count control without a second round trip. A set_seat_count above it is ignored by the tick. Added in 2.4.",
      },
    },
  },
  welcomeMessage: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/welcome-message.schema.json',
    title: 'Blob Royale protocol v3 WebSocket welcome message',
    description:
      'The first and only welcome frame of a session. It is always message_sequence 1, so a client that receives a snapshot first has lost a frame and must close.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['data', 'error', 'meta'],
    properties: {
      data: {
        $ref: 'welcome-data.schema.json',
      },
      error: {
        type: 'null',
      },
      meta: {
        type: 'object',
        additionalProperties: false,
        required: [
          'protocol_version',
          'schema_id',
          'request_id',
          'message_sequence',
          'sent_at_utc',
        ],
        properties: {
          protocol_version: {
            $ref: 'common.schema.json#/$defs/protocol_version',
          },
          schema_id: {
            const: 'blob-royale://protocol/v3/welcome-message',
          },
          request_id: {
            $ref: 'common.schema.json#/$defs/request_id',
          },
          message_sequence: {
            type: 'integer',
            const: 1,
          },
          sent_at_utc: {
            $ref: 'common.schema.json#/$defs/utc_timestamp',
          },
        },
      },
    },
  },
  zoneComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/zone-component.schema.json',
    title: 'Blob Royale protocol v3 zone component',
    description:
      'The safe-zone circle carried by the zone entity, in wu. The zone entity owns no physics_body and no controllable. This component is the only place the zone is published.',
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['center', 'radius'],
    properties: {
      center: {
        $ref: 'common.schema.json#/$defs/vector2',
      },
      radius: {
        $ref: 'common.schema.json#/$defs/nonnegative_world_scalar',
      },
    },
  },
  zoneExposureComponent: {
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://schemas.blob-royale.invalid/protocol/v3/zone-exposure-component.schema.json',
    title: 'Blob Royale protocol v3 zone_exposure component',
    description:
      "Consecutive committed ticks an entity's center has been outside the zone. An absent component reads as zero.",
    'x-status': 'Accepted',
    type: 'object',
    additionalProperties: false,
    required: ['outside_ticks'],
    properties: {
      outside_ticks: {
        $ref: 'common.schema.json#/$defs/safe_integer',
      },
    },
  },
} as const;
