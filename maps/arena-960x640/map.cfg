# canonical: arena_960x640_map -- the shipped arena, reproducing the pre-framework world.
#
# The bounds match the world every accepted fixture and the deployed read-only build used before
# maps existed, so switching the deployment to a map changes no committed physics.

[map]
name=arena-960x640
display_name=Arena 960x640

[bounds]
width_world_units=960
height_world_units=640

[terrain]
ground=solid
